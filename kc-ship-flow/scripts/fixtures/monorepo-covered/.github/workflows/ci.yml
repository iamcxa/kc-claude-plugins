name: CI

on:
  push:
    branches: [main]

jobs:
  pr-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: install island deps
        working-directory: experiments/island
        run: npm install
      - name: run island tests
        working-directory: experiments/island
        run: npm test
