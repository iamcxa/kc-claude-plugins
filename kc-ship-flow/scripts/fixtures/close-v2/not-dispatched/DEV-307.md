---
title: "DEV-307: deferred fixture task, never dispatched"
status: backlog
source:
product: kc-ship-flow
planning-window:
planning-outcome:
sprint: ship-cloud-wrapper
sprint-readiness: defer
started:
completed:
verdict:
worktree:
issue:
pr:
mod-block:
id: fixtureddd003
---

Fixture task: shares `sprint: ship-cloud-wrapper` with DEV-301 but was deferred out of the batch
before `dispatch.sh` ran, so it has no key in `_ship_fence/ship-cloud-wrapper.json` and no `pr:`
(never merged) -- the r3 bite's shape. `close.py` must drop it from the batch's task set, print
`close: DEV-307 not dispatched, skipping` once to stderr, and close the rest of the batch without
it -- not fail with `not all tasks merged`, and not appear in the written receipt.
