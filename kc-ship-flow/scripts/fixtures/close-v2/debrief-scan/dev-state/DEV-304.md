---
title: "DEV-304: merged fixture task with no fence entry at all"
status: done
source:
product: kc-ship-flow
planning-window:
planning-outcome:
sprint: ship-cloud-wrapper
sprint-readiness: ready
started: 2026-09-11T00:00:00Z
completed:
verdict:
worktree:
issue:
pr: pr-merge:504
mod-block:
id: fixtureddd002
---

Fixture task: merged via the pr-merge mod at PR #504; dispatched outside `dispatch.sh`, so the
fence has no key for this slug at all (AC-4) -- it must still be eligible to be "merged" from its
own `pr:` field alone, and must still appear in the receipt with `workspace_id: null` rather than
being counted as a dispatch/merge failure. Its worker's debrief is only named in the body of a
`_debriefs/*.md` file, not in that file's `scope:` frontmatter.
