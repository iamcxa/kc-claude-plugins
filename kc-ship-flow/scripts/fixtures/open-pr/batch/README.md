# batch open-pr fixture — synthetic record for open-pr.sh's own contract test

Two synthetic issues, one per BRANCH used by contract-test.py's open-pr.sh cases (the dry-run body
build and the real bound/unbound origin-binding cases share this one batch-dir).
