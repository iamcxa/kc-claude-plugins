# batch open-pr fixture — synthetic record for open-pr.sh's own contract test

One synthetic issue per BRANCH used by contract-test.py's open-pr.sh cases (the dry-run body build,
the real bound/unbound origin-binding cases, the empty/no-boundary lead refusals, the `--fixes`
multi-issue case, and the backtick-quote lead case all share this one batch-dir).
