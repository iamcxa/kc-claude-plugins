# Evidence limits and next proof

Direction is change. The original admission-to-decision interval was 1163 seconds (19 minutes 23 seconds), including 734 seconds before working started and one additional Captain intervention to correct stage routing. The native guard binds admitted_at to frontmatter started, so its required clock is separately 429 seconds. Neither clock is silently reset or presented as the total orchestration cost.

The accepted input already contained a complete Development Brief. The producer drafted the brief directly from that accepted source and generated a board alongside it. This is evidence of a successful source/brief-to-clean-context-consumer handoff, not a causal board-to-brief elicitation experiment. The board IDs are an agent-authored projection, not newly accepted product decisions.

One actual consumer handled the missing-decision control first, then the happy input. It had no full-answer access during the control. This supplies bounded behavioral evidence, not a general semantic guarantee. The consumer performed actual technical shaping but did not enter a native ideation stage or execute development tasks.

Existing local generator emitted 45 canvas records covering six of six activities. No live host selection UI, rendering server, browser display, canvas edits/readback, or end-user acceptance was exercised. A local board record is not a viewed usable board. Intent-only stories omit status per existing conversation-map contract; generated MISSING status labels do not establish implementation gaps.

The original selected renderer lacked dependencies. Two failed attempts exposed missing yaml then @tldraw/utils; unchanged copied renderer files plus declared existing local dependencies then succeeded. This is a machine-local dependency limitation, recorded in renderer-provenance.json. It required no product edits, network install or paid provider call.

Next smallest missing proof: use accepted board decisions that are not already a completed brief; derive the five-section brief from the board's pinned source and record each decision/unknown's origin, then repeat one independent technical consumer. Include a genuinely unresolved user scope decision only if present, leaving it unresolved until answered. Separately name whether host UI/display is part of that next proof. No new task, delivery profile, Pilot promotion or chooser/kernel edit is authorized by this recommendation.

Cleanup is pending Captain outcome approval. Disposable .context/journey-handoff-poc contains source copies, renderer/dependency link and outputs; remove the directory after durable evidence acceptance. The isolated code worktree has no product diff. State evidence remains the audit artifact. No retained mechanism means a retained-code without-it check is not applicable.
