# Authorized execution scope

Captain explicitly approved the one-task ideation -> implementation correction and resumption in current chat: "就這樣走". Original admission was 2026-09-11T04:45:17Z; working started is 2026-09-11T04:57:31Z. Preserve both timings and record the extra Captain intervention; never claim zero total orchestration overhead. User authorization resumes the planned functional experiment despite that pre-execution blocker; keep overall direction change where the existing close guard requires it.

Use installed kc-dev-flow 4.3.0 for this new task only. Pinned stage is implementation-1 in stage-pin.json. Read the exact stage envelope with profile-contract-loader.py and its selected kernel/base/build, not other profile bundles. No retained source, CI, version, PR or provider edits are authorized in this disposable POC. Existing top stack tools can be read/executed at /Users/kent/conductor/workspaces/kc-claude-plugins/tacoma/.context/worktrees/journey-stack-recut/.context/local-stack/journey-local-progress/kc-journey-map. Do not change that worktree. It includes the existing YAML renderer and historical examples. Input authority is the sibling plan-release-from-journey-board/index.md accepted intent, not the historical example.

Work in the assigned isolated worktree; keep disposable outputs in its .context and evidence under this state task. Producer first derives a valid journey board/YAML from accepted intent, researches only needed factual uncertainties, and writes the single brief. Send FO the absolute brief/source paths and exact revision/digest when ready. FO dispatches one clean-context consumer (the second and final worker; no review panel). Do not spawn additional agents yourself. While consumer runs, execute the missing-decision control and compare the current chooser/kernel principles. The consumer must actually shape necessary development work using only the handoff, not receive your expected answers. FO will relay its result for your final report.

Keep one bounded happy run and one control. Stop by 05:12:31Z if not decision-ready; no silent timer reset. Record useful partial evidence with change if needed. No new Captain answer is needed for settled decisions; a genuinely missing human decision must remain unresolved. Do not fabricate a selection UI call or response. Explicitly state whether live host selection/rendering was exercised or only the local planner/handoff seam.

Record current guard-compatible POC outcome and pending close measurements; inspect installed poc-close-guard.py and profiles/poc-exploration/prove.md for schema. Commit only state evidence/report via existing state owner, never auto-commit product files. Keep future approval wait/cleanup pending. You are valued; focus on the smallest falsifiable handoff and return concrete paths promptly.
