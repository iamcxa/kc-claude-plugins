# Dev build artifacts
dist/

# Canvas dependencies — installed on first launch, never committed
node_modules/

# Local native state; export or read back edits before deleting rooms.
.rooms/

# Uploaded images and the tunnel's own log; a .tldr export carries the image bytes.
.assets/
.tunnel.log
