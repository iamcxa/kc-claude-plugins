# Focused independent re-review: PASS

The original real-server PATCH -> journey-read CLI --out -> YAML identity
instrument was rerun in new owned rooms on both corrected intermediate layers.
Only the layer location, output location and expected tree binding changed.
The original failed runtime evidence remains under `../initial`.

Both same-wording second-ID cases and both distinct-wording controls pass.
All four CLI calls exit zero; every source SHA-256 is unchanged. Full parsed
YAML comparison confirms the sole semantic difference is the requested second
story's wording, including preservation of all other activities, release fields
and layer-3 evidence metadata. Both owned servers stopped.

Bindings: layer 2 `9702db9d0b908b03d44e92dcc87ec20a16a61731`, layer 3
`1738d173046d750819cd40e8ee838dd3eace114e`. `binding.json` confirms the read-file
hashes and frozen producer README match the correction record and neither snapshot
has unstaged tracked changes. `layer-*/same-wording-proof.json` contains real CLI
outputs, expected/actual stories and source hashes. `full-model-proof.json`
records comparison of all output model fields.

Reproduction: set `RECUT_SNAPSHOT_ROOT` to the candidate worktree's `.context`
directory and run the respective `layer-*/same-wording-probe.mjs` with Node after
the declared dependencies are installed. Probes create only disposable fixture
files/rooms beside themselves and stop only their own ephemeral loopback servers.

The reviewed +5/-1 existing stable-ID selector and stale-wording refusal block
is the only promoted code. Producer equivalence and patch-replay receipts were
inspected, not rerun. Layer 3 now equals its original extraction merge plus that
explicit block; layers 1/4/5 and final accepted semantics remain unchanged.
The producer's stale-wording refusal and affected 10/16 read tests plus 1/5-page
smokes remain their owned evidence. No full suite, install or layout rerun.

No unresolved finding remains within this single correction's re-review scope.
This PASS permits presenting the exact local-commit proposal; it does not grant
commit, push, PR, merge, release or delivery acceptance. Task status and Captain
gates were not changed. Original FAIL and the FO-recorded correction round remain
intact. External icon appearance and reused dependencies remain stated limits;
CI cost remains unmeasured. The inherited layer-3 EOF blank line is unchanged.
