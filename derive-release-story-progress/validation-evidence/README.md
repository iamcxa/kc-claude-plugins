# Independent validation evidence

Verdict: PASS for the approved local Pilot slice; product commit and delivery remain Captain-owned.
Candidate: uncommitted 10-file manifest `f0261937f5c5a92455add6e86215c1a7d2444dec1b6fcd6e3e70bbe537128e99`, base/HEAD `bc51b2464659594d3c06806442c77ec15319962b`, branch `codex/journey-progress`. Entry and exit checked every manifest hash and empty product staging. `exit-binding.json` independently measures 460 changed lines / 125 reader+CLI lines.

## Independent exercise

Used separate `.context/progress-validation/source.yaml` and `workflow/` copies and only the new `progress-validation-native` room for edits. The original source/tasks were hashed before and after and unchanged. Existing preview services were reused without restart.

1. Ran the real `lib/journey-progress.mjs source.yaml --workflow-dir workflow --draw progress-validation-native --pages story-map,journey-board` with `JOURNEY_API=http://127.0.0.1:5863`. The copied workflow initially contained all-done R1 tasks; the copied model also included unmapped R2. Exit 1 accompanied successful draw 200 and the unknown R2, consistent with the documented diagnostic contract.
2. Changed only copied `workflow/b.md` from done to implementation. Before refresh the server retained a green R1 alpha snapshot (`snapshot-before-refresh.json`). Repeating the explicit CLI changed alpha to gap/red and R1 from 2/2 to 1/2 complete stories (`refresh.json`, `snapshot-after-refresh.json`). This input refusal/change falsifier distinguishes a timestamped room from automatic live sync and would fail a renderer that discarded new progress.
3. Opened the new R1 board. Pointer double-click selected the card, but did not reliably enter text editing; an initial Enter/fill automation attempt inserted unwanted whitespace/concatenated wording in this disposable room. Recovered using programmatic `editor.select` / `setEditingShape`, then actual native textbox keyboard Meta+A and typing, followed by Escape. No programmatic richText update was used. The final wording was `Review every required task before acceptance`.
4. Ran `JOURNEY_API=http://127.0.0.1:5863 node lib/journey-read.mjs source.yaml progress-validation-native --out readback.yaml`. It reported exactly one reworded story with empty conflict/release/order lists. A byte comparison proved the only source-copy change was that card wording (`native-readback-proof.json`); authored exists stayed exists and no derived keys entered YAML. An initial incorrect readback argument order refused ENOENT and wrote nothing; the corrected documented invocation above passed.
5. Reloaded the actual browser page and independently read both client records and HTTP document records. The wording persisted, authored exists remained, derived gap remained, and the shared native border remained red. `native-reloaded.json` and PNG bind this observation. The earlier longer/whitespace native edit also produced growY 72 with border height 272 and red color, exercising native resize synchronization before recovery.

The keyboard/edit instrument can detect source contamination: producer `wording-leak.txt` demonstrates a failing source-write assertion after adding derived text to story wording; the final independent exact-byte assertion would fail any additional YAML edit. The saved-room before/after pair distinguishes green from red without a standing new harness. Producer five progress, three focused readback and three authored lint passes and all six producer mutation failures were inspected alongside their actual mechanisms; no unchanged green owned suite was rerun.

## Preview and preservation

- [Partial story map](http://127.0.0.1:3743/?room=progress-mixed&d=v-287.-371.2015.1511.page)
- [All-done R1 board](http://127.0.0.1:3743/?room=progress-all-done&d=v-568.-617.2576.1932.jm-board-R1)
- [Independent edited R1 board](http://127.0.0.1:3743/?room=progress-validation-native&d=v-568.-617.2576.1932.jm-board-R1)

The first two were actually opened and visually checked at 1600x1200, approximately 79% and 62% zoom; preserved PNGs were also inspected. The old `d=v0.0.1.1.*` opened at 800%; these links record real viewport bounds, with no product-link change. Native client API calls were used only for page/camera positioning and edit entry; screenshots show readable provenance, three-color legend, distinct development/acceptance language and no new layout overlap at the observed viewport. This is not target-user acceptance/usability proof.

Opening the pristine rooms adds `user:*` records to SQLite. Their DB file hashes therefore changed; no room file was overwritten/restored. Mixed shape updates stop at producer clock 4 and its later clock 5 is a user record; all-done shapes remain clock 1 and later clocks are user records. Source, task, story and border content were preserved. Original services 3737/3741/5858 were not controlled; candidate PIDs 30751/30667 remained live. Named browser `progress-validation` was closed. The new room and local `.context/progress-validation` are temporary review scaffolding removable with the candidate preview after Captain review; evidence copies here remain durable.

## Necessity, documents and authority

The 10 files map to the actual local reader/CLI, shared presentation, both projections, falsifiable focused tests, one new mapping/refresh skill and three bounded documentation integration points. Independent per-section inspection found one mapping-contract home; the drawing skill only links, canvas owns rendering/readback, and cell-contract owns meaning. The CLI and native readback exercise spot-check the changed documentation; the producer mutation mechanisms establish without-it necessity. Dense reader code remains a single bounded path rather than a new abstraction/provider layer; no line-count-only justification, new dependency, writer, CI, migration or background service is retained.

Project-context receipt: impact=none; authority=PRODUCT.md/ARCHITECTURE.md/CLAUDE.md; surface=existing plugin/workflow/provider/delivery boundaries; stale_claim=none introduced; approved_change=none; landed_change=none; validation_evidence=exact 10-file diff, actual explicit-refresh snapshot transition and native readback/byte preservation above. Existing catalog omissions remain inherited and outside this slice. Open GitHub listing still includes precursor PR #394 at `1166747c22f0d6c62db098e5f8cad61bf81174bd`; no progress PR exists, so no candidate provider-feedback disposition or delivery action was claimed.

Pinned kc-dev-flow 4.1.1 loader accepted `validation-stage-pin.json`, attempt `spacedock-ensign-91n5fvm5qt-validation`, digest `9a592aa4b6a1b06ebad5125241f8d902e04afbbc84437cd55a2b5bf41bc9e776`. Loaded retained-document, project-context and delivery-branch-base references because their actual triggers apply; no Planning Receipt/provider reader/comparator, portable PR ceremony, Production runbook or optional panel was activated. Producer RoboRev UNAVAILABLE remains non-gating. CI was unchanged and cost per PR unmeasured. Fixture progress is not real-project progress; scope declaration completeness cannot reveal wholly omitted work, and observation is bounded rather than atomic. Product commit, push, PR, merge/release and delivery acceptance remain outside validation authority.
