# Three-unit approved necessity reduction — immutable producer closure

Captain approved this bounded reduction on 2026-09-11. This record binds the replacement product candidates to the prior minimal-necessity rejection, actual approved changes and producer verification. It is not independent review, local-commit approval or delivery acceptance.

## Exact candidates

Pinned main: `c9c5752fda853737d4a937ad7f59564c5651ca53`. Former cumulative trees 2, 4 and 5 supply the three units. They and all earlier evidence remain frozen. `trees.json`, `unit-N-files.json` and `unit-N.patch` bind each exact adjacent change; replay reproduces all three trees. No product commits exist.

| Unit | Candidate tree | Files | Added/deleted | Generated added/deleted |
|---|---|---:|---:|---:|
| 1 | `063df758d06b2b2170a3378cdff01c4195fbc2b0` | 42 | +6996/-311 | +5245/-0 |
| 2 | `a117b410f9b7dde03ee081f5545b4d9755d77e7b` | 35 | +2194/-160 | +1/-0 |
| 3 | `b52f204f4c6a2b6ec09f2657adcca272e97dc1a0` | 10 | +427/-27 | +0/-0 |

Generated lines are package-lock and native `.tldr`; one native line holds the full JSON. Exact file lists include all source/generated additions, edits and legacy skill removals. Unit 1 already supplies the native planning replacement before removing the old team-ops skill. Unit 2 adds release evidence/detail and standard borders. Unit 3 adds explicit optional local-task progress. Existing Draft #394 remains unchanged and is not a fourth layer.

## Approved changes

Removed the unused frame export and direct CORS/compiler dependencies, plus CORS registration. The actual approved two-dependency lock removes **five** nodes: @fastify/cors, fastify-plugin, mnemonist, obliterator, typescript. No new node or retained-node value changed. React Vite plugin, Fast Refresh, typed source, tsconfig, tsx and runtime versions remain. `dependency-diff.json` and `installation.json` record the exact lock comparison and fresh private npm ci (262 installed, 263 audited, zero vulnerabilities). Units 2/3 reuse that private installation; runtime symlinks are excluded from every candidate tree.

Applied only the scoped document/comment dispositions: nonexistent HTML fallback, destructive room deletion wording, unsupported `later`, misleading projection/status claims, authored event order versus inferred causality, and unmeasured CI/browser claims. Retained unique model/evidence/readback/portability contracts and complete example reproduction. No whole document deletion, new docs framework, schema-error translation, version bump or CI lane. Dated source is frozen in `historical-source/`; example YAML was updated and native example regenerated in unit 2, inherited byte-for-byte by unit 3.

## Actual verification

- Unit 1: 17/17 model/readback tests. Real API PATCH -> `journey-read --out` same-wording second-ID case and distinct-wording control both pass; source hashes unchanged. `same-wording-unit-1.json` and `candidate-check-binding.json` bind the full outputs and final tree. Unit 2's 67/67 suite includes its stronger standing same-wording identity regression. Unit 3 readback source is unchanged from unit 2.
- Unit 2: 67/67 model/example/evidence/readback tests. Unit 3: 9/9 affected progress/example tests (not an additional full suite). Version parity passes on all three snapshots. No repeated fresh installations or browser runs per layer.
- Real browser origin `http://127.0.0.1:52248` connected via WebSocket to API `http://127.0.0.1:52240`; Node REST drew/read/imported records across the supported topology. React Fast Refresh resource loaded. No CORS proxy. Only the client API address was substituted in a disposable runtime copy to protect existing ports/services; candidate transport source is unchanged.
- Native story edit and independent hand note survive browser reload; separate native note survives actual API-server restart. Deleted source story is reconciled away while manual note survives redraw. Malformed mixed batch returns detailed HTTP 400 and persists neither malformed nor earlier valid record. Existing schema prevalidation/error diagnostics were retained, not replaced.
- Live native export contains 5 pages, 277 records, 258 journey-tagged records. Actual CLI import into an owned room succeeds; CLI readback reports no drift. Independent artifact assertions verify both violet unverified host-selection borders and the rendered authored-input/no-causality-inference rule. PNG export via existing editor.toImage seam produces 2374 × 4134. Serializer/toImage library seams were exercised at isolated ports; unchanged fixed-port CLI wrappers are bound by exact source identity to prior proof.
- 17 retained-projection comparisons pass against former snapshots (unit 1 story map; units 2/3 fixture and updated-example selections). All fields compare exactly except random fractional-index bytes normalized to sibling rank. Frame export absent in all three. Comparing two dependency installations in the same comparison process emits a duplicate-tldraw-library warning; the actual browser runtime loads one installation. Browser emitted an existing zh-tw missing-message warning; it did not block editing/export. Probe setup initially used the later-layer API on unit 1 and an overbroad wording assertion; both probe assumptions were corrected without product edits.

## Retained negative/control evidence and acceptance boundary

`retained-code-identity.json` binds the original observed without-it failures in `../minimal-necessity-evidence/README.md`: removing disk persistence loses a manual note after restart; removing reconciliation retains a deleted story; removing release bands loses the unassigned story; removing borders loses status colors; admitting prose-only evidence falsely passes. Original passing controls remain frozen. Retained mechanism source is byte-identical or differs only in explicitly bounded whole-line comments; records/server differ only by approved frame/CORS removal. The 17 projection comparisons and actual runtime checks bridge those removals. This is measured bounded necessity, not a claim of mathematical minimality.

Prior stable-ID negative/control repair remains frozen at `stack-recut-evidence/correction/README.md`; new same-wording proof confirms the promoted repair remains in the complete bottom unit. `retained-code-identity.json` also binds unchanged progress reader/CLI, source-safe readback and exporter code to prior AC-1..AC-5 and six progress mutations. No provider read or planning receipt was added; development completion still leaves delivery acceptance pending.

Old candidates, user source, existing rooms and services were not mutated. Owned browser/API/Vite processes stopped; `owned-services.json` confirms both owned ports closed. Candidate tracked working-tree diffs are empty; root worktree remains clean on codex/journey-stack-recut. Runtime rooms/evidence stay in task scratch. Product commit/push/PR/ready/merge and task/gate status changes remain unauthorized. CI cost per PR is unmeasured. Optional implementation-exit RoboRev is UNAVAILABLE for these uncommitted trees: zero requests. Retained independent validation and later exact local-commit approval remain outstanding.
