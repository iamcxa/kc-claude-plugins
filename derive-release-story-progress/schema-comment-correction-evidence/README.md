# Immutable schema-comment correction closure

The authorized correction closes only the producer edit for the independent `schema-rationale` documentation finding. The earlier independent verdict is preserved; this is exact byte/tree comparison, not fresh independent runtime validation or approval.

Changed only `kc-journey-map/server/canvas-server.ts`: replaced the three-line false browser-crash rationale with exactly:

```ts
// Validate PATCH records before writing so malformed client input receives detailed HTTP 400 diagnostics.
```

`comment-only-proof.json` proves exact substitution/inverse, identical executable bytes, and every other tree entry (blob and mode) unchanged in each candidate. The same corrected server blob is inherited across all three. Existing frame/dependency removals and generated native example remain untouched. Units 2/3 adjacent patches are byte-identical to the prior packet. No install, browser/server start, suite, example regeneration or dependency lookup ran.

| Unit | Old tree | Corrected tree | Adjacent files; + / - |
|---|---|---|---|
| 1 | `063df758d06b2b2170a3378cdff01c4195fbc2b0` | `b5bbc4d8ef0f800072fd9635389c481590ff05fb` | 42; +6994 / -311 |
| 2 | `a117b410f9b7dde03ee081f5545b4d9755d77e7b` | `058298ebd763f16e395c77ebeea1474a193cddc0` | 35; +2194 / -160 |
| 3 | `b52f204f4c6a2b6ec09f2657adcca272e97dc1a0` | `8a581c3c1bdbc2d11783f35cc040cfafd72c9f3f` | 10; +427 / -27 |

Base: `c9c5752fda853737d4a937ad7f59564c5651ca53`. Exact paths, generated-line counts, subjects and cumulative patches are in `trees.json`, `unit-N-files.json`, `unit-N.patch` and `local-commit-review.md`. The only reduction from the prior packet is two comment lines in unit 1.

Prior finding: [finding.json](../three-unit-independent-validation/finding.json), SHA-256 `b5a457731a380c6a85df8436f44e0478ba76ac495fe6562f665c69d2bf36c4c5`. Prior [independent validation](../three-unit-independent-validation/README.md) and [producer proof](../three-unit-reduction-evidence/README.md) remain frozen. Their AC-1..AC-5 behavior, source preservation, five-node dependency reduction and retained negative/control evidence carry forward through this exact comment-only binding; no broader PASS is asserted here.

New complete source snapshots live under the owned worktree `.context/schema-comment-correction/unit-N`; old snapshots are unchanged. No product commits, provider/gate/status mutation or service interaction occurred. Optional uncommitted-tree reviewer availability remains UNAVAILABLE with zero requests; CI cost per PR remains unmeasured. The packet is held for the Captain decision through First Officer orchestration, with no new review loop.
