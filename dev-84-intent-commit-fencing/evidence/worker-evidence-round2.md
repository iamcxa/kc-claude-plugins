## Evidence
DISPATCH_TOKEN: effeac9f1e5a79d2
CANDIDATE_SHA: a6ad0c498e6fd5e38f84c6b4f8da48ac1d58829c
BRANCH: feature/dev-84-intent-commit-fencing-at-most-one-worker-across-holder
BASE_SHA: 3f263c3bb7e91144e3793e3d4f5cf1368906f436
FILES: docs/dev/README.md, scripts/ship-flow/fenced-dispatch.sh, scripts/ship-flow/intent.sh
TESTS: python3 scripts/kc-dev-flow-contract-test.py -> exit 0
SURFACE: scripts/ship-flow/intent.sh -> falsifier | `scripts/ship-flow/intent.sh commit <state> <holder> <writer> <claim> <token> <project> <base> <sha>` against a SIGKILL-orphaned stale lock (see LOCK falsifiers (a) and (b) below; touches scripts/ship-flow/intent.sh) | git checkout 3f263c3bb7e91144e3793e3d4f5cf1368906f436 -- scripts/ship-flow/intent.sh
SURFACE: scripts/ship-flow/fenced-dispatch.sh -> falsifier | `scripts/ship-flow/fenced-dispatch.sh <state> <holder> <writer> <claim> <project> <base> <msg> --pause-before-adopt` (see AC-2 SIGSTOP evidence below; touches scripts/ship-flow/fenced-dispatch.sh) | git checkout 3f263c3bb7e91144e3793e3d4f5cf1368906f436 -- scripts/ship-flow/fenced-dispatch.sh
SURFACE: docs/dev/README.md -> AC-5 | python3 scripts/kc-dev-flow-contract-test.py checks docs/dev/README.md's pinned intent-commit phrases (see AC-5 mutation run below) | git checkout 3f263c3bb7e91144e3793e3d4f5cf1368906f436 -- docs/dev/README.md
WITHOUT_IT_COMMAND: python3 scripts/kc-dev-flow-contract-test.py
WITHOUT_IT_REMOVED_VARIANT: git checkout 3f263c3bb7e91144e3793e3d4f5cf1368906f436 -- docs/dev/README.md
WITHOUT_IT_OBSERVED: retained -> exit 0 ("kc-dev-flow contract: PASS"); removed -> exit 1 ("Ship-flow runtime omits the intent-commit rule: the production entry issues at most one `conductor workspace create` call after a committed intent")
ROBOREV: UNAVAILABLE(reason: no reviewer binary in workspace)
LOCK: Three P1s closed. (1) Trap ordering: `trap release_lock EXIT` is now registered unconditionally at script top level (before `case "$cmd"` dispatch, before any `mkdir` is attempted), and `release_lock` removes a lock only when it is provably this process's own — either the owner marker's pid field matches `$$` (steady state) or, when no marker exists yet, the script-global `LOCK_ACQUIRED=1` flag alone (settable only by this process's own successful `mkdir`), covering "died between acquire and metadata write." (2) Host-safety: staleness is now judged from the lock directory's own mtime via a portable `python3 os.path.getmtime` read (no `stat`, no GNU-only `date` flags) and `kill -0` was removed entirely — a remote/foreign pid is never consulted. (3) TOCTOU: a stale takeover never calls `rm -rf` on a lock this process didn't create; it does `mv <lock> <lock>.stale.<epoch>.<pid>` (atomic rename, harmlessly failing if the source already vanished under a racing reclaimer) and only then retries `mkdir`; the relocated `.stale.*` copy is deleted only after this process holds the fresh lock (or, on this process's own later exit without ever acquiring, by the same exit trap — never touching anyone else's lock). Falsifier (a): a real `mkdir "$LOCKDIR" && exec sleep 30` child was SIGKILLed right after its `mkdir` returned (confirmed directly: the lock dir existed, 0 entries, no `owner` file — exactly "acquired, died before the marker write"); after a 4s wait, `SHIP_LOCK_STALE_S=3 intent.sh commit ...` reclaimed it immediately ("committed sigkill-claim", exit 0), and no `.stale.*` or `ship-lock.d` artifact was left behind afterward. Falsifier (b): with a second such SIGKILL-orphaned stale lock (aged past the 3s test threshold), two `intent.sh commit` calls for different claims (race-x, race-y) were launched concurrently with `SHIP_LOCK_STALE_S=3`; both eventually exited 0 ("committed race-x", "committed race-y") but with a measurable timing split (0.33s vs 0.13s — one retry cycle's worth of `sleep 0.2` apart), i.e. exactly one won the immediate post-relocation `mkdir` and the other backed off and waited its turn; both commits landed cleanly in `git log` with no corruption and no leftover lock/stale directories.
BINDING: Unaffected by this round (no change to `adopt`'s project-comparison code, only to `lock()`). Round-2 evidence stands: wrong-project-entirely refusal exit 8 via the `conductor` test shim; same-name-different-project reconcile-counting fix (old logic "ambiguous intent" exit 5 -> new logic filters by project and adopts, exit 0) confirmed with two real workspaces (now archived) plus a shim-based cross-project falsifier.
AC-1: Re-verified against the round-3 candidate: intent already committed for `dev-84-ac1d`; `fenced-dispatch.sh` for the same claim died at "intent not committed (exists or fenced); reconcile instead of create" exit 4, no `conductor workspace create` line reached.
AC-2 (DEV-79 SIGSTOP falsifier): Not re-run with a new real workspace this round (this round touched only the internals of `lock()`, called identically by `commit`/`adopt` as before; no new probe spend). Round-2 re-run stands: holder-a created a real workspace and SIGSTOPped before adopt; holder-b handed over and `reconcile` adopted it ("adopted ... (exact name match)", exit 0, no `conductor workspace create` in B's path); resumed holder-a's own adopt was fenced (exit 3).
AC-3: Unaffected by this round; round-1 evidence stands (early reconcile blocks "unresolved intent" exit 4 with zero creates; second reconcile after the delayed create lands adopts it, exit 0).
AC-4: Unaffected by this round (the without-it variant never calls `intent.sh`); round-1 evidence stands (two live workspaces for one claim without intent-commit, both archived).
AC-5: Unaffected by this round; re-confirmed exit 0 again via the full contract-test run above on candidate a6ad0c4.
AC-6: No probe Conductor workspaces were created this round (all three falsifiers — trap ordering, SIGKILL staleness, and the two-racer contest — are pure local git/filesystem tests against throwaway `git init --bare` fake-origin repos under `/tmp`, never the real shared `spacedock-state/dev` branch). Nothing new to archive; all workspaces from rounds 1-2 remain archived/deleted as previously reported.
BLOCKER: non
