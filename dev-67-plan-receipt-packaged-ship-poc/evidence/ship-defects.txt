S1 go message sent while workspace still initializing was silently merged into the hello turn and never acted on; session reported idle. Fix in dispatch-layer.sh: wait workspace=ready AND session=idle, then a 15s grace, before sending the task.
S2 `conductor session message` JSON truncates at 64 KB again (mid-string); confirms DEV-64's premise; all reads go through conductor sql.
S3 accept-layer ran the worker's line under env -i HOME=/nonexistent; the contract test creates tmp dirs under HOME-derived paths and failed on retained. A no-secrets sandbox must strip credential vars, not the whole environment.
S4 accept-layer.sh ran the contract test three times (retained, removed, standalone) at ~50-70s each plus a codex review in the same foreground call and hit the 5-minute tool timeout; verify steps need to be separate invocations with their own budgets, and the receipt must be written incrementally so a timeout does not lose the retained/removed results.
S5 Codex review of a worker diff found two P1s the mechanical checks could not (regex YAML parser skipping quoted values; prose pins covering only half the change). kc-pr-review or an equivalent review step is load-bearing for script-producing Issues, not optional.
S6 Two review rounds on one script-bearing Issue took 12 min of worker time plus ~8 min of FO verify; the round cap of 2 held. The second round surfaced new findings outside the Brief's ACs; the FO must disposition those as scope-outside-brief (listed for Captain) rather than loop, or the cap is meaningless.
S7 Conductor MCP exposes run_sql/list_messages/get_session_status but no wait primitive; the MCP server also dropped mid-session, so the FO fell back to the CLI for the whole run. Contract: poll via sql (CLI or MCP), never list_messages; treat MCP availability as optional.
S8 read-evidence.sh sliced the Evidence block up to the next ``` fence; the SQL transcript renders the block inside a fence that opens BEFORE '## Evidence', so the slice was empty and the poller reported no evidence for 6 min after it existed. Slice to the BLOCKER line instead.
S9 A layer-2 worker's Evidence block can be found only through the SQL view AND its fence placement differs from layer-1's; the reader must locate the block by its first and last field names, not by markdown fences.
S10 DEV-65's fix round re-dispatches the same session while DEV-64 (layer 3) is already building on DEV-65's round-0 candidate 353795c. If DEV-65's fix produces a new candidate, layer 3's base no longer equals layer 2's final candidate: the stack contract (layer N+1 bases on layer N's approved candidate) is violated by any post-dispatch fix on a lower layer. Contract needed: freeze a layer's candidate once a higher layer is dispatched on it, or re-dispatch the higher layers after a lower-layer fix.
S11 Linear truncates branchName to ~72 chars; the receipt carried the full generated name and the worker pushed the truncated one. The receipt must carry Linear's exact branchName (it does: the receipt value matched the remote), but the FO's evidence parser assumed the dispatch text's name; pin on the receipt's value only.
S12 Writing an Evidence block into a file through a bash heredoc corrupted a line containing a literal "\n" inside a quoted string; write evidence files from python, never through shell interpolation.
S13 The Evidence block's own AC text quotes the phrase '## Evidence', so rfind on the heading lands inside the block; anchor on the 'CANDIDATE_SHA: <40hex>' line that follows the heading.
