[P1] scripts/ship-flow/worker-transcript.sh:28 — Regex requires the fence to open immediately before `## Evidence`; when the fence opens earlier, valid evidence is missed and the script incorrectly exits 1.

[P2] docs/dev/README.md:488 — Required `## Ship-flow runtime` heading is absent; the contract test only pins prose, so removing or omitting the section heading does not redden it.

```text
Remaining: none
Next: fix both findings (you)
Closable: unverified
```
