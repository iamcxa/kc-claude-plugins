S14 FO nudge quoted the carrier branch SHA as the worker's candidate; the worker verified against ls-remote and correctly refused the false claim. FO messages must never assert remote state the FO has not read from the same source the worker will check.
S15 wait-evidence anchored on 'DISPATCH_TOKEN then CANDIDATE_SHA on consecutive lines'; a worker that reorders or adds a field between them is invisible to the poller for the whole timeout. Anchor on the heading, then require the token anywhere in the block.
S17 A cloud worker can hit the Claude session usage limit mid-round; the session goes idle with a limit banner as its last message and no Evidence block. Detect by the banner text, not by idle; keep the workspace and re-send the round after reset rather than redispatching.
S18 The cloud Claude account's session usage limit is one pool across all cloud workers. Dispatching a second worker while one is limited does nothing; both go idle with the limit banner. Check the banner in the transcript before assuming a fresh worker has quota.
S19 A worker may pause on consent questions when the task arrives via a carrier branch and installs a third-party marketplace; the FO answers with the dispatch token as proof of authorship. Include the three confirmations in the go message up front.
S20 A stray CONDUCTOR_API_KEY env var (from an earlier --env experiment) shadows the valid keychain token; every conductor call returned 'Unauthorized client request' and looked like an API outage. conductor auth whoami shows the token source; unset the var or run with CONDUCTOR_API_KEY= prefix.
S21 The worker's final message is truncated by the harness at roughly 10K characters; an Evidence block that inlines run files loses them. Large run artifacts must be committed to the branch (or a carrier) and the block carries only their paths and hashes.
S22 Linear 'blocks' relation semantics: issueRelationCreate(issueId=X, relatedIssueId=Y, type=blocks) means X blocks Y. DEV-67's fixture was created with issueId=blocked, so every edge was inverted; plan-lint read inverseRelations consistently with that inversion and PASSED, and ship-flow dispatched DEV-66 -> 65 -> 64 when the intended order was 64 -> 65 -> 66. A lint that is consistent with its own input cannot catch a semantic inversion; the fixture creator and the lint shared one wrong belief. Falsifier for plan-lint L6: create X blocks Y in the Linear UI by hand, confirm the lint's edge is (X, Y).
S23 intent.sh derives its lock dir as <state>/.git/ship-lock.d; in a split-root Spacedock checkout .git is a gitdir FILE (worktree), so mkdir fails forever and every commit times out after 30 s. The POC ran only on plain clones. Fix: use $(git -C <state> rev-parse --git-dir)/ship-lock.d or a lock beside the state dir. First real dispatch (DEV-90) failed here before any Conductor call, correctly leaving no intent and no workspace.
S24 CORRECTED 2026-09-04. The original entry said DEV-91's without-it command read two files while its removed variant restored one. That is wrong, and re-reading the recorded block is what showed it: all three greps in DEV-91's WITHOUT_IT_COMMAND point at the SAME file (scripts/kc-dev-flow-contract-test.py), and the variant restores that file, so the pair is not narrow. The real reason it could not fail is that the three sentences it grepped for already existed at its BASE_SHA (00d2dbf5, DEV-90's candidate, which pinned them as a side effect of repointing the contract test). Restoring the file left the greps matching, so both variants exited 0. The catchable shape is 'the command already passes at BASE_SHA', caught by ONE execution at the base, not by a static path comparison. A narrow variant remains a real way to build an unfailable pair, but no recorded pair exhibits it, so a check for it must be proven by a synthetic mutant. The worker's self-report said removed -> exit 2, so it ran something other than what it declared; that part stands. Cost of the wrong entry: one dispatched round (dev94-ac2.g1) built a static path check aimed at a defect DEV-91 does not have.
S25 Codex CLI 0.147.0: chatgpt.com/backend-api/codex/models returns 404 (2026-09-03 ~14:50Z); codex exec produces empty output with no non-zero exit. The review step must detect an empty reviewer output and fall back (FO read for small diffs, kc-pr-review after the PR exists) instead of recording 'no findings'.
S26 A plan cut two Issues where the second's whole deliverable (pin the sentences) was already produced by the first (write the sentences AND pin them, because DEV-90's contract-test repoint carried the pins). The Issue cut was fine on paper; DEV-90's worker did DEV-91's work as a side effect of repointing. Ship-flow's minimal-necessity check caught it at acceptance: without-it could not fail and the FO's mutation with only DEV-90's pins still reddened. Disposition: accept DEV-91's goal as met by DEV-90, no PR. plan-flow POC 3: user-story-splitting must check that a split's deliverable is not a by-product of an earlier split.
S28 Dispatch Briefs for DEV-94 AC-1/AC-2 told the worker to commit as feat(kc-dev-flow) while every changed file lived under docs/plan-flow/ and scripts/ship-flow/, outside the kc-dev-flow package path. The scope was copied from #362/#363, which had the same mislabel. release-please attributes by path, so neither commit reached the kc-dev-flow changelog (verified in Release PR #369 after #372 merged) and no version was mis-bumped; the cost is a misleading subject line on main for #372, which was squash-merged before the retitle. #373 was retitled feat(ship-flow) before merge. Check before dispatch: the Brief's commit scope must match the directory the FILES line will name; plan-flow and ship-flow scripts are repo-owned workflow runtime, not kc-dev-flow.
S29 accept-evidence.sh (#373) ACCEPTED a block whose WITHOUT_IT_COMMAND was 'cmd && echo clean || echo derivative; on candidate exits 0, on removed variant ... exits 1' — the worker appended prose after a semicolon. bash -c ran 'on' as a command, exited 127 at BASE_SHA, and the station read any non-zero as 'the falsifier flips'. The FO's manual base run of the real command exited 0 (the trailing echo masks every code). Two fail-open holes: (a) the base-run check must treat 126/127 as 'command did not run', not as a flip; (b) AC-3 prints 'SKIP: cannot extract paths' and continues when the command is unparseable — it must refuse. A third: the command read ~/.claude/plugins/..., outside the candidate tree, and nothing checked that. The same round's sibling (W1, '&& echo PASS || echo FAIL') was refused correctly because its base exit really was 0. Fix ticket: DEV-94 accept station round 3.
S30 The S29 hardening (030a0bb7) passes its own seven ACs and then REFUSES two blocks the FO accepted the same day (W1r2, W3r2), both good pairs. Two causes. (a) extract_variant_paths recognises only 'git show X:path' and 'git rm ...'; plain 'rm -f path', 'sed -i', 'git checkout -- path', and '> path' redirections are invisible, so a variant that plainly removes the file is read as restoring nothing. (b) The AC-3 rule itself is over-strict: it demands that EVERY path the command reads be restored, but a real command reads its fixture too (plan-lint.py lint <fixture>); the fixture is an input, not the surface, and restoring it is meaningless. The rule the evidence supports: refuse only when the variant alters NONE of the read paths that the candidate changed (that catches W2r2's negation and any no-op variant); when some changed read paths stay unrestored, WARN and let the base run decide. The 'narrow variant' class this was guarding has zero recorded instances (S24 corrected). Cost: the hardening cannot merge as-is or every fixture-reading pair is refused. Fix: DEV-94 accept station round 4 on top of 030a0bb7, with today's four real blocks as fixtures.
S31 'gh stack init --base main <A> <B>' on branch names that existed only on origin (fetched, no local tracking branch) CREATED local branches A and B from a stale local ref (1745b135, 25-day-old release commit; no local main existed), and 'gh stack submit --auto' then pushed them, moving BOTH remote heads back to 1745b135. GitHub closed PR #375 and #376 as empty and refused 'gh pr reopen' while the diff was empty. Recovery: the real commits (df43392f, 7e495afd) were still local; the clobbered remote was an ancestor, so a plain fast-forward push restored each head; reopen then succeeded. No force-push was needed. Cost: two PRs closed for ~4 minutes. Rule: never run gh stack init on remote-only branch names; create local tracking branches at the exact remote SHAs first (git branch A origin/A), and verify remote heads before and after submit. The earlier stack #366 worked because those branches were local.
S32 RETRACTED 2026-09-06. The 'chained git rm dropped' defect was observed against a STALE copy of accept-evidence.sh: the FO's worktree lagged origin/main by three commits (#375, #376, #377) for the whole batch, so every 'station on main' run that day used the pre-#377 script. main's extractor handles 'git rm -f A && git rm -f B' (verified after syncing). DEV-106 was not a false refusal by the station; it was the FO running the wrong station. See S33.
S33 The FO ran the accept station from its own worktree, which was three commits behind origin/main, for an entire batch (DEV-104..107, 2026-09-06). The hardened station (#377) was never the one executing; the FO's manual S29 guard happened to cover the gap, and one 'station defect' (S32) was invented from the stale copy. Once synced, the real station accepts DEV-105, DEV-106 (WARN), DEV-107, and DEV-104 r2 (WARN). Rule: the station of record is `git show origin/main:scripts/ship-flow/accept-evidence.sh` run from a worktree at origin/main, never the FO's working branch; the batch record must name the station's commit. Cost: one retracted defect, two corrected README lines, no wrong merge.
S34 open-pr.sh takes the PR title from the CANDIDATE_SHA commit's subject; after a moved_base rebase the head is the FO's merge commit ('chore: merge the repaired lower layers…'), so #381 opened with the wrong title and the FO retitled it by hand. The title source should be the first non-merge commit above the base, or the Brief's title. Listed for the UAT document against DEV-104.
S35 dev-debrief.py (DEV-107) fills per_issue.code_refusals from the close receipt's residuals and evidence_refusals from WITHOUT_IT_OBSERVED — neither is a refusal; the writer has no source for refusal counts because the batch record keeps them in README prose, not in a field. The FO edited the debrief by hand for batch e56e9f09. Fix belongs with the record shape (claim-*.txt should carry rounds/refusals) and the writer; listed for the UAT document against DEV-107.

S36 (2026-09-06, batch e56e9f09 merge) — the merge queue landed out of order. Sequence `gh pr ready N; gh pr merge N --squash --delete-branch` for #378, #379, #380: GitHub refused #378 and #379 with "To have the pull request merged after all the requirements have been met, add the --auto flag" while each showed MERGEABLE/CLEAN one poll earlier, then merged #380 immediately. Cause not measured (likely the ready_for_review transition re-evaluating protection for a few seconds); effect measured: #380 on main first, #378 and #379 turned CONFLICTING on docs/ship-flow/README.md, each needs an FO merge of main (moved_base) before landing, and #381 will need it twice. Rule for the merge station: mark all PRs ready first, wait until every one reads MERGEABLE/CLEAN again, then merge with --auto so a refusal cannot silently skip a PR in the order. Candidate ticket: merge station script.
