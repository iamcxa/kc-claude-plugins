<!-- section:plan-report -->
# Shadow Review Receipt Verify-Return Plan

> **For executer:** REQUIRED SUB-SKILL: use `superpowers:executing-plans` and preserve RED-before-GREEN evidence task-by-task.

**Goal:** Repair the failed PR1 shadow runtime so it produces a privacy-safe, complete, exact-head receipt while legacy review behavior remains authoritative and byte-stable.
**Architecture:** Keep the source-safe Bash facade, add one fail-closed Python 3.8+ safe-I/O helper for no-follow descriptor snapshots and calendar parsing, admit only closed typed projections, and derive all observation and benchmark claims from canonical bound content.
**Tech stack:** Bash 3.2+, Python 3.8+ stdlib, `jq`, Git, JSONL, plain-Bash tests, GitHub Actions.

<details>
<summary>Completed implementation history retained for audit</summary>

| Completed task | Shipped surface | Verify-return disposition |
|---|---|---|
| T1 | Event runtime and fresh identity | Harden in T6-T7. |
| T2 | Replay, evidence, findings, usage | Harden in T7-T8. |
| T3 | Shadow observer | Replace identity-only seam in T9. |
| T4 | Paired benchmark | Rebind authority in T10. |
| T5 | Docs, canon, CI | Resynchronize in T11. |

</details>

<details>
<summary>Mechanically imported repaired design constraints</summary>

## Plan Imported Design DCs
<!-- section:plan-imported-design-dcs -->
<!-- Generated by plugins/ship-flow/lib/import-design-dcs.sh from entity's Hand-off to Plan block. -->
| # | Type | Assertion | Decision |
|---|---|---|---|
| 1 | data-contract | Stable exact-head key, fresh PR1 runs, typed successor metadata; no resume or GC. | D1 |
| 2 | schema-contract | Closed v1 envelope; typed hash-only non-semantic extensions; legacy behavior remains authoritative. | D2 |
| 3 | data-contract | Candidate/finding identity includes evidence hash; pointers bind review/base/head and LEFT/base, RIGHT+FILE/head. | D3 |
| 4 | contract | Provider-neutral task/result/usage; unavailable usage remains null. | D4 |
| 5 | schema-contract | Invalid input cannot reduce state; quarantine is metadata-only. | D7 |
| 6 | contract | Shadow changes no body, comments, options, event, confirmation, or GitHub mutation; parity uses the production collector. | D2 |
| 7 | data-contract | Closed ShadowObservation/v1 emits every lane and both terminal events; incomplete state is not_observed. | D2 |
| 8 | contract | No-follow snapshots, safe integers, exit precedence, bound benchmark authority, and synchronized canon are required. | D2 |
<!-- /section:plan-imported-design-dcs -->

</details>

## Research Summary

`verify.md` supplies nine merged blockers, six implementation/recovery warnings, and one TDD-evidence warning class. Repaired parent/child D2/D3/D7 override the stale shape DC-2 additive-field wording. Python 3.8+ is an explicit runtime dependency for safe file ingestion: `os.open(O_RDONLY|O_NOFOLLOW|O_CLOEXEC)`, `fstat` regular-file proof, descriptor-only bounded reads, and matching pre/post `(dev, ino, size, mtime_ns, ctime_ns)`; an absent dependency, unsupported `O_NOFOLLOW`, path race, mutation, or oversize input fails closed before state changes. Reverse-recovery audit confirms `review-runtime-safe-io.py` and `tdd-evidence/T6-T10.jsonl` are MISSING at plan time and are intentional new outputs, not stale paths to recover. Lock crash/PID reuse, predecessor lineage, and quadratic append remain owned by existing child 2.3; no GitHub issue is needed.

## Size Re-evaluation

Large verify-return: six remediation tasks across runtime, helper, tests, fixture/corpus, skill seam, docs, canon, CI, and durable TDD evidence. Execute serializes runtime ownership; only benchmark work may overlap the collector after T8.

## Verification Spec

<details>
<summary>One runnable row per repaired incoming design constraint</summary>

| DC | Verify procedure | Expected |
|---|---|---|
| DC-1 D1 identity | `bash kc-pr-flow/scripts/review-runtime.test.sh` and inspect named fresh/successor assertions | Fresh exact-head runs and typed successor metadata validate; no resume/GC behavior appears. |
| DC-2 D2 event contract | `bash kc-pr-flow/scripts/review-runtime.test.sh --case privacy-envelope` | Canonical append/replay, duplicate no-op, closed keys, and hash-only non-semantic extensions pass while legacy authority is unchanged. |
| DC-3 D3 evidence | `bash kc-pr-flow/scripts/review-runtime.test.sh --case evidence-binding` | Nested identity, LEFT/base, RIGHT+FILE/head, evidence hashes, candidate IDs, merge keys, and finding IDs agree. |
| DC-4 D4 provider/usage | `bash kc-pr-flow/scripts/review-runtime.test.sh` and inspect named provider/usage assertions | Provider-neutral envelopes pass; unavailable stays null; only same-provider reported jq-safe usage compares. |
| DC-5 D7 rejection | `bash kc-pr-flow/scripts/review-runtime.test.sh --case privacy-envelope` plus state-root forbidden-content scan | Invalid input cannot reduce state; quarantine has exactly four metadata keys and no rejected bytes/links. |
| DC-6 D2 parity | `bash kc-pr-flow/scripts/review-shadow.test.sh --case production-collector` | Off/on/failure enter the same collector; six frozen legacy artifacts are byte-identical and GitHub mutation log is empty. |
| DC-7 D2 lifecycle | `bash kc-pr-flow/scripts/review-shadow.test.sh --case production-collector` | Every lane has started/candidates/finished before synthesis/run terminals; incomplete or identity-only state is typed `not_observed`. |
| DC-8 D2 safety/canon | `bash kc-pr-flow/scripts/review-runtime.test.sh --case safe-io && bash kc-pr-flow/scripts/review-runtime-benchmark.test.sh --case authority-binding` plus T11 exact checks | No-follow ingestion, exit precedence, bound benchmark authority, synced canon, and durable TDD receipts pass. |

</details>

## Canonical Doc Actions

| Doc | Action | Source | Rationale |
|---|---|---|---|
| ROADMAP.md | skip | plan | Existing parent row owns all three children; 2.3 already owns deferred recovery/performance work. |
| PRODUCT.md | update | design | State the closed sanitized collector and complete-observation boundary. |
| ARCHITECTURE.md | update | design | Replace additive/raw quarantine, stale evidence, and identity-only language with repaired D2/D3/D7. |

## Plan

### T6 — Close durable envelopes and quarantine
task_id: T6
layer: L4
wave: W1
files: `kc-pr-flow/scripts/review-runtime.sh`, `kc-pr-flow/scripts/review-runtime.test.sh`, `kc-pr-flow/test/fixtures/review-runtime/valid-events.jsonl`, `docs/ship-flow/2.1-shadow-review-receipt/tdd-evidence/T6.jsonl`
skills_needed: [test, test-driven-development, security-best-practices]
reviewer_questions: Does validation admit exactly the known envelope plus `extensions[{namespace,key,value_sha256,byte_count}]`, forbid semantic extension authority, and persist quarantine as exactly `reason_code,input_sha256,byte_count,quarantined_at` with no rejected file or link?
tdd_contract:
  red_command: "bash kc-pr-flow/scripts/review-runtime.test.sh --case privacy-envelope"
  expected_red_failure: "Unknown top-level/raw extension values still validate and rejected bytes still appear in quarantine."
  green_command: "bash kc-pr-flow/scripts/review-runtime.test.sh --case privacy-envelope"
  refactor_check: "bash -n kc-pr-flow/scripts/review-runtime.sh kc-pr-flow/scripts/review-runtime.test.sh && bash kc-pr-flow/scripts/review-runtime.test.sh"
parallel_group: serial; depends_on: []; owned_paths: [runtime, runtime-test, valid-events, tdd-evidence/T6.jsonl]; integration_owner: executer@2.1
steps: Write named failing cases and record RED; implement exact-key/type validation and metadata-only quarantine; record GREEN/REFACTOR; commit explicit task paths.
done: D2/D7 privacy row passes; T6 evidence JSONL records command, expected result, exit code, output SHA-256/excerpt, timestamp, and RED before GREEN.

### T7 — Make ingestion, limits, dates, and batch status fail closed
task_id: T7
layer: L4
wave: W2
files: `kc-pr-flow/scripts/review-runtime-safe-io.py`, `kc-pr-flow/scripts/review-runtime.sh`, `kc-pr-flow/scripts/review-runtime.test.sh`, `docs/ship-flow/2.1-shadow-review-receipt/tdd-evidence/T7.jsonl`
skills_needed: [test, test-driven-development, best-practices]
reviewer_questions: Does the helper create a new mode-0600 destination from one O_NOFOLLOW descriptor, reject non-regular/changed/oversize input, parse real UTC RFC3339 calendar values, and make missing Python/O_NOFOLLOW exit 69 before mutation? Are all config/event integers within 0..9007199254740991, and is batch precedence fixed at 75 temporary block, then 74 durability block, then 1 quarantined, then 0 success?
helper_contract: `review-runtime-safe-io.py snapshot --source SOURCE --destination DEST --limit-bytes LIMIT` writes no stdout and returns 0 only after opening source once with `O_RDONLY|O_NOFOLLOW|O_CLOEXEC`, proving regular via `fstat`, reading at most `LIMIT+1`, matching pre/post descriptor `(dev,ino,size,mtime_ns,ctime_ns)`, and creating destination once with `O_WRONLY|O_CREAT|O_EXCL|O_NOFOLLOW` mode 0600 without path reopen. Dependency/O_NOFOLLOW absence=69; invalid limit/config or oversize=73; unsafe non-regular/symlink input=2; race/mutation/destination durability=74. Runtime mixed-batch precedence is 75 > 74 > 1 > 0. Tests assert each exact code on macOS and CI Linux.
tdd_contract:
  red_command: "bash kc-pr-flow/scripts/review-runtime.test.sh --case safe-io"
  expected_red_failure: "Symlink swap, unsafe integer/date, and later quarantine still bypass or downgrade existing guards."
  green_command: "bash kc-pr-flow/scripts/review-runtime.test.sh --case safe-io"
  refactor_check: "python3 -m py_compile kc-pr-flow/scripts/review-runtime-safe-io.py && bash -n kc-pr-flow/scripts/review-runtime.sh kc-pr-flow/scripts/review-runtime.test.sh && bash kc-pr-flow/scripts/review-runtime.test.sh"
parallel_group: serial; depends_on: [T6]; owned_paths: [safe-io-helper, runtime, runtime-test, tdd-evidence/T7.jsonl]; integration_owner: executer@2.1
steps: Add race/dependency/boundary/calendar/precedence RED probes and record them; implement descriptor snapshot plus bounded validation; record GREEN/REFACTOR; commit explicit paths.
done: Safe-ingestion row passes on macOS and CI Linux; no path reopen occurs after descriptor acquisition.

### T8 — Bind evidence, candidates, and findings to exact review content
task_id: T8
layer: L4
wave: W3
files: `kc-pr-flow/scripts/review-runtime.sh`, `kc-pr-flow/scripts/review-runtime.test.sh`, `kc-pr-flow/test/fixtures/review-runtime/valid-events.jsonl`, `docs/ship-flow/2.1-shadow-review-receipt/tdd-evidence/T8.jsonl`
skills_needed: [test, test-driven-development, best-practices]
reviewer_questions: Does one validator serve event validation and verify-evidence, require nested review_key/base_sha/head_sha equality, bind git_blob LEFT to base and RIGHT/FILE to head, and include evidence_content_sha256 in candidate_id, merge_key, and finding_id?
tdd_contract:
  red_command: "bash kc-pr-flow/scripts/review-runtime.test.sh --case evidence-binding"
  expected_red_failure: "Historical object SHAs and changed evidence hashes still validate or retain old candidate/finding identities."
  green_command: "bash kc-pr-flow/scripts/review-runtime.test.sh --case evidence-binding"
  refactor_check: "bash -n kc-pr-flow/scripts/review-runtime.sh kc-pr-flow/scripts/review-runtime.test.sh && bash kc-pr-flow/scripts/review-runtime.test.sh"
parallel_group: serial; depends_on: [T7]; owned_paths: [runtime, runtime-test, valid-events, tdd-evidence/T8.jsonl]; integration_owner: executer@2.1
steps: Add table-driven RED mutations and record them; centralize pointer predicates and identity formulas; record GREEN/REFACTOR; commit explicit paths.
done: D3 row passes and every accepted candidate/finding is mechanically bound to the containing exact-head event.

### T9 — Replace identity-only shadowing with the closed collector
task_id: T9
layer: L4
wave: W4
files: `kc-pr-flow/scripts/review-runtime.sh`, `kc-pr-flow/scripts/review-shadow.test.sh`, `kc-pr-flow/skills/kc-pr-review/SKILL.md`, `docs/ship-flow/2.1-shadow-review-receipt/tdd-evidence/T9.jsonl`
skills_needed: [test, test-driven-development, write-docs, best-practices]
reviewer_questions: Does the exact `ShadowObservation/v1` key schema reject raw/arbitrary values, emit run.started then each lane.started/candidates/lane.finished then synthesis.finished/run.finished, and return observed only after complete replay? Do off/on/collector-failure serialize the same observation into the same executable seam while body/comments/event/options/confirmation/GitHub-log bytes remain equal?
tdd_contract:
  red_command: "bash kc-pr-flow/scripts/review-shadow.test.sh --case production-collector"
  expected_red_failure: "The production seam emits only run.started and the parity oracle does not feed legacy artifacts through the collector."
  green_command: "bash kc-pr-flow/scripts/review-shadow.test.sh --case production-collector"
  refactor_check: "bash -n kc-pr-flow/scripts/review-runtime.sh kc-pr-flow/scripts/review-shadow.test.sh && bash kc-pr-flow/scripts/review-shadow.test.sh"
parallel_group: pr1-w4; depends_on: [T8]; owned_paths: [runtime, shadow-test, review-skill, tdd-evidence/T9.jsonl]; integration_owner: executer@2.1
steps: Add serialized collector/lifecycle/parity RED and record it; implement exact closed projection and typed not_observed reasons; wire one fail-open 6b call; record GREEN/REFACTOR; commit explicit paths.
done: D2 shadow row passes with no second review or mutation capability and no identity-only success.

### T10 — Bind benchmark recall and receipt authority
task_id: T10
layer: L7
wave: W4
files: `kc-pr-flow/scripts/review-runtime-benchmark.sh`, `kc-pr-flow/scripts/review-runtime-benchmark.test.sh`, `kc-pr-flow/test/fixtures/review-runtime/paired-runs.jsonl`, `docs/ship-flow/2.1-shadow-review-receipt/tdd-evidence/T10.jsonl`
skills_needed: [test, test-driven-development, benchmark]
reviewer_questions: Do expected/observed findings resolve to canonical candidates with the same evidence hash, and does each arm recompute `content_sha256=sha256(canonical behavior,lanes,candidates,findings,uncertain refs,usage)` plus `receipt_id=sha256(run_id|review_key|content_sha256)` before scoring?
tdd_contract:
  red_command: "bash kc-pr-flow/scripts/review-runtime-benchmark.test.sh --case authority-binding"
  expected_red_failure: "Removing candidates/evidence or retaining a stale receipt hash still yields valid perfect recall."
  green_command: "bash kc-pr-flow/scripts/review-runtime-benchmark.test.sh --case authority-binding"
  refactor_check: "bash -n kc-pr-flow/scripts/review-runtime-benchmark.sh kc-pr-flow/scripts/review-runtime-benchmark.test.sh && bash kc-pr-flow/scripts/review-runtime-benchmark.test.sh"
parallel_group: pr1-w4; depends_on: [T8]; owned_paths: [benchmark, benchmark-test, paired-corpus, tdd-evidence/T10.jsonl]; integration_owner: executer@2.1
steps: Add candidate-removal/evidence-drift/stale-receipt RED and record it; implement canonical authority checks before recall; record GREEN/REFACTOR and repeat-report diff; commit explicit paths.
done: Corpus without bound evidence is invalid, deterministic valid reports are byte-equal, and unavailable usage cannot claim efficiency.

### T11 — Synchronize docs, CI, and durable execute evidence
task_id: T11
layer: meta
wave: W5
files: `PRODUCT.md`, `ARCHITECTURE.md`, `kc-pr-flow/{README.md,CLAUDE.md,reference/review-runtime.md,docs/review-runtime.md}`, `.github/workflows/review-runtime-tests.yml`, `docs/ship-flow/2.1-shadow-review-receipt/tdd-evidence/*.jsonl`
skills_needed: [write-docs, github-workflows]
reviewer_questions: Do canonical/plugin docs describe Python fail-closed safe I/O, closed extensions/quarantine, exact collector/evidence/benchmark contracts and 2.3 deferrals without PR2/PR3 claims; does CI cover helper syntax and every changed consumer path; do T6-T10 receipts each prove ordered RED/GREEN/REFACTOR?
TDD: skip -- documentation, workflow wiring, and execute-evidence audit reuse the completed behavior suites and static validators.
parallel_group: serial; depends_on: [T9, T10]; owned_paths: [canon, plugin-docs, workflow, tdd-evidence]; integration_owner: executer@2.1
steps: Update canon and plugin docs; extend path filters and Python/shell checks; audit each evidence JSONL with `jq -se`; run the full Verification Spec, shellcheck, actionlint, yq, `git diff --check`, and forbidden-state probes; commit explicit paths.
checks: `python3 -m py_compile kc-pr-flow/scripts/review-runtime-safe-io.py`; `bash -n kc-pr-flow/scripts/review-runtime.sh kc-pr-flow/scripts/review-runtime.test.sh kc-pr-flow/scripts/review-shadow.test.sh kc-pr-flow/scripts/review-runtime-benchmark.sh kc-pr-flow/scripts/review-runtime-benchmark.test.sh`; `shellcheck kc-pr-flow/scripts/review-runtime.sh kc-pr-flow/scripts/review-runtime.test.sh kc-pr-flow/scripts/review-shadow.test.sh kc-pr-flow/scripts/review-runtime-benchmark.sh kc-pr-flow/scripts/review-runtime-benchmark.test.sh`; `actionlint .github/workflows/review-runtime-tests.yml`; `yq eval '.' .github/workflows/review-runtime-tests.yml >/dev/null`; `for f in PRODUCT.md ARCHITECTURE.md kc-pr-flow/reference/review-runtime.md kc-pr-flow/docs/review-runtime.md; do grep -Fq 'ShadowObservation/v1' "$f" && grep -Fq 'metadata-only quarantine' "$f"; done`; `jq -se 'group_by(.phase)|map(length>0)|all' docs/ship-flow/2.1-shadow-review-receipt/tdd-evidence/T{6,7,8,9,10}.jsonl`; `git diff --check`. Every command exits 0.
done: Canon matches repaired D2/D3/D7, CI exercises all runtime surfaces, and durable evidence makes every remediation RED-before-GREEN auditable.

## Context Manifest

- **Skills loaded**: ship-plan, superpowers:writing-plans, ship-flow:test-driven-development.
- **INVARIANTS sections read**: ship-plan Principles 6, 8, and 15 contract; verify-return keeps status execute.
- **Architecture docs consulted**: PRODUCT.md, ROADMAP.md, ARCHITECTURE.md, parent/child design, shape, verify.md, kc-pr-flow/CLAUDE.md.
- **Domains touched**: schema/generalist-only; repaired design has no open decision.
- **Lens dispatched**: none for bounded verify-return; failed verify panel findings are the research input.
- **Lens findings integrated**: 9 blockers plus 3 implementation warnings integrated; 3 recovery/performance warnings routed to child 2.3; 1 separate TDD-evidence warning class integrated; 0 ignored.
- **Folder guidance**: files=`kc-pr-flow/**` -> folder_guidance_files=[kc-pr-flow/CLAUDE.md]; folder_guidance_skills=[]; codex_context_boundary=root AGENTS.md/CLAUDE.md intentionally excluded from folder_guidance_files.

## Plan Report

status: passed
stage_cost: bounded verify-return local planning
iterations: 2 self-review passes + 3 bounded cross-review rounds
dimensions: repaired-contract coverage, atomicity, wave safety, runnable verification, TDD evidence, canonical sync, and skill coverage pass
reviewer_verdict: PROCEED; execute remains gated by fresh verify
scope_anchoring: all verify findings covered; lock/PID reuse, predecessor lineage, and quadratic append explicitly remain child 2.3
skill-coverage: PASS

### Metrics

status: passed
duration_minutes: 24
iteration_count: 5
task_count: 6
verification_spec_count: 8
model_split: executor-selected

### Hand-off to Execute
<!-- section:hand-off-to-execute -->
- **tdd-ledger**: `tdd-ledger.txt` reports `status=pass records=6`; persisted `tdd-ledger.jsonl` also passes live ship-flow 0.9.0 `validate-tdd-ledger.py --plan ... --require-ledger-jsonl ...`.
- **tdd-evidence**: T6-T10 each own `tdd-evidence/<task>.jsonl`; append RED, GREEN, and REFACTOR records immediately after commands, never reconstruct them after implementation.
- **wave_order**: W1 T6 -> W2 T7 -> W3 T8 -> W4 (T9 || T10) -> W5 T11.
- **critical_assumptions**: Python 3.8+ and O_NOFOLLOW are available or collection fails closed with exit 69; exact collector has no mutation capability; runtime overlap remains serial through T9.
- **architecture_context**: update PRODUCT/ARCHITECTURE/plugin docs in T11; ROADMAP unchanged; child 2.3 is the durable route for lock recovery, lineage, and append complexity.
- **stub_flags**: none.
- **skills_needed_summary**: T6 security+test; T7/T8 runtime+test; T9 parity+docs+test; T10 benchmark+test; T11 docs+workflow; five distinct sets.

| Task | Parallel group | Depends on | Owned paths | Integration owner |
|---|---|---|---|---|
| T6 | serial | - | runtime/test/fixture/evidence | executer@2.1 |
| T7 | serial | T6 | safe-I/O/runtime/test/evidence | executer@2.1 |
| T8 | serial | T7 | runtime/test/fixture/evidence | executer@2.1 |
| T9 | pr1-w4 | T8 | runtime/shadow/skill/evidence | executer@2.1 |
| T10 | pr1-w4 | T8 | benchmark/test/corpus/evidence | executer@2.1 |
| T11 | serial | T9,T10 | canon/docs/CI/evidence audit | executer@2.1 |

| Task | Verify lens | Evidence required |
|---|---|---|
| T6 | security/schema | privacy RED/GREEN, state ban scan, exact quarantine keys |
| T7 | concurrency/type/silent | race, dependency, numeric, calendar, exit-precedence probes |
| T8 | data-contract/security | pointer mutation matrix and identity hashes |
| T9 | parity/silent | serialized collector lifecycle plus six byte-parity artifacts |
| T10 | benchmark/adversarial | invalid-corpus mutations and repeated report diff |
| T11 | documentation/workflow | cold-read contract, CI/static/full-suite output, evidence audit |

<!-- section:context-routing-manifest -->
```yaml
context-routing-manifest:
  domain_matches: [schema]
  knowledge_modules: []
  required_skills: []
  stage_hints: {plan: []}
  consumer_obligations: [preserve repaired D1-D4 and D7, keep PR1 shadow-only, retain generalist-only marker]
  future_provider_boundary: "Provider hints are non-authoritative; the local registry remains canonical."
```
<!-- /section:context-routing-manifest -->

## Context Routing Receipt

Schema/generalist routing maps T6-T10 to their contract questions and evidence rows; T11 owns canonical sync. No absent schema specialist is claimed.
<!-- /section:hand-off-to-execute -->
<!-- /section:plan-report -->
