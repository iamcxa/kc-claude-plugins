# Workflow Cost Record

This is non-authoritative observation history. It is not read during ordinary
continuation and no gate, CI job, delivery action, or archive depends on it.
Per-task measurements remain in their own entities.

Format:

`- <date> · <clause or file> — <what happened>. Cost: <unit>. <resolution>`

- 2026-08-03 · `Gate Authority` — an agent searching a 1,300-line workflow by
  task keywords skipped the authority section and escalated two delegated
  decisions. Cost: two unnecessary captain gates and one re-derivation turn.
  Resolved by keeping authority in the bounded always-loaded README.
- 2026-08-09 · validation rejection routing — repeated correction fixed
  instances of a class whose shape needed re-cutting. Cost: two correction
  rounds plus a captain stop decision. Resolved by requiring an explicit route
  choice after two rejected cycles.
- 2026-08-11 · repeated implementation review — two high-reasoning reviewer
  attempts consumed about three minutes each without a verdict even though
  implementation had no open judgment. Cost: blocking time before the first
  integrated slice. Resolved by one EM verdict at ideation and validation only.
