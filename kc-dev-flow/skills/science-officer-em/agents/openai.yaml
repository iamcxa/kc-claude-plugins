interface:
  display_name: "Science Officer EM (Legacy)"
  short_description: "Emit the legacy assurance report envelope"
  default_prompt: "Use $science-officer-em only when a consumer requires the legacy Science Officer EM report envelope."
