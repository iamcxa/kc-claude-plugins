# Document and comment disposition

No complete retained document is proposed for deletion. No frozen source or historical work item was edited. Two searches were actually performed for each proposed cut/correction class: literal/alias `rg` over the final candidate and independent tracked `git grep` for symbols, model fields and backlinks. Exact queries/output are in raw `document-second-home-searches.json`; search classes below identify both retained outputs. Search matches establish second homes and implementation reachability, not behavior proof.

## Per-section accounting

| Retained section/block | Second-home result and disposition |
|---|---|
| SKILL: introduction/Two modes | Keep map from intent versus check against code distinction; conversation guide covers facilitation, but main invocation owns route selection. `projection`, `contracts` searches. |
| SKILL: Process | Keep current inspection/citation method, declared assumptions and owner interaction; runtime lint cannot establish evidence truth by itself. Repeated drawing commands defer to canvas Run/Three projections. `contracts`, `projection`. |
| SKILL: Rendering | Shorten repeated projection explanations to selection/default plus canvas link; canvas Three projections and cell-contract Three surfaces retain exact meaning. FigJam/native routes remain; do not promise deleted HTML template. `projection`, `fallback`. |
| SKILL: Hard rules | Keep evidence boundaries, verbatim person language, uncertainty and approval semantics. These instruct human/agent conduct; source code cannot replace them. Qualify statuscard requirement to actual journey-board output instead of implying default story-map renders full deployment status. `contracts`, `projection`. |
| SKILL: Check-mode output / Verify before presenting / Where this fails | Keep output receipt, observed-check requirement and uncertainty limits; no second home replaces the command's handoff responsibility. Remove no section. `contracts`, `evidence`. |
| canvas: Split that makes it worth having | Correct “room is a cache / Delete .rooms/ and nothing is lost.” SQL room persistence and unclaimed manual edits have independent value; observed no-disk restart loses hand note. Example rules already require read/export before rerender, but example status.irreversible still overclaims losslessness. Canonical warning should say export/read edits before deleting rooms; deleting rooms loses unexported native state. `cache` both searches. |
| canvas: Run it | Keep exact prerequisites/startup/ports. Cut dependency-free HTML/PNG instructions: old template is deleted, current journey-export requires live browser/editor. SKILL provides YAML/Markdown fallback and native export instructions have a valid second home. Correct doctor fallback message and all three standalone descriptions to match. `fallback` both searches. |
| canvas: Three projections, drawn by request | Keep canonical commands, model shape and separate evidence standards. Remove unsupported `later:` example field: symbol search has no `model.later` consumer; runtime accepts actual releases/activities/slices. Keep `slices` because journey-board consumes it. Shorten repeated narrative definitions by linking cell-contract; qualify required status to evidence mode (map-only stories may omit it). Unsupported-state UNASSESSED wording should describe journey-board label, not promise it on every story-map card. `projection`, `evidence`. |
| canvas: Draw a task observation | Keep discoverable render invocation and derived-versus-hand-authored distinction; mapping mechanics have their second home in progress skill. Replace long duplicated mapping explanation with link, preserving pending acceptance. `contracts`, `projection`. |
| canvas: Moving a board somewhere else | Keep exact import/export semantics and overwrite warning. Move timestamped “82 records / 71 carrying journey meta” one-off browser result into historical work evidence if preserving it; neither searches nor current example prove those counts are current. Do not delete the only record of that historical observation. Canonical reusable instructions and example commands remain. `example`, `evidence`. |
| canvas: The two directions | Keep source/native distinction, lossy inverse, duplicate metadata refusal and conflict resolution; read.mjs encodes safety but does not replace user-facing recovery. Repeated projection summaries defer to earlier section. `contracts`, `projection`. |
| canvas: Two traps, both verified in a browser | Keep fractional index and note sizing facts not recoverable from superficial code intent; they prevent previously observed native failures. Phrase observations as bounded to current native version. `evidence` plus records/native source references. |
| canvas: Licence | Keep upstream native library conditions and local-only boundary; no code substitute. No audit mutation or new legal claim. |
| cell-contract: Three surfaces / Columns | Keep semantic evidence standards and common journey backbone; shorten repeated rendering commands only, with canvas canonical second home. `projection`, `contracts`. |
| cell-contract: Lane 1 / Stories | Keep verbatim person wording, goal/story distinction, horizontal release meaning, three states and honest unknown; qualify status requirement by mode. No new inference from statuses. `contracts`, `projection`. |
| cell-contract: Interpret task-derived colors | Keep color interpretation and delivery-pending warning where readers interpret colors; full mapping declaration belongs in progress skill. Do not erase the warning merely because it repeats a safety boundary. `contracts`, `projection`. |
| cell-contract: Lane 2 / Lane 3 | Keep narrative system-flow and invariant/owner/cost semantics; executable references do not replace semantic criteria. `contracts`. |
| cell-contract: The status card / Language | Keep unproven/unmerged/undeployed/irreversible/as-of definitions and language rule; clarify which generated projection displays the card. `contracts`, `projection`. |
| example README: opening/import/readback instructions | Keep whole file: no second home for exact offline `tldraw.com` workflow plus worked filenames and commands. Import-overwrites warning repeated intentionally at dangerous command. `example` both searches. |
| example README: What is in it / regeneration block | Keep exact generation commands and native portability limitations. Trim generic page/status prose whose second homes are canvas Three projections and cell-contract; do not remove unique example instructions or silently reinterpret historical host-unverified statements. `example`, `projection`. |
| example YAML: journey/releases/steps/stories/ownership/rules | Keep coherent worked schema and gap/unverified/exists examples, not a live project authority. Native example and tests depend on these distinctions. Correct events-carry-order rule: authored event order is modeled, not dynamically derived causality. `contracts`, `example`. |
| example YAML: status | Historical dated author claims are not live proof. Preserve historical source externally if updating the illustrative status block; correct unsafe room-deletion assertion and mark dated PR/deploy claims as example observations. Current model.status is read by journey-board, so deleting the whole block would lose demonstrated semantics. Regenerate native example together after approved fixture edits. `cache`, `evidence`, `example`. |
| map-from-conversation: What you must not do / Four passes | Keep unique facilitator boundaries, one-question pacing, owner-selected release and non-invention rule. No second-home removal proposed. |
| map-from-conversation: Empty activity / What room gets / Document input | Keep unique release coverage prompt, model handoff and disclosure when conversation is a document. Rendering cannot replace these choices. |
| progress skill: intro / Declare required work / Read observation | Keep exact identity/exhaustiveness ownership, explicit refresh CLI, archived/reopened/unknown limits, non-atomic snapshot caveat and pending acceptance. No automatic provider mapping exists. `contracts` both searches. |
| ROADMAP: new release registration and compatibility paragraphs | Keep specifically Captain-authorized lifecycle record and stored sprint compatibility semantics. Searches find runtime release handling and task evidence, but no authority to erase this source registration. This is a justified lifecycle exception, not duplicate runtime documentation. `roadmap` both searches. |
| Deleted team-ops entry and cell contract | New standalone skill/cell contract are second homes for the old map/check, evidence, three lanes, statuscard and language semantics. New conversation/progress material is additional accepted value. Legacy HTML renderer has no supported current implementation home; retain its deletion and correct advertisements, not restore a second renderer. `fallback`, `contracts`, `projection`. |

## Comments

Actually removed in disposable code: only the unused `records.frame` function's attached comment (with the function), and lines within the concrete schema/CORS/Vite alternatives where applicable; exact patches are retained. No separate comments-only test or all-comments-removal claim was made. Everything below is a producer recommendation, not an applied source edit.

Retain non-rederivable rationale: `doctor` connect-versus-bind dual-stack incident; `records` font size adjustment/native visibility, fractional ordering and measured note sizing; standard child border portability; `read` per-page duplicate scope, changed-only competing edits, stale wording guard, lossy inverse and YAML formatting preservation; `rooms` open-room lifetime for unattended PATCH; `canvas-server` queued early socket messages; App no-assets boundary and manual growY update; smoke owned stdout/port identity and cleanup; export live-editor/native bounds distinction and import replacement warning; upstream licence attribution. These explain failure boundaries or external contracts, not a function's name.

Cut/rewrite restatement or overclaim: `records` obsolete two-page introductory text and frame comment; duplicated storymap release-band explanation when header/contract already says it; render releaseCoverage motivational prose repeating computed coverage; lint introductory marketing; funcmap categorical real causal/build-order assertion; doctor/cache/fallback false claims; workflow comment estimating two runner-minutes without measurements and implying browser sticky visibility from a server smoke. Retain short section labels only where they make a long projection routine navigable; no blanket comment ban. Tests' historical counterexamples and independent expectations remain; self-example lint does not prove delivery.

This pass proposes targeted prose edits and linked second homes, not documentation deletion based on LOC. Unique content requires relocation before any later deletion. Frozen correction/initial evidence remains unchanged.
