# CI reader prerequisite correction — uncommitted

Published [Draft #415](https://github.com/iamcxa/kc-claude-plugins/pull/415), [Draft #416](https://github.com/iamcxa/kc-claude-plugins/pull/416) and [Draft #417](https://github.com/iamcxa/kc-claude-plugins/pull/417) are linked in native [stack #418](https://api.github.com/repos/iamcxa/kc-claude-plugins/stacks/418). Readback in `published-stack.json` confirms all three remain OPEN Draft at their approved candidates. The publication record reports lower-layer checks passed; [hosted run 34548053072](https://github.com/iamcxa/kc-claude-plugins/actions/runs/34548053072) fails 4/72 model cases on both Node 22.13.0 and 24 because the real Spacedock executable is absent. Exact original log: `/Users/kent/conductor/workspaces/kc-claude-plugins/tacoma/.context/journey-stack-recut/local-stack-commits/ci-417-failure.log`, SHA-256 `96350f046b5ac36b4799e3688240cfd4ad7535592729c23a42d15359b55a8a9b`.

## Exact correction

Only `.github/workflows/kc-journey-map-tests.yml` changes: +15/-0 lines install pinned Spacedock before existing model tests. It reuses the working repository download/checksum/install/PATH pattern from `marketplace-parity.yml`, with **stable v0.27.2**, the reader version already used by passing local proof and freshly verified here. Official Linux amd64 archive SHA-256: `8a4e58ecc64bcf7d82e3558c53f2ba5049af04ac2c99b8cdd6e51b2c469a3584`.

Downloaded official [v0.27.2 release assets](https://github.com/spacedock-dev/spacedock/releases/tag/v0.27.2); both Linux amd64 and local Darwin arm64 archive bytes match the official GitHub asset digests and downloaded checksums.txt. `release-assets.json` and `checksums.txt` preserve origin/checksum evidence. No moving latest lookup or unverified prerelease choice. Parsed workflow comparison removes only the new step and equals the original document, proving triggers, matrix, existing tests, permissions and timeout unchanged. YAML parse and Bash syntax pass.

## Actual falsifier and control

The existing five real-reader tests were run against source-identical progress test/reader/refresh files and the existing private dependency installation. With PATH restricted to `/usr/bin:/bin`, **1/5 passed, 4/5 failed**, including `spawnSync spacedock ENOENT`. With only the freshly downloaded checksum-verified v0.27.2 reader added to that PATH, **5/5 passed**. Existing tests include real task listing, refusal, projection borders, isolated-server refresh and source-byte preservation. `reader-proof.json` and missing/present logs record commands, PATH and result. No fake reader, skipped test, new harness, product semantics or test edit.

This control ran on macOS ARM64 with the recorded local Node version. An already available container was briefly inspected and is ARM64; no new environment was built. The **Linux amd64 hosted matrix remains unverified** until a later approved commit/push runs CI. Asset validity and local tests do not assert hosted green. Cost per PR is **unmeasured**; no new lane, matrix row, trigger or paid-cost claim.

Base `aabb8e7e8320b65fa852feae961df2e438d8f002`; candidate tree `64ca7b19f431188830b43f6824f485e7c90206df`. Full file/blob/SHA-256 and patch SHA-256 are in `manifest.json`. The real index is unstaged; no product commit exists. Original root, lower-layer worktrees, published branch heads, existing PR bodies/Candidate lines, stack links, old proof, user sources and existing services are unchanged. Owned test fixtures/services clean themselves up. Next authority is the Captain's exact local-commit decision; push/PR edits/ready/merge/acceptance remain unauthorized.
