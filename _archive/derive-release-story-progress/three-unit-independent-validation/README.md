# Independent three-unit review — HOLD for one documentation correction

Behavior, approved dependency/code reductions and three-unit topology **PASS** at the bounded Pilot depth. Documentation completion **FAILS** on one retained factual assertion; the exact local-commit packet is not ready for approval yet. No runtime defect or new architecture work was found.

## Exact binding

| Unit / accepted workflow | Candidate | Adjacent change |
|---|---|---|
| 1: complete editable planning | `063df758d06b2b2170a3378cdff01c4195fbc2b0` | 42 files, +6996/-311 |
| 2: optional release evidence/detail and borders | `a117b410f9b7dde03ee081f5545b4d9755d77e7b` | 35 files, +2194/-160 |
| 3: explicit optional local-task progress | `b52f204f4c6a2b6ec09f2657adcca272e97dc1a0` | 10 files, +427/-27 |

Base `c9c5752fda853737d4a937ad7f59564c5651ca53`; ambient code HEAD is only the base. Independently read each snapshot index tree, compared every file/numstat entry, hashed each patch, and replayed patches with owned temporary indexes: all exact. All immutable producer evidence hashes passed before the authorized mutable packet edit. [independent-binding.json](independent-binding.json) records this check. The producer's preliminary same-wording scratch tree differs from final unit 1 only in index membership; the actual readback file SHA-256 matches final `fa1227fb5747aae67926f2730af0f17c4c48f1369d8a0a5e7c4a61811030e158` and the final patch/tree binding is separately verified.

## Single open finding

`kc-journey-map/server/canvas-server.ts:13–15`, all three units, says a record rejected by prevalidation “would be accepted by the store and then crash every connected tab.” The retained without-prevalidation observation disproves the accepted-by-store premise: native storage rejects the malformed record with HTTP 500. The existing original and new producer controls reject the mixed batch atomically with detailed HTTP 400; prevalidation earns its place through diagnostics, not an otherwise accepting store.

Materiality: medium documentation correctness under the pinned kernel factual-claim rule; it misstates the preserved safety boundary and could misdirect a later change. This is not a runtime failure. Smallest correction is comment-only, first in unit 1 and inherited by 2/3: “Validate PATCH records before writing so malformed client input receives detailed HTTP 400 diagnostics.” Keep schema validation and server-error behavior unchanged. [finding.json](finding.json) binds exact text/source and the actual negative evidence. No self-repair was performed; First Officer owns disposition under the approved prose-correction scope.

## Closed findings and evidence

- Actual package/lock comparison independently confirms only direct `@fastify/cors` and `typescript` removed, with five removed lock nodes; no added nodes or retained-node changes. Package and lock roots agree in all three units. `@vitejs/plugin-react`, Vite config/Fast Refresh, typed source and runtime versions remain. Fresh private installation and distinct-origin browser WebSocket plus Node REST are producer observations reviewed here, not rerun claims.
- Unused `frame` is absent. A fresh independent two-case comparison covers all requested projections for the fixture (108 records) and corrected example (263 records). Records pass the native schema, and every field matches former rendering with the same input except random fractional-index suffixes normalized to sibling rank. The same instrument detects the retained missing-border mutant before accepting the corrected output. See [focused-projection-proof.json](focused-projection-proof.json). Comparing old/new installations in one process emits a duplicate-tldraw-utils warning; it is not a browser bundle result.
- A fresh doctor failure/control executes the original and corrected messages in copies without dependencies. Both properly exit 1; only corrected output gives the real YAML/Markdown fallback. The same text check rejects the original nonexistent HTML/PNG promise. Existing ports merely answered read-only probes; they were not stopped or modified. [doctor-fallback-control.json](doctor-fallback-control.json).
- A fresh native-artifact check validates all 277 records, five pages and 258 tagged records; unit 2/3 native files are identical. Both host-selection story borders remain violet, and authored event-order/no-inference wording is rendered. Mutating one saved host border to green makes that same assertion fail. [native-example-proof.json](native-example-proof.json). This complements producer browser edit/reload, restart persistence, deleted-story reconciliation/manual-note survival, native export/import/no-drift readback and PNG observations.
- Readback, explicit progress reader/CLI, App and export wrappers match prior bytes. Render/storymap/lint/room behavior matches after removing only whole-line comments; records/server differences are exactly approved frame/CORS removals. Existing core negative/control observations and six progress mutations therefore remain bounded evidence for the same seams, not new test runs. The core removals are mutation probes; malformed input is a refusal probe. No broad suite, clean install or repeated browser loop was run by this reviewer.

## Document/comment disposition

Independently compared the complete correction delta and repeated literal/alias versus tracked symbol/backlink searches; [document-searches.json](document-searches.json) retains both. The prior [60-path necessity mapping](../minimal-necessity-evidence/surface-map.md) remains the full accounting, with the following approved disposition updates:

| Corrected blocks | Independent disposition |
|---|---|
| Marketplace/adapter fallback descriptions; doctor; canvas Run | Closed: real native prerequisites and YAML/Markdown fallback replace missing HTML renderer claims; doctor negative/control distinguishes old/new. |
| Room comments and `.gitignore`; canvas source/backup; example irreversible status | Closed: unexported native state can be lost, with readback/export recovery retained. Producer restart/removal observations match. |
| SKILL rendering/verification; canvas projections/model/status; cell-contract surfaces/stories/statuscard | Closed: map-only intent can omit evidence, full statuscard belongs to journey-board or accompanying report, unsupported `later` removed; actual model/readback/evidence contracts remain. |
| Function-map comments; canvas events; example handoff/release/rule | Closed: authored modeling input does not infer causal/build order; actual native artifact contains corrected rule. |
| CI introduction; example test introduction; records/render/storymap/lint comments | Closed: unmeasured CI cost and no-browser scope are explicit; restatement/overclaims trimmed, non-rederivable sizing/order/readback/lifecycle rationale retained. One schema rationale above remains open. |
| Canvas portability historical counts; example dated status/source | Closed: repeatable commands replace mutable count proof; original dated canvas/YAML are preserved under producer historical-source. The example labels historical claims and supplies no live delivery evidence. |
| SKILL/canvas/progress repeated commands and page definitions | Closed: invocation conversation stays in skill, canonical setup/export/readback in canvas, mapping/reader limits in progress skill, meanings in cell-contract. Unique example open/import/readback/regenerate instructions and overwrite warnings survive. |

No complete document was deleted by this correction; prior standalone migration and lifecycle registration are unchanged. Conversation facilitation, evidence/acceptance limits, native portability, duplicate/stale/lossy-readback boundaries and licence notes remain. This is a bounded per-section disposition, not a new documentation system or a claim of mathematically irreducible prose.

## Acceptance and topology boundary

AC-1: complete drawing remains independent of task progress. AC-2: exact unchanged local identity/aggregation code preserves story rather than task ratios. AC-3: prior archive/reopen/ambiguity/unknown evidence remains bound. AC-4: saved native borders and authored/derived provenance retain pending delivery acceptance. AC-5: unchanged stable-ID readback code, new producer same-wording/second-ID and distinct-wording control preserve source bytes. The broader native persistence/story/evidence checks cover predecessor obligations beyond those five progress ACs.

Three units remain the smallest defensible grouping under the accepted criterion: one complete person workflow per review unit and no temporary detail implementation immediately replaced by borders. A separate infrastructure unit has no established ownership need; two units would conflate planning intent with optional evidence inspection. This is a responsibility/reviewability conclusion, not “fewer PRs means minimum code.” Repository-native parent-branch stacking remains later delivery work; existing Draft #394 is not treated as a fourth candidate.

Mutable proposed subjects were corrected to `feat(kc-journey-map): ...`; immutable producer manifests/closure remain unchanged, so their original subject spelling is historical input. Candidate code/trees, old five layers, source rooms and services remain untouched. No owned service/browser was started. No provider/PR/merge/acceptance claim; CI cost per PR remains unmeasured. Stop at this finding and route only the bounded comment correction; no new audit/design loop is warranted.
