## Evidence
DISPATCH_TOKEN: effeac9f1e5a79d2
CANDIDATE_SHA: 6573daaa9bd9c40707c699fa582726c28ffa51b0
BRANCH: feature/dev-84-intent-commit-fencing-at-most-one-worker-across-holder
BASE_SHA: 3f263c3bb7e91144e3793e3d4f5cf1368906f436
FILES: docs/dev/README.md, scripts/ship-flow/fenced-dispatch.sh, scripts/ship-flow/intent.sh
TESTS: python3 scripts/kc-dev-flow-contract-test.py -> exit 0
SURFACE: scripts/ship-flow/intent.sh -> falsifier | `scripts/ship-flow/intent.sh reconcile <state> <holder> <writer>` after two concurrent commits and a mismatched-project adopt (see LOCK, AC-2, BINDING evidence below; touches scripts/ship-flow/intent.sh) | git checkout 3f263c3bb7e91144e3793e3d4f5cf1368906f436 -- scripts/ship-flow/intent.sh
SURFACE: scripts/ship-flow/fenced-dispatch.sh -> falsifier | `scripts/ship-flow/fenced-dispatch.sh <state> <holder> <writer> <claim> <project> <base> <msg> --pause-before-adopt` (see AC-2 SIGSTOP evidence below; touches scripts/ship-flow/fenced-dispatch.sh) | git checkout 3f263c3bb7e91144e3793e3d4f5cf1368906f436 -- scripts/ship-flow/fenced-dispatch.sh
SURFACE: docs/dev/README.md -> AC-5 | python3 scripts/kc-dev-flow-contract-test.py checks docs/dev/README.md's pinned intent-commit phrases (see AC-5 mutation run below) | git checkout 3f263c3bb7e91144e3793e3d4f5cf1368906f436 -- docs/dev/README.md
WITHOUT_IT_COMMAND: python3 scripts/kc-dev-flow-contract-test.py
WITHOUT_IT_REMOVED_VARIANT: git checkout 3f263c3bb7e91144e3793e3d4f5cf1368906f436 -- docs/dev/README.md
WITHOUT_IT_OBSERVED: retained -> exit 0 ("kc-dev-flow contract: PASS"); removed -> exit 1 ("Ship-flow runtime omits the intent-commit rule: the production entry issues at most one `conductor workspace create` call after a committed intent")
ROBOREV: UNAVAILABLE(reason: no reviewer binary in workspace)
LOCK: Portable lock replaces flock. Seen to fail first: (a) the round-1 `flock`-based lock, run with PATH restricted to a shim dir containing no `flock`, died `line 14: flock: command not found` / `intent: lock timeout` exit 6 on every commit — reproducing exactly the FO-host failure described. (b) my first mkdir-based rewrite also failed once under the same no-flock PATH with `line 1: d: unbound variable`: the EXIT trap referenced a function-`local` lock-dir variable that was already out of scope by the time the trap fired under `set -u`; fixed by making the lock-dir path a script-global (`LOCK_DIR`, not `local`) so the trap can still see it. After the fix, under a fully clean `env -i PATH=<no-flock-shim>` (no `flock` reachable anywhere, confirmed absent from the shim): 3 iterations x 2 parallel `commit` calls for different claims all succeeded (6 commits: 308efe4/463792f, 6e73824/a2441d0, c948503/1ba3b90), and a repeat of an already-committed claim exited 4 ("intent exists for noflock2-a-1 (unresolved); reconcile, do not create"). Lock dir is `<state>/.git/ship-lock.d` (mkdir-atomic acquire, pid+timestamp written inside, 120s+dead-pid staleness rule, removed by an EXIT trap on every exit path).
BINDING: Two cases. (1) Wrong project entirely, re-confirmed under the new lock code (via the `conductor` test shim, no new real workspace needed): `intent.sh adopt` with intent.project=00000000-... against a workspace whose real project is 8f58f9d4-...: "intent: project mismatch: intent=00000000-0000-0000-0000-000000000000 workspace=8f58f9d4-cb71-443a-b64d-c2a225248c7b; refusing to adopt 33333333-..." exit 8. (2) Codex P2 — same-name workspace in another project must not cause `ambiguous intent`: a real `conductor workspace create` with an exact duplicate name across two different real projects (kc-claude-plugins and qnow) turned out to be impossible to reproduce live because Conductor itself auto-suffixes the second create to `-v1` on an org-wide name collision (observed directly: the qnow workspace's actual registered name became `...-v1`), so the collision was reproduced with a `conductor` CLI test shim returning two same-name entries in different projects. Old (round-1) reconcile counted both by name first and blocked: "ambiguous intent, 2 live workspaces named dev-84-crossproj-...: 11111111-... 22222222-...; BLOCK" exit 5 — seen to fail. New reconcile filters each name-matching candidate by its real project (one more `conductor workspace get` per candidate) before counting: "adopted 11111111-1111-4111-8111-111111111111 (exact name match)" exit 0, correctly ignoring the wrong-project namesake. All real probe workspaces from this investigation (dfccf17f, 0cf87ea2) archived.
AC-1: Re-verified against the round-2 candidate: intent already committed for `dev-84-ac1c`; `fenced-dispatch.sh` for the same claim died at "intent not committed (exists or fenced); reconcile instead of create" exit 4, no `conductor workspace create` line reached. Unchanged from round 1 (this path doesn't touch the lock or the reconcile counting fix).
AC-2 (DEV-79 SIGSTOP falsifier): re-run end-to-end against the round-2 candidate with a fresh real workspace. holder-a created 145a0742-6591-4216-8a12-f0cd39e44609, SIGSTOPped before adopt. holder-b handed over (writer 1->2) and `intent.sh reconcile` logged "adopted 145a0742... (exact name match)" exit 0 — no `conductor workspace create` in B's path. Resumed holder-a's own adopt logged "adopt refused (fenced, already adopted, or project mismatch)" (root cause confirmed fenced: writer 2 vs caller 1) — refused before any further external action. Workspace archived.
AC-3: Not re-run with a fresh real workspace this round (to bound probe-workspace spend); carried on code-inspection plus AC-2's re-run as coverage: the late-arrival "0 live workspaces -> unresolved intent, BLOCK" branch is untouched by both this round's changes (the new per-candidate project filter only executes over a non-empty name-match list, and the lock only wraps `commit`/`adopt`, not the `reconcile` list/count path), and the "1 candidate -> adopt" branch is the exact path AC-2 just re-confirmed live. Round-1 log stands: early reconcile "unresolved intent, no live workspace named dev-84-ac3-...; BLOCK (do not create)" exit 4; second reconcile after delayed create landed "adopted 4304a1b6... (exact name match)" exit 0.
AC-4: Unaffected by this round (the without-it variant `.context/fenced-dispatch-no-intent.sh` never calls `intent.sh`). Round-1 evidence stands: without intent-commit, holder-a and holder-b independently created two live workspaces (4dd47b9e, 673981a3) for the identical claim; both archived.
AC-5: Unaffected by this round (README/contract-test unchanged). Round-1 evidence stands: paragraph removed -> contract-test exit 1 on the missing pinned phrase; restored -> exit 0. Re-confirmed exit 0 again this round via the full contract-test run above (candidate 6573daa).
AC-6: Probe workspaces created and archived this round: 145a0742-6591-4216-8a12-f0cd39e44609 (deleted), dfccf17f-5f79-4e27-812f-a3bd899a7e47 (archived), 0cf87ea2-a468-4a17-ba45-76e36b141145 (archived) — all confirmed non-live by direct `workspace get`. Combined with round 1's archived set (473cc3cd, 4304a1b6, a1ea5fa5, 673981a3, 4dd47b9e), nothing from this work is live. At most 1 real probe workspace was ever alive at once this round (well under the 4-alive budget). All coordination-state races (LOCK, AC-1, cross-project BINDING) ran against throwaway `git init --bare` fake-origin repos under `/tmp`, never against the real shared production `spacedock-state/dev` branch, which remains untouched by this work.
BLOCKER: non
