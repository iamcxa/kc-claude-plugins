# Station 0 re-verification, plan round 2026-09-04

Candidate cluster the Captain selected at station 2: "tests run nowhere in CI".
Each row is one command run against the working tree at 420e7282 on 2026-09-04.

| Candidate | Observation (date) | Command run today | Result | Disposition |
|---|---|---|---|---|
| `e2e-pipeline-suite-has-no-ci-job` | 949 tests, no workflow runs them (2026-08-01) | `git log --diff-filter=A -- .github/workflows/e2e-pipeline-tests.yml` | workflow added by #173; real-browser by #181; `npm run test:ci` runs the suite | **dropped — fixed** |
| `tracked-tests-hardcode-another-repo` | 5 tests hardcode `/Users/kent/Project/carlove` (2026-07-26) | `grep -nE "/Users/\|E2E_CORPUS_ROOT\|skip" e2e-pipeline/compiler/test/integration.test.js` | absent corpus now skips; `E2E_CORPUS_ROOT` overrides | **dropped — fixed** |
| `monorepo-ci-test-strength` | 61 of 71 test files belong to plugins no workflow mentions (2026-07-26) | file census + workflow/glob reachability | 53 unreached: 49 kc-nightwatch, 3 kc-hyperfocus, 1 kc-pr-flow | **held — but see ruling below** |
| `spacedock-route-test-passes-nowhere` | skips in CI, fails locally (2026-08-20) | `grep -n "which\|SKIP" kc-dev-flow/scripts/profile-spacedock-route.test.py`; `grep -n contract-test .github/workflows/kc-dev-flow-release-gate.yml` | skip guard present; release-gate installs spacedock but never calls contract-test | **held** |

## Corrections this station produced

- A first pass reported five kc-dev-flow `.test.py` files as unreached. False: `scripts/kc-dev-flow-contract-test.py`
  executes all five at line 350, and `marketplace-parity.yml` runs it as a required check. The grep
  looked for the filename in workflow YAML only and missed execution one level down. Fixed before any
  Issue was cut.
- `kc-nightwatch` holds 49 of the 53 unreached files. The Captain ruled on 2026-08-23
  (`kc-nightwatch-deprecated-in-place`) to stop investing and leave it published. That entity's own
  body records a prior session shaping CI work around these same 49 files before the ruling surfaced.
  Adding CI here would repeat it.

## Outcome

Two of four candidates dropped as already fixed. The remaining pain does not support a six-Issue cut,
so this cluster is not the batch. Station 2 re-runs on the remaining candidates.
