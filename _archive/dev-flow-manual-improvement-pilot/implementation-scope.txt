Captain explicitly confirmed a local controlled loop after forbidding Spacedock upstream changes. Current task brief is authority; old upstream-dependent plan and stopped delivery are historical only. Selected retained finding: surface-map-check.py fails test-only Go changes because _test.go is missing from EXCLUDE_SUFFIXES, contrary the existing Pilot non-test scope. FO baseline on exact main 0ec3380f590cbaf5b01ee1c325eb222da99a3c5a: temp Git diff only internal/task_test.go and empty evidence returns 1 missing SURFACE line; default expected 0. --no-exclude correctly returns 1. Source bytes match latest main. First verify newly created assigned worktree ownership and cleanliness, then fast-forward ONLY that new worker branch to this exact fetched base if needed; never alter coordinator main/root or stopped repair trees. Exact repair files: kc-dev-flow/scripts/surface-map-check.py and scripts/kc-dev-flow-contract-test.py; <=150 gross lines including tests. No commits, push, PR, external comments, install, new provider/cloud/model launch or original task cleanup. Existing-session host worker only. Use simple retained one-case execution evidence, no new standing runner/service. Check all relevant queue candidates and preserve PR-feedback-first disposition from current scope report. Record duplicate identity and refusal to launch duplicate work honestly. Minimal controls must ensure production Go remains required, --no-exclude remains required, explicitly mapped Go test remains checked, and old faulty producer makes the same regression fail. Native case subprocesses max60s, no automatic retry; inherited shape cost ceilings were proposals, not proof of a hard cap. No full-suite rerun without relevance. Root session usage may be observed from exact own log; never attribute shared CODEX_THREAD_ID to child as separate usage, never expose chats or secrets. Unknown actor usage => no whole-cycle cost/efficiency claim. Keep product edits isolated; state artifacts/report belong in supplied state root and use state prerequisite/native state commit. Current user approval supersedes old upstream premise; native gate consume accepted old attempt and advanced implementation, observed but not authorization to revive any stopped work. Independent reviewer runs after your report. Keep this bounded and concise.
