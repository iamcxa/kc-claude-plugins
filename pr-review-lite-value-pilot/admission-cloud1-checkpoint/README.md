# Cloud admission checkpoint — PASS

The one authorized Cloud admission completed on 2026-09-20 before either review
arm. It independently applied the frozen control's legacy triage to PR #434 and
returned Lite / bugfix with `full_pass`, `probe_required`, `cross_model` and
`noise_filter` all false. The unmodified candidate planner returned the same six
modes for the identical repository, PR, base/head and file metadata. No mode was
forced and no review finding was produced.

The registered one-claim launch used an explicit prompt file, stdin isolation for
all preceding non-model subprocesses, preopened durable stdout/stderr, a 1,200-second
external process-group timeout, normal provisioned Cloud OAuth, and the registered
per-process traffic controls. The command exited 0 after 80.384955883 seconds. The
single final result receipt reports success, `claude-opus-5`, USD
0.46017775000000005 list-basis estimated host cost and complete usage fields. This
reported cost is not proof of an additional subscription charge or a hard invoice
ceiling; the prior Mac attempts retain unknown final costs separately.

The effective init receipt contains the pinned control plugin version 1.12.0,
Read/Grep/Glob/Bash, zero MCP servers and `dontAsk`. All model messages identify
Opus 5. Tool calls were read-only Glob/Read/Grep plus one allowed `git diff
--numstat`; no permission denial, Agent, write, web or MCP call occurred. Debug
evidence reports zero active hooks, no declared marketplace and skipped plugin
auto-update. There is no marketplace pull/clone or model fallback evidence. The
target and product tracked trees remain clean.

Private raw evidence remains under
`.context/pr-review-lite-value-pilot/runs/dev434-cloud1/admission/`. The sanitized
[summary](summary.json) binds its hashes. Admission analysis is not supplied to
either review arm. The next registered action is the control arm; control timing
must begin before its fresh checkout/acquisition and include complete review plus
confirmation-request delivery.
