# Subsequent real-trial proposal

Status: proposal only; not launch-ready, not sealed, and not authority to claim or spend.

## What would be tested

Use the immutable PR434 snapshot in iamcxa/kc-claude-plugins, base 33ffd50b217004411f58277cf986ccee7974fe55 and head 29007f90fc628ae40faf2903dd02d9895f4dcffe. PR434 is now MERGED; the snapshot, not the live PR state, is the target. Its purpose remains the two-file kc-journey-map correction that measures the one-journey banner at actual width and restores the 10px draw offset while preserving the geo-height invariant.

The future run must cover all six required Lite dimensions:

1. goal alignment
2. code correctness
3. test evidence
4. security risk
5. silent failure
6. documentation accuracy

The route is direct claude-opus-5 at high effort on the trusted authenticated host. It uses the supported normal first-party OAuth boundary, restricted/safe mode, explicit empty setting sources, StructuredOutput-only tool metadata, no plugin directories, no MCP servers, no JEV, no outer model intermediary and no second full LLM pass. Posting stays off; the result is an unpublished preview until a human separately decides publication.

## Why it cannot launch now

The consumed R4 packet, supervisor, claim, archive and recorder are historical evidence and must never be reset or relaunched. This document does not create a replacement packet.

The current worktree has only two dirty scripts. That projection omits the corrected catalog blob required for .mjs classification and does not match the passed kc-pr-flow subtree. The executable still equal-splits USD 2.4806 over twelve possible attempts (USD 0.20671666666666666 each), which previously produced three censored error_max_budget_usd outcomes. Repeating that split would not be an informative use of another paid proof.

Known-cost retention is repaired, but genuinely missing, ambiguous or invalid cost remains unknown. A numeric partial total is not complete spend. The USD 2.4806 threshold governs child-reported costs only: it is not an invoice cap, cannot prevent in-flight overshoot, and excludes Cloud, supervisor, assistant, subscription and invoice costs.

## Three separate authorization boundaries

### 1. Minimal exact correction commit

The already validated correction consists of exactly:

1. kc-pr-flow/schemas/review-capability-catalog-v1.json
2. kc-pr-flow/scripts/review-capability.py
3. kc-pr-flow/scripts/review-capability.test.py

The catalog change is one line: add mjs to the code_change path pattern. Materializing those exact three blobs over HEAD produces the validated kc-pr-flow subtree. The proposed English title is "fix(kc-pr-flow): harden Lite provider custody and typed output". A separate commit decision may authorize this correction alone. It must not stage marketplace.json or any unrelated repository evolution.

Budget allocation is not in this correction commit and is not a prerequisite to committing the passed correction.

### 2. Later budget-allocation implementation and packet preparation

Before another paid proof, a separately authorized model-free batch should:

1. Implement an explicit per-lane allocation interface while retaining the unchanged aggregate child-reported threshold of USD 2.4806, six dimensions, at most two attempts per lane, 120-second attempt limit, exit-75-only first retry and zero whole-run retries.
2. Validate that later candidate with local stubs and the existing provider-schema/compiler boundary; retain invalid/unknown-cost, timeout, cleanup, duplicate and exact-once falsifiers. No provider/model call belongs in this batch.
3. Select and identify the exact later product tree after independent review.
4. Build a new task-private packet from that exact tree with a new run identity, absent claim path and empty mutable output/archive/receipt locations. Preserve all source, dependency, runtime, auth, target, contract, server-row and duplicate-custody guards.
5. Present the fresh packet identities and preflight evidence for a separate decision. Do not claim, launch or record delivery during preparation.

An analysis-only candidate allocation per lane per possible attempt is: code correctness 0.31, documentation accuracy 0.26, goal alignment 0.14, security risk 0.17, silent failure 0.23, test evidence 0.13. It totals USD 1.24 for one attempt in all six lanes and USD 2.48 for two attempts in all six, leaving USD 0.0006 under the threshold. It clears the three observed censored spends by only USD 0.004615, 0.002086 and 0.00654. That arithmetic does not prove any lane can complete, so adopting this vector or a higher threshold is a separate scope decision and must be validated before packet preparation.

### 3. Separate paid-launch decision

The later paid decision is about one exact fresh packet only. Correction-commit approval or budget-implementation approval does not imply launch authority.

## Required fresh-run custody

If and only if a later decision separately approves the exact fresh packet and paid proof:

- Start external timing before the first trial-specific preparation and retain preparation-through-public-delivery elapsed separately from the supervisor window.
- Complete every preflight before claim creation; the supervisor alone atomically creates the absent claim once.
- Give the supervisor one unreset 900-second deadline. Give each child attempt at most 120 seconds. Retry only the first exit-75 result once; never replay the whole run.
- Bind the exact integrated product tree, exact frozen PR434 target, runtime/executable/auth/config hashes, six required requests, attempt argv/environment-key names, streams, terminals and costs.
- Fail closed on target/config/auth/startup drift, unexpected tool/plugin/MCP/hook/settings behavior, missing required capability, invalid/unknown cost where completeness is required, timeout, mutation, cleanup failure, threshold breach, judgment/finalization failure, archive failure or delivery-query failure. Retain partial evidence without converting it to success.
- After child completion, perform the assistant judgment once, finalize once, archive once and produce the unpublished public result with cited evidence, gaps and actions.
- Only after successful public delivery invoke the fresh sealed recorder exactly once to bind the authoritative Conductor row and receivedAt. Duplicate or mismatched run/claim/marker/archive/server-row evidence must refuse.
- Make no GitHub comment or review post, product commit/push/PR/merge, fallback provider, JEV call or second full LLM pass.

## Success and stop interpretation

A successful mechanical run must still show six usable capability results, bounded custody, honest known/unknown costs, completed judgment/finalization/archive and one authoritative public delivery receipt. It would establish only one decision-ready unpublished preview on the frozen case. It would not by itself prove savings, calibration, two-day fit, release readiness, the proposed 17-story slice or general provider reliability.

Any required-lane failure, unknown complete spend, timeout, mutation, permission/config widening, missing archive/receipt or delivery ambiguity is a STOP for that one run. There is no whole-run retry and no consumed-packet fallback.

## Single next decision

Authorize or decline only the minimal exact three-file correction materialization and commit described in boundary 1. It contains no budget-policy change and grants no paid-proof authority. Budget-allocation implementation/validation is the later boundary 2; one exact fresh-packet paid launch is boundary 3.
