# Cloud treatment checkpoint — STOP

The single authorized Cloud attempt stopped during the treatment outer process on
2026-09-20. The treatment interval began at
`2026-09-20T03:21:23.287772779Z`, before its fresh checkout and acquisition.
The pinned outer Claude process reached its init receipt and began inspecting the
frozen inputs, but FO cancelled its process group before the repository-owned
adapter was invoked. The launch returned signal 15 / exit 143. By
`2026-09-20T03:22:16.603525066Z`, process readback showed no surviving treatment
or adapter process.

The stop is mandatory under the Cloud registration. A read-only ensign preflight
established that the unchanged CLI adapter launches its five child Claude
processes with an in-memory stdin pipe and piped stdout/stderr, discards child
stderr, and records neither exact child argv/environment keys nor a startup/init
receipt. Its child argv does not include the registered Cloud outer controls
`--restricted`, empty setting sources, explicit settings, or an explicit empty
plugin surface. The inherited traffic flags and tools-disabled request do not by
themselves prove the required effective settings/hooks/plugins. There is no
authorized wrapper or candidate repair that could add this custody evidence while
keeping the pinned treatment unchanged.

FO launched the outer process before the ensign's requested preflight reply
arrived. That sequencing error consumed the one treatment launch even though the
adapter itself had not yet run. The raw stream contains 26 events from session
`be68abef-9503-4745-a6b3-cf8c4f48db14`, only `claude-opus-5`, the expected outer
init surface, and no final result receipt. Therefore treatment outer cost is
unknown and must not be treated as zero. No capability child call, retry,
reviewer judgment, finalization, complete treatment review, confirmation request,
or `END REVIEW` occurred.

Five unique outer message IDs expose only partial snapshots: 10 input, 74,175
cache-create, 217,768 cache-read and 17 output tokens when summed by unique
message ID. These are retained diagnostics, not a complete usage receipt and not
a basis for reconstructing dollars. The observed tools were one Skill load, two
Reads and four Bash calls; there was no Write, adapter process or provider raw
envelope.

Admission and control remain valid standalone evidence from this Cloud attempt;
the comparison is invalid and cannot be scored. Calibration and adjudication were
not run. The missing all-three calibration-key confirmation is still unrecorded,
but it is no longer the immediate blocker because this attempt has already
stopped. The control's retained non-required malformed-path denial remains
documented in its own checkpoint and was not replayed or repaired.

Private raw files remain under
`.context/pr-review-lite-value-pilot/runs/dev434-cloud1/treatment/`. The summary
binds their hashes without copying private streams into task state. No product
file, PR, review, remote comment, merge, release, default or workflow stage was
changed. No additional model launch, adapter repair, arm replay, calibration or
judge call is authorized from this checkpoint.
