# Validation correction: retain current bytes on knowledge rollback

Finding `annotation:rollback-peer-edit-loss`, disposition `resolution:fo-repair-rollback`, and unchanged feedback receipt `feedback-validation-1.json` authorize this correction under the existing eight-file/850 bound. Installed correction pin loads successfully for `implementation-correction-1`; task status remains validation. Rejected patch SHA-256 `cabbdd5221aec0b94597f4aeaca455810fe75b90922d76c4226fefda7547efe5` identifies an uncommitted diff, not a fabricated commit.

Knowledge archive commit-failure rollback now reverses the location/index staging while retaining the current archived entity bytes, including its single consumed approval. Native retry revalidates that consumed approval and its immutable evidence. Legacy product rollback still restores its prior bytes. No new approval field, rejection predicate or callback remains: once rollback never writes a stale preimage, those additions are unnecessary.

The retained native regression injects a body edit in each vulnerable interval: after capture but before consumption, and after consumption but before archive proof. A rejecting Git hook captures actual archived bytes. Both cases require failed commit, byte-identical current content restored live, peer text and consumed approval retained, then successful native retry with the same single consumed approval. Removing only the final production correction using a temporary test overlay makes both cases fail (`correction-window-red.json`). Final focused tests pass 76 events (`correction-final-focused.*`).

The unchanged independent reproducer also passes (`correction-final-independent-replay/result.json`): actual commit failure, archived peer edit present, live restored, peer edit retained. The earlier preimage-rejection experiment is retained as historical `correction-focused.*` / `correction-independent-replay/`; it is superseded and not evidence for the final source. The initial failing regression is retained in `correction-before.json`.

Exact final upstream patch: `6dc9e67966b29ba29eca9ce28a375081ff6c0345ac32f4ef13846bc8d0e2f93e`, eight files / 801 gross against `af70297ddae6ec64444849e8e3fcf57484bc16e1`. Only merge.go and the existing merge_guard_test.go changed from the rejected candidate. Local patch remains `2ffa88aaef51a4b482a71d190b5f3cbcbc2a5579d539a7cb87adfab12d685d0c`, one file / 24 gross against `c9c5752fda853737d4a937ad7f59564c5651ca53`. Both product branches/HEADs remain unchanged and uncommitted; exact patches/readback retained as `correction-{upstream,coord}.patch` and `correction-readback.json`.

All 18 live original task/evidence/pin files remain unchanged. Copied-original positive publication recovery and local contract PASS remain producer evidence bound to the rejected predecessor; those unchanged journey surfaces were independently reviewed, while fresh final-source native tests cover the affected rollback/retry path. This does not rerun or claim original cloud/task closure or live rebinding.

Required full-tree gofmt ran and only its verified pre-existing release-test whitespace delta was restored before final checks (`correction-format.json`). No ninth file, new gate protocol, provider call, product commit, installation, or cloud/resource action. The inherited hard crash between rename and commit stays excluded. Independent acceptance and Captain exact-file commit confirmation remain outstanding. Object-only surface-map/RoboRev remain unavailable without a permitted candidate commit; zero requests.

Final-source required full and race commands each return 1 with 3,107 passing test events, 11 skips, and only TestContractCasks/edge-satisfies-pin plus its parent failing (2 failure events). Both runs explicitly bind the corrected hash. No data-race diagnostic. The prior serial candidate/untouched-base controls establish the unchanged 0.27.1 edge cask versus 0.28 pin mismatch; no redundant control or release-policy change was made. These runs are not all-green acceptance.
