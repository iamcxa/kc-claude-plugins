# Integrated review candidate

The approved native knowledge route now consumes the exact preselected terminal approval under the existing gate lock, resolves strict knowledge declaration and retained evidence from immutable Git objects, verifies no product diff and clean exact product identity, and writes the whole checked entity. The existing archive owner revalidates that same consumed approval and frozen evidence while retaining the native lock and checking actual bytes. Retry does not spend approval again. Failed archive commit does not roll back over intervening writes.

## Exact uncommitted scope

- Upstream: base `af70297ddae6ec64444849e8e3fcf57484bc16e1`, eight approved files, 743 gross lines. Patch SHA-256 `cabbdd5221aec0b94597f4aeaca455810fe75b90922d76c4226fefda7547efe5`.
- Coordinator: base `c9c5752fda853737d4a937ad7f59564c5651ca53`, only `docs/dev/_mods/pr-merge.md`, 24 gross lines. Patch SHA-256 `2ffa88aaef51a4b482a71d190b5f3cbcbc2a5579d539a7cb87adfab12d685d0c`.
- Both branches remain `spacedock-ensign/knowledge-output-cannot-terminalize`; no product commit, push, PR, installation, provider/model request or cloud run. Path-scoped state evidence publication is separate.

## Review evidence

`integrated-focused.json` records 29 passing knowledge test events, including native archive-once, clean committed archive push failure and native resume, true peer conflict, approval/evidence tamper, whole-byte comparison and intervening-write protection. Existing product regression checks also execute in the required suites. `integrated-local-contract.json` records the existing coordinator contract PASS.

`copied-original-native-complete.json` uses complete local main/state Git objects and the original task snapshot. The unchanged copy remains blocked under its reason sentinel; the explicit route refuses that sentinel. A TEST ONLY rework preserves the historical approval as superseded, then a separate synthetic declaration and native approval produce finalized/unpublished after an intentionally rejected local push. Native state commit publishes the same archive commit; repeat is no-op, exact archived bytes and HEAD unchanged. These are synthetic decisions, not Kent's live approval. Earlier dirty completion-artifact refusal was corrected only by committing the disposable copy; raw output is retained.

All 18 live original task/evidence/Briefing/pin files remain byte-identical to the initial snapshot. The original semantic archival approval persists; it never contained the new typed declaration, and live native rebinding remains with the original task owner. No original closure, resource cleanup, independent cloud export, final cost measurement or automatic reauthorization is claimed.

## Required-check boundary

Full test run returned 1: 3,104 pass events, 2 fail events (one failing leaf plus its parent), 11 skipped. The only failed leaf is `TestContractCasks/edge-satisfies-pin`: published edge cask 0.27.1 is below the existing 0.28 contract pin. Serial isolated controls reproduce the identical assertion on the final candidate and a disposable untouched pinned base; see `release-cask-control.json`. This is a proven pre-existing external release-state failure, not a knowledge repair failure, and no release/install scope was added.

Required full-tree gofmt returned 0 but exposed a pre-existing four-line whitespace delta in `internal/release/runtime_live_evidence_workflow_test.go`. Exact current bytes were verified equal to gofmt(HEAD), then only that delta was restored. Evidence is retained in `preexisting-gofmt.*`. Full run began before this restoration; final-source race evidence is reported separately. No unrelated formatting is included in the candidate.

The inherited hard crash between rename and archive commit stays outside this bounded repair. Recovery proof covers clean committed archive publication failure only. Fresh independent validation and Captain exact-file confirmation remain before product commit. Object-based surface-map and RoboRev are UNAVAILABLE without a committed candidate; manual map supplied, zero RoboRev requests.

Final-source required race run also returned 1 with 3,104 pass events, the same failed leaf and parent (2 fail events), and 11 skipped; no data-race diagnostic. It records exact candidate SHA-256 above. Thus both required suites ran, but neither is represented as all-green.
