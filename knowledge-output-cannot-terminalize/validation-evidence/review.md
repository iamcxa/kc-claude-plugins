# Independent validation — REJECTED

Reviewed exact approved Pilot candidate at attempt `validation/1`; installed 4.3.0 profile loader accepted its frozen pin. No product files, staging, commits, installation, PRs, live original approval, providers, models or cloud resources were changed.

## Blocking finding: archive commit rollback overwrites an intervening body edit

**High severity; AC-4.** `internal/status/merge.go:497-505` protects only modifications made after the archived-byte snapshot. `captureArchiveState` reads the rollback content earlier at `merge.go:750`, before the terminal consumer takes its lock. If another writer appends a body edit in that interval, the consumer correctly validates/preserves the current bytes and the archive contains that edit. When Git commit then fails, the new condition sees an unchanged archive and `rollbackArchive` at `merge.go:803` writes the stale pre-lock `snap.content`, silently deleting the peer edit. This is an ordinary commit failure, not the excluded rename-to-commit hard crash.

A disposable test-only Go overlay injects the peer append during `captureArchiveState`'s subsequent `git ls-files` call, then a fixture pre-commit hook captures the archive and returns failure. Actual candidate result: `rc=1`, `peer_edit_present_before_failed_commit=true`, `live=true`, `peer_edit_retained=false`. The existing test for an edit made *inside* the failing hook does not cover this earlier interval.

Evidence: [raw failure](rollback-prelock.json), [archive before failed commit](rollback-before-commit.md), [restored live entity](rollback-after-failure.md), [test addition](rollback-prelock-test.patch). Before hash `fab8d954a3bfbd5205f60f4de02f99c255247006051199bed19f3a0b1cc91140`; restored hash `e6a69437952f62c281316b1b037b1a6408e3ed22465b2d7b44bc71f84f40b7dd`. Both exact files were retained before fixture cleanup. Reproduce without product edits: `rtk proxy python3 reproduce-rollback.py /absolute/candidate /absolute/disposable-output` from this evidence directory. The harness creates only a temporary Go overlay and disposable native fixture.

**Smallest fix:** bind the rollback preimage to the same under-lock snapshot checked by the knowledge consumer, or refuse rollback when that preimage cannot be proven current. Keep the repair within the already approved `merge.go` / native consumer surfaces and add this one regression alongside the existing rollback test. Preserve normal product behavior; do not widen crash-recovery scope.

## Acceptance evidence

- **AC-1 PASS:** freshly rebuilt exact candidate native finalization reports finalized, PASSED, consumed, active absent. Retained producer original-copy archive independently hashes to its recorded bytes and preserves evidence.
- **AC-2 PASS for exercised controls:** fresh missing approval/declaration, changed identity/digest/worktree, real successor substitution, Briefing/evidence tamper, PR/reason sentinel, product claim/diff/dirt/head and whole-entity drift tests refuse without mutation. Immutable evidence is resolved through Git object plus content digest; the flag alone never supplies authority.
- **AC-3 PASS:** fresh genuine merged PR/local-merge and open/malformed/missing product-proof controls preserve prior behavior. Rejected verdict is not the knowledge happy path.
- **AC-4 FAIL:** fresh ordinary positive/archive-once/consumed-resume/committed-publication retry/conflict/tamper tests pass, but the independent pre-lock-edit + failed-commit case above loses data. The pre-existing hard crash remains excluded.
- **AC-5 VERIFIED PRODUCER PROOF:** read every original-copy command/result; retained copied archive matches SHA-256 `36244d099e3ffb452b89c8d75a49e22a0cadd0761c47ded7a6a2a1e5a524429e` at unchanged copy HEAD `de9a1f34e492ea5c812020c1e85accf01e45c11a`, clean status, historical superseded approval and one new consumed synthetic approval. Baseline is rc0/blocked; explicit route refuses the original sentinel; synthetic native rebinding then finalizes and native state commit recovers publication. The producer executable was temporary and is no longer retained; this worker freshly compiled/exercised the actual candidate through focused tests, not a claim to replay the original-copy CLI build. All 18 live original files match the captured source revision/hash; no cloud operation was run. This proves a copied test journey, not real original closure/cleanup/final measurement.

## Candidate, required evidence and scope

- Upstream root `.context/spacedock-knowledge-delivery`; HEAD/base `af70297ddae6ec64444849e8e3fcf57484bc16e1`; branch `spacedock-ensign/knowledge-output-cannot-terminalize`; exact full binary diff SHA-256 `cabbdd5221aec0b94597f4aeaca455810fe75b90922d76c4226fefda7547efe5`; 8 approved files / 743 gross, within 850.
- Local code root `.context/routine-coordinator/.worktrees/spacedock-ensign-knowledge-output-cannot-terminalize`; HEAD/base `c9c5752fda853737d4a937ad7f59564c5651ca53`; same branch; exact diff SHA-256 `2ffa88aaef51a4b482a71d190b5f3cbcbc2a5579d539a7cb87adfab12d685d0c`; 1 approved file / 24 gross, within 40. Both final hashes unchanged; both diff checks and changed Go formatting pass.
- [Fresh focused tests](focused.json): 74 pass, 0 fail, 0 skip; explicit native knowledge, terminal approval and existing merge delivery paths. Independent regression separately fails as above. Tests are meaningful refusal/state checks: deleting approval/product checks allows forbidden writes; breaking archive recovery changes archive bytes/HEAD or remote conflict preservation.
- [Producer audit](producer-evidence-audit.json): recounted full/race raw events each 3,104 pass / 11 skip / 2 fail (one cask leaf plus parent), no race diagnostic. Only `TestContractCasks/edge-satisfies-pin` fails: edge cask 0.27.1 is below contract 0.28. Serial untouched-base control reproduces the same assertion. Full run included a subsequently restored pre-existing whitespace-only delta; race explicitly binds the final patch. Both required commands were run by the producer; neither is all-green, and acceptance of that red release-state residual is not delegated to this worker. No redundant full sweep was run.
- Source-map checker requires a real candidate commit and remains unavailable; no fabricated Git object. Optional RoboRev remains UNAVAILABLE, zero requests. PR/provider state was not queried under the explicit no-provider scope. Captain exact-file commit approval, subsequent delivery checks, installation and original live closure remain outstanding.

## Necessity and consumer review

Read all 8 upstream changed files and the full local mod delta, including surrounding native consumer/archive/write functions. Six upstream non-test surfaces map to one accepted journey: explicit CLI selection/help; immutable proof/identity under the existing consumer lock; whole-entity write expectation; native archive binding; commit-failure safety; consumer instructions. Two changed test files exercise those boundaries. The local mod removes only false reason-sentinel advice and points to the canonical declaration. Without explicit route the retained baseline is blocked/unknown flag; without archive proof the earlier producer integration fails at the native archive owner. No unmapped dependency, new service, extra consumer, schema, file or speculative obligation was found. Kept comments explain lock/resume and rollback safety, not a second policy home; validation made no product removals. The native command claims in both changed docs were exercised by focused tests and raw original-copy evidence; the rollback failure prevents acceptance of the complete safety claim.

`project_context`: impact `none`; authority root PRODUCT.md, ARCHITECTURE.md and CLAUDE.md; surface repository-local workflow/native upstream delivery, not profile selection, loading, release ownership or plugin catalog. No stale claim in those described boundaries is introduced. Planned check and fresh validation evidence: read the bound context and compare it with all changed consumer/CLI surfaces and native focused outcomes; rollback finding is internal recovery behavior. No context edit is authorized or needed for this slice.
