---
phase: 02-core-cockpit
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - app/server/index.ts
  - app/frontend/index.html
  - app/frontend/app.ts
  - app/frontend/pages/dashboard.ts
  - app/frontend/pages/runs.ts
  - app/frontend/pages/config.ts
  - app/frontend/components/schedule-bar.ts
  - app/frontend/components/bottom-nav.ts
  - app/frontend/components/sidebar.ts
  - app/frontend/components/target-detail.ts
  - app/frontend/components/trigger-dialog.ts
  - app/frontend/components/log-stream.ts
  - app/frontend/components/run-timeline.ts
  - app/frontend/lib/api.ts
  - app/frontend/vendor/preact.module.js
  - app/frontend/vendor/htm.module.js
  - app/frontend/vendor/signals.module.js
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - EXEC-01
  - EXEC-02
  - EXEC-03
  - EXEC-06
  - EXEC-07
  - EXEC-08
  - EXEC-09
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-04

user_setup:
  - service: browser
    why: "Visual UI verification — open http://localhost:3200 and confirm rendering"
    env_vars: []
    dashboard_config:
      - task: "Start the app first: cd app && bun run server/index.ts"
        location: "Terminal"

must_haves:
  truths:
    - "Opening http://localhost:3200 shows master-detail dashboard with sidebar target list and detail panel"
    - "Sidebar lists targets from /api/targets; clicking a target updates the right panel with that target's north star and last run"
    - "Clicking Run or the ellipsis menu opens the trigger modal with Production/Dry-run toggle, custom instructions textarea, self-repair toggle, and Start Run button"
    - "Start Run button is disabled while a run is active; clicking Start Run sends POST /api/runs and closes the modal"
    - "Schedule bar at top shows scheduler state, interval, and next run countdown that ticks every 10 seconds"
    - "Runs page shows run history with status badge, target name, trigger type, duration; filterable by status and target"
    - "Clicking a run navigates to /runs/:id showing phase progress bar and log panel; if run is active, log panel connects SSE and shows real-time parsed phases"
    - "Log stream renders phase headers with symbols (completed=check, running=dot, failed=x) and collapsible tool call rows; Show raw toggle switches to raw text"
    - "Config page shows locked padlock message"
    - "Bottom nav highlights active page in accent color"
    - "Preact vendor files served from /vendor/ with no CDN calls required"
  artifacts:
    - path: "app/frontend/index.html"
      provides: "import map pointing to /vendor/ + root mount point"
      contains: "importmap"
    - path: "app/frontend/app.ts"
      provides: "Hash-based router, bottom nav, page rendering"
      contains: "hashchange"
    - path: "app/frontend/components/log-stream.ts"
      provides: "SSE-connected collapsible phase log with auto-scroll and Show raw toggle"
      contains: "EventSource"
    - path: "app/frontend/components/trigger-dialog.ts"
      provides: "Modal with mode toggle, custom instructions, self-repair toggle"
      contains: "dialog"
    - path: "app/frontend/vendor/preact.module.js"
      provides: "Vendored Preact 10.23.1 ESM build"
    - path: "app/frontend/vendor/htm.module.js"
      provides: "Pre-bound htm/preact ESM (exports named 'html')"
      contains: "html"
    - path: "app/server/index.ts"
      provides: "Static file serving for /vendor/*, /pages/*, /components/*, /*.ts"
      contains: "serveStatic"
  key_links:
    - from: "app/frontend/index.html import map"
      to: "app/frontend/vendor/htm.module.js"
      via: "\"htm/preact\" key in importmap"
      pattern: "htm/preact"
    - from: "app/frontend/components/log-stream.ts"
      to: "GET /api/runs/:id/stream"
      via: "new EventSource('/api/runs/${runId}/stream')"
      pattern: "EventSource"
    - from: "app/frontend/components/trigger-dialog.ts"
      to: "POST /api/runs"
      via: "fetch('/api/runs', { method: 'POST', body: JSON.stringify({...}) })"
      pattern: "fetch.*api/runs"
    - from: "app/frontend/pages/dashboard.ts"
      to: "GET /api/targets"
      via: "fetch('/api/targets') on mount"
      pattern: "api/targets"
    - from: "app/server/index.ts"
      to: "app/frontend/ directory"
      via: "serveStatic from hono/bun"
      pattern: "serveStatic"
---

<objective>
Build the complete Preact+HTM frontend: vendor setup, import maps, app router, dashboard master-detail layout, runs history/detail with live SSE log stream, trigger modal, schedule bar, and config stub. Wire static file serving in the Hono server. Ends with human-verified working dashboard.

Purpose: First visual phase — turns the backend APIs built in Plans 02-01/02-02 into a usable cockpit. Requires human visual verification.
Output: 16 new frontend files + vendor downloads + server/index.ts static serving extension
</objective>

<execution_context>
@/Users/kent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-cockpit/02-CONTEXT.md
@.planning/phases/02-core-cockpit/02-RESEARCH.md
@.planning/phases/02-core-cockpit/02-UI-SPEC.md

<interfaces>
<!-- Key contracts from Plans 02-01/02-02 the executor needs. No codebase exploration needed. -->

API endpoints (all localhost — no auth header needed):
  GET  /api/targets              -> Target[]
  GET  /api/targets/:name        -> Target
  POST /api/runs                 body: { target, mode, custom_prompt?, self_repair? } -> { run_id }  202
  GET  /api/runs                 ?status=&target= -> Run[]
  GET  /api/runs/:id             -> Run and { summary? }
  DELETE /api/runs/:id           -> { ok: boolean }
  GET  /api/runs/:id/stream      -> SSE: event 'log' data ParsedLogEvent; event 'ping' data ''
  GET  /api/schedule             -> ScheduleConfig
  PUT  /api/schedule             body: Partial ScheduleConfig -> ScheduleConfig

Types (from app/shared/types.ts):
  Target { name, type, monitors, watch, respond, indicators, north_star, path? }
  Run { id, target, mode, trigger, status, custom_prompt?, started_at?, completed_at?, duration_seconds?, log_path }
  ParsedLogEvent { type, content?, raw, phase?, tool_name?, agent_name?, is_phase_start? }
  ScheduleConfig { enabled, interval_hours?, self_repair_before }

Design system (locked in 02-UI-SPEC.md):
  --bg: #0d1117 (page background)
  --panel: #161b22 (sidebar, panels, modal, bars, nav)
  --border: #30363d (borders, dividers)
  --text: #c9d1d9 (body text)
  --muted: #8b949e (timestamps, secondary metadata)
  --accent: #58a6ff (active nav, selected target, running phase, links)
  --success: #3fb950 (completed, check mark)
  --error: #f85149 (failed, x mark)
  --warn: #d29922 (queued, timeout)
  --btn-primary: #238636 (Start Run button)
  --btn-secondary: #21262d (ghost buttons)
  --btn-danger: #da3633 (Cancel, Remove buttons)
  sidebar: 240px fixed, bottom nav: 48px height, schedule bar: 48px height
  modal: max-width 480px, border-radius 8px, position fixed overlay

Copywriting contract (exact strings from 02-UI-SPEC.md):
  Primary CTA: "Start Run" / "Run All" / "Dry Run" / "Cancel Run"
  Modal dismiss: "Never mind"
  Empty — no target selected: "Select a target from the sidebar to view details"
  Empty — no targets: "No targets configured. Add a target to get started."
  Empty — no runs: "No runs yet. Trigger a run to see history here."
  Worker offline: "Worker offline — runs cannot be triggered. Restart the server to reconnect."
  SSE disconnected: "Log stream disconnected. [Reconnect]"
  Schedule bar enabled: "Scheduler: every {N}h . Next in {countdown} . Last: {status} {time-ago}"
  Schedule bar disabled: "Scheduler: off . Last: {status} {time-ago}"
  Config stub: "Config editor coming in Phase 3. Edit targets.yaml directly for now."
  Add Target: "+ Add Target"
  Log symbols: dot = running (animated), check = complete, x = failed
  Log raw toggle: "Show raw" / "Show parsed"
  Auto-scroll resume: "Down Resume"

HTM import pattern (CRITICAL — from RESEARCH.md Pattern 1):
  import { html } from 'htm/preact'
  import { useState, useEffect, useRef } from 'preact/hooks'
  import { render } from 'preact'
  Component syntax: html`div class="foo"` (tagged template, NOT JSX)

Pitfall 2 (CRITICAL — pre-bound htm vendor):
  htm.module.js MUST export named 'html'. Use esm.sh/htm@3.1.1/preact NOT esm.sh/htm@3.1.1
  If download fails: verify with: grep "export.*html" vendor/htm.module.js

Bun static serving (from RESEARCH.md Pattern 3):
  import { serveStatic } from 'hono/bun'  -- NOT 'hono/middleware'
  app.use('/vendor/*', serveStatic({ root: frontendDir }))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor files download + import map (index.html) + lib/api.ts + server static serving extension</name>
  <files>
    app/frontend/vendor/preact.module.js,
    app/frontend/vendor/htm.module.js,
    app/frontend/vendor/signals.module.js,
    app/frontend/index.html,
    app/frontend/lib/api.ts,
    app/server/index.ts
  </files>
  <read_first>
    - app/server/index.ts (current file — add serveStatic and root route; maintain auth middleware order)
    - .planning/phases/02-core-cockpit/02-RESEARCH.md §Pattern 1 (import map exact syntax), §Pattern 3 (static serving), §Pitfall 2 (htm pre-bound)
    - .planning/phases/02-core-cockpit/02-UI-SPEC.md §Registry Safety (pinned versions)
  </read_first>
  <action>
**Step 1: Download vendor files.**

Run these Bash commands to create vendor directory and download ESM builds:

  mkdir -p /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app/frontend/vendor
  curl -L "https://esm.sh/preact@10.23.1" -o /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app/frontend/vendor/preact.module.js
  curl -L "https://esm.sh/htm@3.1.1/preact" -o /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app/frontend/vendor/htm.module.js
  curl -L "https://esm.sh/@preact/signals@1.3.0" -o /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app/frontend/vendor/signals.module.js

After download, verify htm exports named 'html' (CRITICAL — if missing, download fails):
  grep -c "export.*html" /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app/frontend/vendor/htm.module.js

If count is 0, the wrong esm.sh URL was used. Try the /standalone variant:
  curl -L "https://esm.sh/htm@3.1.1/preact/standalone" -o /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app/frontend/vendor/htm.module.js

Use the absolute path to kc-nightwatch/app/ directory for all vendor downloads.

**Step 2: Create app/frontend/index.html.**

Content:
  DOCTYPE html declaration
  html lang="en"
  head with charset UTF-8, viewport meta
  title: Nightwatch
  script type="importmap" with:
    "preact": "/vendor/preact.module.js"
    "preact/hooks": "/vendor/preact.module.js"
    "htm/preact": "/vendor/htm.module.js"
    "@preact/signals": "/vendor/signals.module.js"
  style tag with CSS variables and resets:
    --bg: #0d1117
    --panel: #161b22
    --border: #30363d
    --text: #c9d1d9
    --muted: #8b949e
    --accent: #58a6ff
    --success: #3fb950
    --error: #f85149
    --warn: #d29922
    --btn-primary: #238636
    --btn-secondary: #21262d
    --btn-danger: #da3633
    --font-body: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif
    --font-mono: "SF Mono", "Fira Code", "Fira Mono", "Roboto Mono", monospace
    html/body: height 100%, background var(--bg), color var(--text), font-family var(--font-body), font-size 14px
    #app: height 100%, display flex, flex-direction column
    button: cursor pointer, font-family inherit, font-size 14px, border 1px solid var(--border), border-radius 6px, padding 6px 12px
    button:focus: outline 2px solid var(--accent), outline-offset 2px
    button:disabled: opacity 0.5, cursor not-allowed
    a: color var(--accent), text-decoration none
  body:
    div id="app" with text "Loading..."
    script type="module" src="/app.ts"

**Step 3: Create app/frontend/lib/api.ts.**

Typed fetch wrapper with functions:
  - get(path): returns JSON, throws on non-ok
  - post(path, body?): returns JSON, sets Content-Type application/json
  - put(path, body): returns JSON
  - del(path): fire-and-forget DELETE

Exported api object with methods:
  - getTargets(): fetch GET /api/targets, returns Target[]
  - getRuns(filter?): fetch GET /api/runs with ?status and ?target query params, returns Run[]
  - getRun(id): fetch GET /api/runs/:id, returns Run and optional summary
  - triggerRun(body): fetch POST /api/runs with { target, mode, custom_prompt?, self_repair? }, returns { run_id }
  - cancelRun(id): fetch DELETE /api/runs/:id
  - getSchedule(): fetch GET /api/schedule, returns ScheduleConfig
  - updateSchedule(partial): fetch PUT /api/schedule, returns updated ScheduleConfig
  - webhook(body): fetch POST /api/webhook with { target?, mode? }, returns { run_id }

Import types from ../../shared/types.ts using type-only imports.
BASE = '' (same origin).

**Step 4: Extend app/server/index.ts.**

Add import at top:
  import { serveStatic } from 'hono/bun'

Add FRONTEND_ROOT constant after imports:
  const FRONTEND_ROOT = path.join(import.meta.dir, '../frontend')
  (need: import path from 'node:path' if not already imported — check file first)

Add static serving routes AFTER auth middleware but BEFORE app.route('/', healthRoutes):
  app.use('/vendor/*', serveStatic({ root: FRONTEND_ROOT }))
  app.use('/pages/*', serveStatic({ root: FRONTEND_ROOT }))
  app.use('/components/*', serveStatic({ root: FRONTEND_ROOT }))
  app.use('/lib/*', serveStatic({ root: FRONTEND_ROOT }))
  app.use('/app.ts', serveStatic({ root: FRONTEND_ROOT }))
  app.use('/shared/*', serveStatic({ root: path.join(import.meta.dir, '..') }))

Add root route for SPA (AFTER health/api routes):
  app.get('/', async (c) => {
    return c.html(await Bun.file(path.join(FRONTEND_ROOT, 'index.html')).text())
  })
  </action>
  <verify>
    <automated>ls /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app/frontend/vendor/ 2>&1 && grep "serveStatic" /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app/server/index.ts 2>&1 && grep "importmap" /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app/frontend/index.html 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - app/frontend/vendor/ contains preact.module.js, htm.module.js, signals.module.js (non-empty files)
    - grep "export.*html" app/frontend/vendor/htm.module.js returns at least 1 match (pre-bound version confirmed)
    - app/frontend/index.html contains "importmap" and "/vendor/htm.module.js"
    - app/server/index.ts contains "serveStatic" (from hono/bun)
    - app/server/index.ts contains "/vendor/*" static route
    - app/frontend/lib/api.ts exports "api" object with getTargets, getRuns, triggerRun functions
    - bun run --bun tsc --noEmit exits 0 (no TypeScript errors from server changes)
  </acceptance_criteria>
  <done>
    - 3 vendor files exist and are non-empty
    - htm.module.js exports named 'html' (grep confirms)
    - index.html has importmap pointing to /vendor/ files
    - server/index.ts serves /vendor/*, /pages/*, /components/*, /lib/*, /app.ts, /shared/*
    - lib/api.ts has typed fetch wrapper for all API endpoints
  </done>
</task>

<task type="auto">
  <name>Task 2: App router + all page and component files (dashboard, runs, config, schedule-bar, sidebar, target-detail, trigger-dialog, log-stream, run-timeline, bottom-nav)</name>
  <files>
    app/frontend/app.ts,
    app/frontend/pages/dashboard.ts,
    app/frontend/pages/runs.ts,
    app/frontend/pages/config.ts,
    app/frontend/components/schedule-bar.ts,
    app/frontend/components/bottom-nav.ts,
    app/frontend/components/sidebar.ts,
    app/frontend/components/target-detail.ts,
    app/frontend/components/trigger-dialog.ts,
    app/frontend/components/log-stream.ts,
    app/frontend/components/run-timeline.ts
  </files>
  <read_first>
    - app/frontend/lib/api.ts (api object methods — all data fetching must use these)
    - .planning/phases/02-core-cockpit/02-RESEARCH.md §Pattern 1 (HTM component syntax), §Pattern 7 (hash routing), Code Examples section (ScheduleBar, LogStream full implementations)
    - .planning/phases/02-core-cockpit/02-UI-SPEC.md §Component Inventory, §Interaction Contracts (all UI behavior specs), §State Inventory, §Copywriting Contract
    - .planning/phases/02-core-cockpit/02-CONTEXT.md §SSE Log Presentation, §Trigger UX, §Master-Detail Layout, §Run History, §Scheduler
  </read_first>
  <action>
Build all 11 frontend files. Every file uses the same import pattern:
  import { html } from 'htm/preact'
  import { useState, useEffect, useRef } from 'preact/hooks'
  (never JSX, never .jsx extension, always .ts extension)

**app/frontend/app.ts** — root component, hash-based router, bottom nav:
  Imports: html from htm/preact, useState/useEffect from preact/hooks, render from preact
  Imports: Dashboard from ./pages/dashboard.ts, Runs from ./pages/runs.ts, Config from ./pages/config.ts
  Imports: BottomNav from ./components/bottom-nav.ts, ScheduleBar from ./components/schedule-bar.ts
  Imports: api from ./lib/api.ts
  Types: Page = 'dashboard' | 'runs' | 'config'
  getPage(): reads location.hash, returns 'runs' if starts with '#/runs', 'config' if '#/config', else 'dashboard'
  App component:
    state: page (Page), schedule (ScheduleConfig | null)
    useEffect on mount: fetch api.getSchedule(), setSchedule; listen to hashchange
    render: div.app wrapping ScheduleBar, page content (conditional on page), BottomNav
    page content: if 'dashboard' render Dashboard; if 'runs' render Runs; if 'config' render Config
    App div style: display flex, flex-direction column, height 100%, overflow hidden
  render(html App, document.getElementById('app'))

  Layout structure (from 02-UI-SPEC.md Interaction Contracts):
    ScheduleBar: full width, 48px, position relative (not fixed — part of flex flow)
    Main content area: flex 1, overflow hidden, contains page content + bottom nav
    BottomNav: 48px, position fixed bottom 0, full width

**app/frontend/components/schedule-bar.ts** — top bar:
  Props: schedule (ScheduleConfig | null), onToggle (called when user clicks enable/disable)
  state: countdown (string)
  useEffect: if schedule.enabled and interval_hours, compute nextRunAt = last known + intervalMs; setInterval(10_000) to update countdown display "Xh Ym"
  Render structure:
    div.schedule-bar with style: background var(--panel), border-bottom 1px solid var(--border), height 48px, display flex, align-items center, padding 0 16px, gap 16px, font-size 14px
    If schedule null: show "Loading..."
    Else:
      toggle button (ON text if enabled, OFF text if disabled): on click calls onToggle
      if enabled: "every {interval_hours}h" and countdown display
      if disabled: "Scheduler: off"
      "Last run:" placeholder (no run history in schedule endpoint yet — show "—")

**app/frontend/components/bottom-nav.ts** — 3-tab navigation:
  Props: current ('dashboard' | 'runs' | 'config')
  Render: nav.bottom-nav fixed bottom, 48px height, background var(--panel), border-top 1px solid var(--border)
  Three equal a tags: href="#/dashboard" Dashboard, href="#/runs" Runs, href="#/config" Config
  Active tab: color var(--accent), border-top 2px solid var(--accent)
  Inactive tab: color var(--muted)
  Layout: display flex, width 100%, each tab: flex 1, display flex, align-items center, justify-content center

**app/frontend/components/sidebar.ts** — left sidebar target list:
  Props: targets (Target[]), selectedTarget (string | null), onSelect (targetName: string) => void, onRun (targetName: string) => void
  Render:
    aside.sidebar: width 240px, min-width 240px, background var(--panel), border-right 1px solid var(--border), display flex, flex-direction column, overflow hidden
    Scrollable ul: flex 1, overflow-y auto, list-style none, padding 8px 0
    Each li:
      Click calls onSelect(target.name)
      If selectedTarget === target.name: background rgba(88,166,255,0.1), border-left 2px solid var(--accent)
      Else: border-left 2px solid transparent
      Content: div with target.name (font-weight 600, 14px) and type badge (span with 12px label, var(--muted))
      Last run status dot: small circle, color based on last run status (green success, red error, yellow warn, gray if none)
    Bottom: "+ Add Target" button (full width, 8px margin, ghost style, color var(--muted))

**app/frontend/components/target-detail.ts** — right panel:
  Props: target (Target | null), lastRun (Run | null), onRun (mode: 'production' | 'dry-run') => void, onRemove () => void
  If target null: div.empty-state centered "Select a target from the sidebar to view details"
  Else:
    div.target-detail: flex 1, overflow-y auto, padding 16px
    Header: h2 target.name, type badge
    North star section: label "North star", p.north-star target.north_star (italic, var(--muted))
    Action buttons row: "Run" button (primary green), "Dry run" button (secondary), ellipsis button for context menu
    Context menu (shown on ellipsis click, state: showMenu boolean):
      Small dropdown: Run, Dry run, separator, Edit (disabled), Chat (disabled), separator, Remove target
      Edit: button disabled with title/tooltip "Coming in Phase 3" (aria-disabled="true", opacity 0.5)
      Chat: button disabled with title/tooltip "Coming in Phase 3" (aria-disabled="true", opacity 0.5)
      Remove: shows inline confirmation "Remove {name}? This will not delete any files." [Never mind] [Remove]
    Last run summary section: if lastRun, show status badge + duration + trigger type; if null, "No runs yet"
    RunTimeline: if lastRun has phases_completed data, show it; else omit

**app/frontend/components/trigger-dialog.ts** — run trigger modal:
  Props: target (string), isOpen (boolean), onClose () => void, onStart (opts: { mode, custom_prompt?, self_repair }) => void, isDisabled (boolean)
  If not isOpen: return null
  Overlay: position fixed, inset 0, background rgba(0,0,0,0.7), display flex, align-items center, justify-content center, z-index 100
    Click overlay calls onClose
  Modal card: background var(--panel), border 1px solid var(--border), border-radius 8px, max-width 480px, width 90%, padding 24px
    stopPropagation on click
    role="dialog", aria-modal="true"
  Title: h3 "Run {target}" (or "Run All targets" if target === '__all__')
  Section 1 — Mode toggle:
    Two adjacent buttons: "Production" and "Dry Run"
    Active button: background var(--btn-primary), color white, border-color var(--btn-primary)
    Inactive: ghost style
    state: mode ('production' | 'dry-run'), default 'production'
  Section 2 — Custom instructions:
    label "Custom instructions (optional)"
    textarea 4 rows, full width, background var(--bg), color var(--text), border 1px solid var(--border), border-radius 6px, padding 8px, font-family inherit, resize vertical
    placeholder "Optional: additional instructions for this run"
    state: customPrompt (string)
  Section 3 — Self-repair:
    label with checkbox input: "Run self-repair first"
    state: selfRepair (boolean), default true
  Footer: display flex, justify-content flex-end, gap 8px
    "Never mind" button (ghost, calls onClose)
    "Start Run" button (primary green, disabled if isDisabled, calls onStart with collected state, then calls onClose)
  Keyboard: useEffect to listen Escape key -> onClose; focus trap (optional but implement if feasible)

**app/frontend/components/run-timeline.ts** — phase progress bar:
  Props: phases (string[]), activePhase (string | null)
  The 6 standard phase names: 'Phase 0', 'Phase 1', 'Phase 2', 'Phase 3', 'Phase 4', 'Phase 5'
  Render: div.run-timeline, display flex, gap 4px, height 8px
  Each phase segment: flex 1, border-radius 4px
    Color: if phase in phases -> var(--success); if phase === activePhase -> var(--accent) (animated); else var(--border)
  Below: optional label row showing active phase name (12px, var(--muted))

**app/frontend/components/log-stream.ts** — SSE connected collapsible log:
  Props: runId (string), initialEvents (ParsedLogEvent[]), isCompleted (boolean)
  Types: PhaseGroup { phase: string, events: ParsedLogEvent[], status: 'running' | 'complete' | 'failed' }
  state: phases (PhaseGroup[]), collapsed (Set of phase strings), autoScroll (boolean), showRaw (boolean), rawLines (string[]), connected (boolean)
  useEffect on runId: if not isCompleted, open EventSource('/api/runs/{runId}/stream')
    On 'log' event: parse JSON as ParsedLogEvent, call appendToPhases to update phases state
    On 'error' event: set connected=false
    On 'ping' event: no-op (keepalive)
    Return cleanup: es.close()
  appendToPhases(prev, event): if event.is_phase_start, add new PhaseGroup; else append to last group's events; return new array
  Auto-scroll: useRef for bottom div; useEffect on phases: if autoScroll, call scrollIntoView smooth
  onScroll: if user scrolled up more than 50px from bottom, set autoScroll=false
  Render:
    div.log-container: background var(--bg), border-radius 4px, overflow-y auto, max-height (fill available), font-family var(--font-mono), font-size 13px
    Top bar: right-aligned "Show raw"/"Show parsed" toggle button (small, ghost)
    If showRaw: pre.raw-log with rawLines.join newline
    Else: phase groups list
      Each PhaseGroup:
        div.phase-header: padding-left 16px, height 36px, display flex, align-items center, gap 8px, cursor pointer, hover background var(--panel)
          Timestamp (var(--muted)), status symbol (running=animated dot var(--accent), complete=check var(--success), failed=x var(--error)), phase name
          Click -> toggle collapsed set
        If not collapsed: tool call events indented 32px
          Agent dispatch events: "-> Agent: {agent_name} dispatched" in var(--muted)
          Tool call events: "-> Tool: {tool_name}" in var(--muted)
          Text content events: rendered in var(--text)
    If not connected and not isCompleted: "Log stream disconnected. [Reconnect]" button that re-creates EventSource
    Sticky bottom-right resume button (if autoScroll=false): "Down Resume", background var(--panel), color var(--accent)
    div ref=bottomRef at end for auto-scroll target

**app/frontend/pages/dashboard.ts** — master-detail layout:
  Imports: Sidebar, TargetDetail, TriggerDialog, ScheduleBar-level state (schedule from parent)
  state: targets (Target[]), selectedTarget (string | null), lastRuns (Record of string to Run), showDialog (boolean), dialogTarget (string)
  useEffect on mount: fetch api.getTargets(), setTargets; fetch api.getRuns(), map to lastRuns by target name (most recent per target)
  Layout: div.dashboard: display flex, height 100% (fills content area below schedule-bar)
    Sidebar: target list with selectedTarget, onSelect updates selectedTarget, onRun opens dialog
    TargetDetail: selected target data, lastRun from lastRuns map, onRun opens dialog, onRemove confirms then calls api.cancelRun (wrong — Remove is not cancel; just show confirmation, no API in Phase 2 for remove)
  TriggerDialog: shown when showDialog=true, onClose hides it, onStart calls api.triggerRun then hides dialog
  Global Run All button: small button in top-right corner of content area (or in TargetDetail header) that opens dialog with target='__all__'
  Handle active runs: poll api.getRuns() every 5 seconds while a run status='running' or 'queued'; update lastRuns

**app/frontend/pages/runs.ts** — run history list + detail + live view:
  state: runs (Run[]), selectedId (string | null), statusFilter (string), targetFilter (string)
  useEffect on mount: fetch api.getRuns(), setRuns
  Hash sub-route: if location.hash contains '#/runs/', extract id and set selectedId
  Layout:
    If selectedId: show RunDetail view (call api.getRun(selectedId) for detail with summary; render RunTimeline + LogStream)
    Else: show RunList view
  RunList: filter controls (Status select + Target select), then run rows
    Each row: status badge + target name + trigger type + duration (formatted from duration_seconds) + time-ago (from started_at)
    Click row: set location.hash = '#/runs/' + run.id
  RunDetail:
    Back link: "Back to runs" -> set location.hash = '#/runs'
    Run header: status badge, target, mode, trigger, duration
    RunTimeline: phases from run.summary.phases_completed if available
    If run.status === 'running': render LogStream with SSE connected
    Else: render LogStream with isCompleted=true (shows stored events or empty)
    Cancel button: if status === 'running', show Cancel button -> api.cancelRun(id)

**app/frontend/pages/config.ts** — stub:
  Render: div.config-page, display flex, align-items center, justify-content center, height 100%
    div with lock symbol, h2 "Config", p "Config editor coming in Phase 3. Edit targets.yaml directly for now."
    Color: var(--muted) for the message, var(--text) for heading
  </action>
  <verify>
    <automated>cd /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app && bun run --bun tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - app/frontend/app.ts exists and contains "hashchange" (hash router)
    - app/frontend/components/log-stream.ts contains "EventSource" (SSE client)
    - app/frontend/components/trigger-dialog.ts contains "dialog" (accessibility role)
    - app/frontend/pages/dashboard.ts contains "api/targets" (data fetch)
    - app/frontend/pages/runs.ts contains "EventSource" or references LogStream
    - app/frontend/pages/config.ts contains "Phase 3" (stub message)
    - app/frontend/components/schedule-bar.ts contains "setInterval" (countdown timer)
    - app/frontend/components/sidebar.ts contains "Add Target" (add button)
    - All 11 frontend .ts files exist (ls app/frontend/components/ and app/frontend/pages/ confirm)
    - bun run --bun tsc --noEmit exits 0 (TypeScript clean — note: browser globals like EventSource require lib: ["DOM"] in tsconfig, check tsconfig.json before running)
  </acceptance_criteria>
  <done>
    - All 11 frontend component/page files created
    - No TypeScript errors in server-side code (frontend may have browser-global type gaps if DOM lib not in tsconfig — acceptable)
    - HTM tagged template syntax used throughout (no JSX angle brackets in .ts files)
    - api.ts functions called for all data fetching (no raw fetch in components)
    - UI-SPEC colors, copywriting, and interaction contracts followed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — open dashboard and confirm full UI works</name>
  <what-built>
    Complete Nightwatch dashboard frontend:
    - Master-detail layout with target list sidebar and detail panel
    - Trigger modal with Production/Dry-run toggle, custom instructions, self-repair toggle
    - SSE-connected log stream with collapsible phase sections
    - Run history list with status/target filters and detail view
    - Schedule bar with countdown
    - Config stub page
    - Bottom navigation
  </what-built>
  <how-to-verify>
    1. Start the server: cd /path/to/kc-nightwatch/app && bun run server/index.ts
    2. Open http://localhost:3200 in a browser
    3. Verify: Page loads without console errors (check browser DevTools)
    4. Verify: Sidebar shows target list from nightwatch-targets.yaml (or "No targets configured" message if empty)
    5. Click a target — verify right panel shows north star text and action buttons
    6. Click "Run" — verify modal opens with Production/Dry Run toggle, textarea, self-repair checkbox, "Never mind" and "Start Run" buttons
    7. Click "Never mind" — verify modal closes without triggering a run
    8. Navigate to Runs tab (bottom nav) — verify run history list shows or "No runs yet" message
    9. Navigate to Config tab — verify locked padlock message appears
    10. Verify schedule bar at top shows scheduler state
    11. Verify bottom nav highlights active page in blue accent color
    12. Optional: trigger a run and verify SSE log stream updates in real-time
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found (e.g., "console error: htm.module.js missing 'html' export" or "sidebar empty despite targets in yaml")</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete (including checkpoint approval):

  cd /path/to/kc-nightwatch/app
  bun test  (full suite must still pass)
  bun run --bun tsc --noEmit  (server code type-clean)
  ls frontend/vendor/  (3 vendor files)
  grep "serveStatic" server/index.ts  (static serving wired)
  grep "importmap" frontend/index.html  (import map present)
  grep "EventSource" frontend/components/log-stream.ts  (SSE client present)
  grep "hashchange" frontend/app.ts  (router present)

Human checkpoint: Dashboard renders, modal works, runs page shows history, config shows stub, nav works.
</verification>

<success_criteria>
- Dashboard loads at http://localhost:3200 without console errors
- Target sidebar populates from API; clicking a target updates detail panel
- Trigger modal opens/closes; Start Run sends POST /api/runs (202 returned)
- Runs page shows history with filter controls
- Run detail shows phase timeline and log (SSE connects for active runs)
- Config page shows stub message
- Bottom nav highlights active page
- Schedule bar shows scheduler state and countdown
- All existing backend tests pass (no regressions)
- Human verifies and types "approved"
</success_criteria>

<output>
After checkpoint approval, create `.planning/phases/02-core-cockpit/02-03-SUMMARY.md` following the summary template.
</output>
