---
phase: 07-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/frontend/components/chat-drawer.ts
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "chat-drawer.ts does not exist in the codebase"
    - "No file in app/ contains any import or reference to chat-drawer"
    - "bun test passes after deletion (no test regressions)"
    - "Dead phases variable is absent from target-detail.ts (CLEAN-02 — completed in Phase 6)"
    - "Sidebar Add Target button opens AddTargetWizard (CLEAN-03 — completed in Phase 6)"
  artifacts:
    - path: "app/frontend/components/chat-drawer.ts"
      provides: "DELETED — file must NOT exist"
  key_links: []
---

<objective>
Delete the orphaned chat-drawer.ts file and verify all Phase 7 cleanup requirements are satisfied — including the two items (CLEAN-02, CLEAN-03) already completed in Phase 6.

Purpose: Remove dead code and close out the v1.1 milestone cleanly.
Output: One fewer file in the codebase, zero dangling references, all CLEAN-* requirements verified.
</objective>

<execution_context>
@/Users/kent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cleanup/07-CONTEXT.md
@.planning/phases/06-frontend-wiring/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete chat-drawer.ts and verify no dangling references</name>
  <files>app/frontend/components/chat-drawer.ts</files>
  <read_first>
    - app/frontend/components/chat-drawer.ts (the file to delete — confirm it exists)
    - app/frontend/components/chat-panel.ts (confirm it does NOT import chat-drawer)
  </read_first>
  <action>
1. Grep the entire `app/` directory for any string containing `chat-drawer` (imports, comments, references). Expected result: zero matches outside chat-drawer.ts itself.
2. Also check `app/frontend/components/chat-panel.ts` — per CONTEXT.md discretion item, verify it does NOT depend on chat-drawer. (It imports from shared types and api, not from chat-drawer — confirmed in discovery.)
3. Delete `app/frontend/components/chat-drawer.ts`.
4. Run `grep -r "chat-drawer" app/` to confirm zero results after deletion.
5. Run `cd app && bun test` to verify no test regressions.
6. Note: `npx tsc --noEmit` has pre-existing errors unrelated to this change (missing DOM lib types in browser-side Preact+HTM files, mcp-tools arithmetic). Deletion will REDUCE error count by removing chat-drawer.ts's 18 errors. Do NOT attempt to fix pre-existing typecheck errors — they are out of scope.
  </action>
  <verify>
    <automated>cd /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch && test ! -f app/frontend/components/chat-drawer.ts && ! grep -r "chat-drawer" app/ && echo "PASS: chat-drawer.ts deleted, zero references"</automated>
  </verify>
  <acceptance_criteria>
    - `app/frontend/components/chat-drawer.ts` does not exist (file deleted)
    - `grep -r "chat-drawer" app/` returns zero matches
    - `cd app && bun test` exits 0
  </acceptance_criteria>
  <done>chat-drawer.ts is deleted, no file references it, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Verify CLEAN-02 and CLEAN-03 remain complete from Phase 6</name>
  <files></files>
  <read_first>
    - app/frontend/components/target-detail.ts (verify no dead `phases = []` variable)
    - app/frontend/components/sidebar.ts (verify onAddTarget prop exists and is wired)
    - app/frontend/pages/dashboard.ts (verify AddTargetWizard is mounted and onAddTarget is passed to Sidebar)
  </read_first>
  <action>
CLEAN-02 and CLEAN-03 were completed in Phase 6 plan 06-02 task 3 (commit e84382a). This task verifies they remain intact — no re-implementation needed.

1. **CLEAN-02 verification (dead phases variable):**
   - Grep `target-detail.ts` for `phases` as a variable declaration. Expected: zero matches. The dead `const phases = lastRun?.log_path ? [] : []` was removed in Phase 6.
   - Grep for `phases.length` in target-detail.ts. Expected: zero matches.

2. **CLEAN-03 verification (sidebar Add Target button):**
   - Grep `sidebar.ts` for `onAddTarget`. Expected: present in Props interface AND in both button onClick handlers.
   - Grep `dashboard.ts` for `onAddTarget`. Expected: passed as prop to Sidebar component.
   - Grep `dashboard.ts` for `AddTargetWizard`. Expected: imported and mounted.

3. Print verification summary confirming both requirements are still satisfied.
  </action>
  <verify>
    <automated>cd /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch && ! grep -E "const phases|let phases|var phases" app/frontend/components/target-detail.ts && grep -q "onAddTarget" app/frontend/components/sidebar.ts && grep -q "AddTargetWizard" app/frontend/pages/dashboard.ts && echo "PASS: CLEAN-02 and CLEAN-03 verified"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "const phases|let phases|var phases" app/frontend/components/target-detail.ts` returns zero matches (CLEAN-02)
    - `grep "onAddTarget" app/frontend/components/sidebar.ts` returns matches showing the prop is wired (CLEAN-03)
    - `grep "AddTargetWizard" app/frontend/pages/dashboard.ts` returns matches showing wizard is mounted (CLEAN-03)
  </acceptance_criteria>
  <done>CLEAN-02 (dead phases variable removed) and CLEAN-03 (sidebar Add Target wired) confirmed present from Phase 6 — no regressions</done>
</task>

</tasks>

<verification>
All three CLEAN requirements verified:
- CLEAN-01: `chat-drawer.ts` deleted, zero references remain
- CLEAN-02: No dead `phases` variable in `target-detail.ts` (completed Phase 6, verified here)
- CLEAN-03: Sidebar Add Target opens AddTargetWizard (completed Phase 6, verified here)
- `bun test` passes with no regressions
</verification>

<success_criteria>
1. `app/frontend/components/chat-drawer.ts` does not exist
2. `grep -r "chat-drawer" app/` returns empty
3. `bun test` exits 0
4. No `phases` variable declaration in target-detail.ts
5. `onAddTarget` prop wired in sidebar.ts
6. `AddTargetWizard` imported and mounted in dashboard.ts
</success_criteria>

<output>
After completion, create `.planning/phases/07-cleanup/07-01-SUMMARY.md`
</output>
