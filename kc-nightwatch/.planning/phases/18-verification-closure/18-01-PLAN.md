---
phase: 18-verification-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-data-layer-foundations/15-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [VIZ-01, SIG-02, SIG-03]
gap_closure: true

must_haves:
  truths:
    - "Phase 15 has a VERIFICATION.md that formally verifies VIZ-01, SIG-02, and SIG-03"
    - "REQUIREMENTS.md shows VIZ-01, SIG-02, and SIG-03 as checked [x] with status Complete in traceability table"
    - "The verification report cites concrete evidence (file paths, line numbers, test counts) for each requirement"
  artifacts:
    - path: ".planning/phases/15-data-layer-foundations/15-VERIFICATION.md"
      provides: "Formal verification report for Phase 15 orphaned requirements"
      contains: "status: passed"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement checkboxes and traceability"
      contains: "[x] **VIZ-01**"
  key_links:
    - from: "15-VERIFICATION.md"
      to: "v4.0-MILESTONE-AUDIT.md"
      via: "Closes orphaned requirement gaps identified in audit"
      pattern: "VIZ-01.*VERIFIED|SIG-02.*VERIFIED|SIG-03.*VERIFIED"
---

<objective>
Create the missing Phase 15 VERIFICATION.md to formally close 3 orphaned requirements (VIZ-01, SIG-02, SIG-03) and update REQUIREMENTS.md checkboxes.

Purpose: The v4.0 milestone audit found VIZ-01, SIG-02, and SIG-03 flagged as "orphaned" because Phase 15 never produced a VERIFICATION.md. The integration checker confirmed all three are implemented and wired correctly. The UAT (15-UAT.md) confirmed all 6 tests passed with live data. This plan creates the formal verification artifact and updates the requirements tracking document.

Output: 15-VERIFICATION.md in Phase 15 directory, updated REQUIREMENTS.md with [x] checkboxes and "Complete" traceability status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v4.0-MILESTONE-AUDIT.md
@.planning/phases/15-data-layer-foundations/15-01-SUMMARY.md
@.planning/phases/15-data-layer-foundations/15-02-SUMMARY.md
@.planning/phases/15-data-layer-foundations/15-UAT.md
@.planning/phases/16-health-page-enrichment/16-VERIFICATION.md

<interfaces>
<!-- Evidence locations the executor needs for the verification report -->

VIZ-01 evidence (sparkline real data):
- app/server/routes/health-api.ts line 75: `history: cal.history` (replaced fake [0, rate] stub)
- app/shared/types.ts line 183: `history: number[]` in CalibrationData
- app/shared/types.ts line 221: `history: number[]` in HealthIndicatorData
- app/server/services/feedback-store.ts line 63: `function buildHistory(` (per-run bucketing)
- 15-UAT.md Test 6: health API returns real history arrays
- bun test tests/server/health-api.test.ts: 20 pass, 0 fail

SIG-02 evidence (N-gate < 10):
- app/server/services/feedback-store.ts line 151: `current_threshold: null`
- app/server/services/feedback-store.ts line 152: `threshold_null_reason: \`Accumulating data (${total}/10)\``
- app/shared/types.ts line 181: `current_threshold: number | null`
- app/shared/types.ts line 182: `threshold_null_reason?: string`
- 15-UAT.md Test 3: indicators with <10 feedback return null threshold
- bun test tests/server/calibration.test.ts: 10 pass (includes 4 N-gate tests)

SIG-03 evidence (EMA alpha=0.3):
- app/server/services/feedback-store.ts line 56: `const ALPHA = 0.3`
- app/server/services/feedback-store.ts line 160: `emaThreshold = ALPHA * rate + (1 - ALPHA) * emaThreshold`
- app/server/services/feedback-store.ts lines 163-164: `Math.min(0.9, Math.max(0.1, emaThreshold))` (clamping)
- 15-UAT.md Test 2: CalibrationData returns history array and nullable threshold
- bun test tests/server/calibration.test.ts: 10 pass (includes 6 EMA computation tests)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 15 VERIFICATION.md</name>
  <files>.planning/phases/15-data-layer-foundations/15-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/16-health-page-enrichment/16-VERIFICATION.md (format reference — use same structure: frontmatter, observable truths table, required artifacts table, key link verification, behavioral spot-checks, requirements coverage, gaps summary)
    - .planning/phases/15-data-layer-foundations/15-01-PLAN.md (must_haves section at lines 16-42 — the truths and artifacts to verify)
    - .planning/phases/15-data-layer-foundations/15-02-PLAN.md (must_haves section at lines 18-66 — additional truths and artifacts)
    - .planning/phases/15-data-layer-foundations/15-01-SUMMARY.md (requirements-completed field, accomplishments, key decisions)
    - .planning/phases/15-data-layer-foundations/15-02-SUMMARY.md (provides, key-files, accomplishments)
    - .planning/phases/15-data-layer-foundations/15-UAT.md (6 passed UAT tests — live endpoint verification)
    - .planning/v4.0-MILESTONE-AUDIT.md (gaps.requirements section — the 3 orphaned requirements and their evidence)
    - app/server/services/feedback-store.ts (lines 55-170 — EMA computation, buildHistory, N gate, ALPHA constant)
    - app/server/routes/health-api.ts (line 75 — cal.history usage replacing fake stub)
    - app/shared/types.ts (lines 176-200 — CalibrationData, ForgeResultData, SignalPriorityItem types)
  </read_first>
  <action>
Create `.planning/phases/15-data-layer-foundations/15-VERIFICATION.md` following the same structure as 16-VERIFICATION.md.

**Frontmatter:**
```yaml
---
phase: 15-data-layer-foundations
verified: 2026-03-27T00:00:00Z
status: passed
score: 10/10 must-haves verified
re_verification: true
re_verification_reason: "Phase 15 had no VERIFICATION.md — 3 requirements orphaned in v4.0 milestone audit"
gaps: []
human_verification: []
---
```

Note: `re_verification: true` because this is retroactive verification of already-completed work. No human verification needed — all behaviors are verifiable programmatically via tests and grep.

**Phase Goal:** "CalibrationData uses EMA-smoothed thresholds with real per-run history, minimum sample gate, and new types for forge/signals endpoints"

**Observable Truths table (10 items):**

Combine must_haves from both 15-01-PLAN.md and 15-02-PLAN.md:

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | CalibrationData includes a history array of per-run reject rates capped at 30 entries | VERIFIED | `types.ts` line 183: `history: number[]`; `feedback-store.ts` line 59: `const HISTORY_WINDOW = 30`; `feedback-store.ts` uses `slice(-30)` |
| 2 | CalibrationData returns null threshold with explanatory message when feedback count is below 10 | VERIFIED | `feedback-store.ts` lines 151-152: `current_threshold: null, threshold_null_reason: \`Accumulating data...\``; 4 N-gate tests pass in calibration.test.ts |
| 3 | CalibrationData threshold is computed via EMA alpha=0.3 instead of the old linear formula | VERIFIED | `feedback-store.ts` line 56: `const ALPHA = 0.3`; line 160: `emaThreshold = ALPHA * rate + (1 - ALPHA) * emaThreshold`; no occurrences of `0.5 + (rejectRate - 0.5) * 0.5` remain |
| 4 | ForgeResultData and SignalPriorityItem types are exported from shared/types.ts | VERIFIED | `types.ts` line 186: `export interface ForgeResultData`; line 196: `export interface SignalPriorityItem` |
| 5 | GET /api/forge/results returns 200 with forge_result data from nightwatch-self-repair.yaml | VERIFIED | `forge.ts` exports `forgeRoutes`; 6 tests pass in forge.test.ts; UAT Test 4 confirmed live |
| 6 | GET /api/forge/results returns {forge_result: null, stale: true} when YAML file is missing | VERIFIED | `forge.ts` null check at top of handler; forge.test.ts test 1 verifies this case |
| 7 | GET /api/signals/priority returns indicators sorted descending by confidence_weight x (1 - reject_rate) | VERIFIED | `signals.ts` contains `items.sort((a, b) => b.score - a.score)`; 7 tests pass including sort verification |
| 8 | Signals endpoint caps at 30 runs | VERIFIED | `signals.ts` contains `allRuns.slice(0, 30)`; signals.test.ts test 4 verifies cap |
| 9 | Health API per_indicator_rates[].history uses real CalibrationData.history (not fake [0, rate] stub) | VERIFIED | `health-api.ts` line 75: `history: cal.history`; no `[0, Math.round(cal.reject_rate` remains; UAT Test 6 confirmed live |
| 10 | Both new routes are registered in server/index.ts | VERIFIED | `index.ts` lines 24-25: imports; lines 167-168: `app.route('/', forgeRoutes)` and `app.route('/', signalsRoutes)` |

**Required Artifacts table:**

List each artifact from the must_haves of both plans, with path, expected content, status, and line number evidence. Include:
- app/shared/types.ts — CalibrationData with history, nullable threshold; ForgeResultData; SignalPriorityItem
- app/server/services/feedback-store.ts — getCalibrationData() with EMA, N gate, buildHistory
- app/server/routes/forge.ts — GET /api/forge/results
- app/server/routes/signals.ts — GET /api/signals/priority
- app/server/routes/health-api.ts — cal.history replaces stub
- app/server/index.ts — route registration
- app/tests/server/calibration.test.ts — EMA + N gate tests
- app/tests/server/feedback.test.ts — history + threshold tests
- app/tests/server/forge.test.ts — forge endpoint tests
- app/tests/server/signals.test.ts — signals endpoint tests
- app/tests/server/health-api.test.ts — updated mock with history

**Key Link Verification table:**

Verify each key_link from both plan must_haves by grepping for the pattern. Include:
- feedback-store.ts imports CalibrationData from types.ts
- feedback-store.ts reads from yaml-store via readYamlFile
- forge.ts reads self-repair YAML via readYamlFile
- signals.ts calls getCalibrationData() from feedback-store
- signals.ts calls listRuns/getRun from run-store
- index.ts imports and registers forgeRoutes and signalsRoutes
- health-api.ts uses cal.history from getCalibrationData()

**Behavioral Spot-Checks table:**

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| EMA + N gate unit tests | `bun test tests/server/calibration.test.ts` | 10 pass, 0 fail | PASS |
| Feedback integration tests | `bun test tests/server/feedback.test.ts` | 14 pass, 0 fail | PASS |
| Forge endpoint tests | `bun test tests/server/forge.test.ts` | 6 pass, 0 fail | PASS |
| Signals endpoint tests | `bun test tests/server/signals.test.ts` | 7 pass, 0 fail | PASS |
| Health API tests | `bun test tests/server/health-api.test.ts` | 20 pass, 0 fail | PASS |
| Full test suite | `bun test` | 450 pass, 0 fail | PASS |

Run each test command from `cd /Users/kent/Project/kc-claude-workspace/kc-nightwatch/app` to get the actual current pass/fail counts. Use the actual numbers from the test run, not the numbers listed above (those are from the planner's check and may differ slightly from executor context).

**Requirements Coverage table:**

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-----------|-------------|--------|----------|
| VIZ-01 | 15-01, 15-02 | Health page shows per-indicator reject rate trend as sparkline with real historical data | SATISFIED | `health-api.ts:75` uses `cal.history` (real data); `feedback-store.ts` `buildHistory()` produces per-run rates; UAT Test 6 passed |
| SIG-02 | 15-01 | Calibration data is hidden for indicators with fewer than 10 feedback entries | SATISFIED | `feedback-store.ts:151` returns `current_threshold: null` when `total < 10`; `threshold_null_reason` provides human message; UAT Test 3 passed |
| SIG-03 | 15-01 | Calibration threshold uses EMA smoothing (alpha=0.3) instead of raw all-time average | SATISFIED | `feedback-store.ts:56` `ALPHA = 0.3`; line 160 EMA formula; clamped [0.1, 0.9]; 6 EMA tests pass; UAT Test 2 passed |

**Gaps Summary:** "No gaps. All 10 observable truths verified. All artifacts exist with substantive implementations (no stubs). All key links wired. 450 tests pass. UAT confirmed all 6 scenarios with live data."

**Anti-Patterns Found:** "No blocker or warning anti-patterns found."

**Important:** The executor MUST actually run the grep commands and test commands to get live evidence. Do NOT copy numbers from the plan — run `bun test` and `grep` commands, then record the actual results. The line numbers cited above are from the planner's snapshot and may shift if other work has been committed.
  </action>
  <verify>
    <automated>test -f /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/.planning/phases/15-data-layer-foundations/15-VERIFICATION.md && grep -c "VERIFIED" /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/.planning/phases/15-data-layer-foundations/15-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/15-data-layer-foundations/15-VERIFICATION.md exists
    - File contains `status: passed` in frontmatter
    - File contains `re_verification: true` in frontmatter
    - File contains `VIZ-01` with `SATISFIED` in requirements coverage
    - File contains `SIG-02` with `SATISFIED` in requirements coverage
    - File contains `SIG-03` with `SATISFIED` in requirements coverage
    - File contains at least 10 rows with `VERIFIED` in observable truths table
    - File contains `450 pass` or higher test count in behavioral spot-checks (actual count from test run)
    - File contains `gaps: []` in frontmatter
    - File contains evidence line numbers from actual source files (not placeholder values)
  </acceptance_criteria>
  <done>
    15-VERIFICATION.md exists with status: passed. All 3 orphaned requirements (VIZ-01, SIG-02, SIG-03) formally verified as SATISFIED with concrete evidence (file paths, line numbers, test counts from actual runs). 10/10 observable truths verified. No gaps.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md checkboxes and traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — 63 lines; need to update lines 12, 19, 20 checkboxes and lines 45-47 traceability status)
  </read_first>
  <action>
Update `.planning/REQUIREMENTS.md` with the following changes:

**Checkbox updates (3 lines):**

Line 12 (VIZ-01): Change `- [ ] **VIZ-01**:` to `- [x] **VIZ-01**:`
Line 19 (SIG-02): Change `- [ ] **SIG-02**:` to `- [x] **SIG-02**:`
Line 20 (SIG-03): Change `- [ ] **SIG-03**:` to `- [x] **SIG-03**:`

**Traceability table updates (3 rows):**

Line 45 (VIZ-01 row): Change `| VIZ-01 | Phase 15 → **Phase 18** (verification closure) | Pending |` to `| VIZ-01 | Phase 15 (verified in Phase 18) | Complete |`
Line 46 (SIG-02 row): Change `| SIG-02 | Phase 15 → **Phase 18** (verification closure) | Pending |` to `| SIG-02 | Phase 15 (verified in Phase 18) | Complete |`
Line 47 (SIG-03 row): Change `| SIG-03 | Phase 15 → **Phase 18** (verification closure) | Pending |` to `| SIG-03 | Phase 15 (verified in Phase 18) | Complete |`

**Coverage summary update (lines 54-58):**

Change:
```
- Satisfied: 4 (VIZ-02, VIZ-03, FORGE-01, VIZ-02)
- Pending: 3 (VIZ-01, SIG-02, SIG-03 — verification gap)
- Unsatisfied: 1 (SIG-01 — code gap)
```

To:
```
- Satisfied: 6 (VIZ-01, VIZ-02, VIZ-03, SIG-02, SIG-03, FORGE-01)
- Pending: 0
- Unsatisfied: 1 (SIG-01 — code gap, Phase 19)
```

**Traceability updated timestamp (line 62):**

Change `*Traceability updated: 2026-03-27 (gap closure phases 18-19 added)*` to `*Traceability updated: 2026-03-27 (Phase 18 verification closure complete)*`

Do NOT change any other lines. Preserve all other content exactly.
  </action>
  <verify>
    <automated>grep -c "\[x\]" /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md line 12 contains `[x] **VIZ-01**`
    - REQUIREMENTS.md line 19 contains `[x] **SIG-02**`
    - REQUIREMENTS.md line 20 contains `[x] **SIG-03**`
    - REQUIREMENTS.md contains `| VIZ-01 | Phase 15 (verified in Phase 18) | Complete |`
    - REQUIREMENTS.md contains `| SIG-02 | Phase 15 (verified in Phase 18) | Complete |`
    - REQUIREMENTS.md contains `| SIG-03 | Phase 15 (verified in Phase 18) | Complete |`
    - REQUIREMENTS.md contains `Satisfied: 6`
    - REQUIREMENTS.md contains `Pending: 0`
    - REQUIREMENTS.md contains `Unsatisfied: 1 (SIG-01`
    - grep -c "\[x\]" returns 6 (VIZ-01, VIZ-02, VIZ-03, SIG-02, SIG-03, FORGE-01)
    - SIG-01 line is unchanged (still `[ ]`)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md has 6 of 7 requirements checked as [x]. VIZ-01, SIG-02, SIG-03 updated from Pending to Complete in traceability table. Only SIG-01 remains unchecked (Phase 19 scope). Coverage summary reflects 6 satisfied, 0 pending, 1 unsatisfied.
  </done>
</task>

</tasks>

<verification>
1. `test -f .planning/phases/15-data-layer-foundations/15-VERIFICATION.md` — file exists
2. `grep "status: passed" .planning/phases/15-data-layer-foundations/15-VERIFICATION.md` — verification passed
3. `grep -c "SATISFIED" .planning/phases/15-data-layer-foundations/15-VERIFICATION.md` — returns 3 (VIZ-01, SIG-02, SIG-03)
4. `grep -c "\[x\]" .planning/REQUIREMENTS.md` — returns 6
5. `grep "\[ \]" .planning/REQUIREMENTS.md` — returns only SIG-01 line
6. `grep "Satisfied: 6" .planning/REQUIREMENTS.md` — found
</verification>

<success_criteria>
- Phase 15 has a VERIFICATION.md with status: passed and all 3 requirements (VIZ-01, SIG-02, SIG-03) marked SATISFIED
- REQUIREMENTS.md reflects 6/7 satisfied with only SIG-01 remaining
- The v4.0 milestone audit's "orphaned requirements" gap for Phase 15 is formally closed
- No code changes were needed (verification-only phase)
</success_criteria>

<output>
After completion, create `.planning/phases/18-verification-closure/18-01-SUMMARY.md`
</output>
