---
phase: 11-frontend-outcomes-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/server/routes/outcomes.ts
  - app/server/index.ts
  - app/frontend/lib/api.ts
  - app/frontend/pages/outcomes.ts
  - app/frontend/components/bottom-nav.ts
  - app/frontend/app.ts
  - app/tests/server/outcomes-api.test.ts
autonomous: true
requirements: [OUT-02, OUT-04, UI-01]

must_haves:
  truths:
    - "Visiting #/outcomes shows a filterable list of all NW-created PRs and Linear issues"
    - "Selecting an outcome in the list shows its full detail panel with clickable URL"
    - "Clicking Check status on an outcome detail panel returns cached status"
    - "The bottom nav has 5 tabs in order: Dashboard, Runs, Outcomes, Health, Config"
    - "The black line between content area and bottom nav is gone"
  artifacts:
    - path: "app/server/routes/outcomes.ts"
      provides: "GET /api/outcomes and GET /api/outcomes/:id/status endpoints"
      exports: ["outcomesRoutes"]
    - path: "app/frontend/pages/outcomes.ts"
      provides: "Outcomes page with list-detail split panel"
      exports: ["Outcomes"]
    - path: "app/tests/server/outcomes-api.test.ts"
      provides: "Route-level tests for outcomes API"
  key_links:
    - from: "app/frontend/pages/outcomes.ts"
      to: "/api/outcomes"
      via: "api.getOutcomes() in usePoll"
      pattern: "api\\.getOutcomes"
    - from: "app/server/routes/outcomes.ts"
      to: "outcome-store.ts"
      via: "queryOutcomes and readOutcomes imports"
      pattern: "queryOutcomes|readOutcomes"
    - from: "app/frontend/app.ts"
      to: "app/frontend/pages/outcomes.ts"
      via: "import and hash router case"
      pattern: "page === 'outcomes'"
    - from: "app/server/index.ts"
      to: "app/server/routes/outcomes.ts"
      via: "app.route registration"
      pattern: "outcomesRoutes"
---

<objective>
Outcomes page end-to-end: server API routes, frontend API client, new Outcomes page with split-panel layout, bottom nav 5th tab, app router extension, and nav gap fix.

Purpose: Delivers the core new surface (OUT-02) — a dedicated page aggregating all NW-created PRs and Linear issues with filtering and detail view. Also fixes the long-standing nav gap visual bug (UI-01) and wires the status endpoint for outcome tracking (OUT-04).

Output: Working Outcomes page accessible via #/outcomes with full filter+detail interaction, 5-tab bottom nav, and clean nav border.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-frontend-outcomes-ui-polish/11-CONTEXT.md
@.planning/phases/11-frontend-outcomes-ui-polish/11-RESEARCH.md
@.planning/phases/11-frontend-outcomes-ui-polish/11-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/shared/types.ts:
```typescript
export interface OutcomeRecord {
  id: string
  type: 'pr' | 'linear_issue'
  target: string
  signal_id: string
  run_id: string
  url: string
  branch?: string
  status: 'open' | 'merged' | 'closed' | 'completed' | 'cancelled'
  created_at: string
}
```

From app/server/services/outcome-store.ts:
```typescript
export const OUTCOMES_YAML_PATH: string
export async function readOutcomes(outcomesPath?: string): Promise<OutcomeRecord[]>
export async function appendOutcome(record: OutcomeRecord, outcomesPath?: string): Promise<void>
export async function queryOutcomes(
  filter?: { target?: string; type?: string; status?: string; since?: string },
  outcomesPath?: string,
): Promise<OutcomeRecord[]>
```

From app/frontend/lib/api.ts (existing pattern):
```typescript
async function get<T>(path: string): Promise<T>
export const api = { getRuns(...), getWorkerState(), ... }
```

From app/frontend/app.ts:
```typescript
type Page = 'dashboard' | 'runs' | 'health' | 'config'  // Must add 'outcomes'
function getPage(): Page  // Must add #/outcomes case
```

From app/frontend/components/bottom-nav.ts:
```typescript
type Page = 'dashboard' | 'runs' | 'health' | 'config'  // Must add 'outcomes'
// Tab order: Dashboard, Runs, Health, Config → must become Dashboard, Runs, Outcomes, Health, Config
```

From app/frontend/lib/use-poll.ts:
```typescript
export function usePoll(callback: () => void, intervalMs: number, active: boolean): void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server outcomes API routes + tests</name>
  <files>app/server/routes/outcomes.ts, app/server/index.ts, app/tests/server/outcomes-api.test.ts</files>
  <read_first>
    - app/server/routes/api.ts (route pattern reference — Hono router, query param extraction)
    - app/server/index.ts (route registration pattern — app.route('/', X))
    - app/server/services/outcome-store.ts (queryOutcomes and readOutcomes signatures)
    - app/shared/types.ts (OutcomeRecord interface)
    - app/tests/server/outcome-store.test.ts (test pattern reference — temp dir, bun:test imports)
  </read_first>
  <behavior>
    - Test: GET /api/outcomes with no filters returns all outcomes from queryOutcomes
    - Test: GET /api/outcomes?target=foo filters by target
    - Test: GET /api/outcomes?type=pr&status=open applies both filters
    - Test: GET /api/outcomes/:id/status returns { status: 'open' } for existing record
    - Test: GET /api/outcomes/:id/status returns 404 for non-existent id
  </behavior>
  <action>
    Create `app/server/routes/outcomes.ts`:
    - Import `Hono` from 'hono', import `queryOutcomes`, `readOutcomes` from '../services/outcome-store.ts'
    - Export `const outcomesRoutes = new Hono()`
    - GET `/api/outcomes` route: extract `c.req.query('target')`, `c.req.query('type')`, `c.req.query('status')` query params, call `queryOutcomes({ target, type, status })` (converting null/empty to undefined), return `c.json(records)` (per D-02 filter support)
    - GET `/api/outcomes/:id/status` route: extract `c.req.param('id')`, call `readOutcomes()`, find record by id, if not found return `c.json({ error: 'not found' }, 404)`, if found return `c.json({ status: record.status })` — cached status only, no live gh call (per D-13, D-14, OUT-04 cached approach)

    Register in `app/server/index.ts`:
    - Add `import { outcomesRoutes } from './routes/outcomes.ts'` after the existing mcpRoutes import (line 21)
    - Add `app.route('/', outcomesRoutes)` after `app.route('/', healthApiRoutes)` (after line 162)

    Create `app/tests/server/outcomes-api.test.ts`:
    - Use `bun:test` (describe, it, expect, beforeEach, afterEach)
    - Use temp dir pattern from outcome-store.test.ts: `mkdtemp(join(tmpdir(), 'nw-outcomes-api-'))` in beforeEach, `rm(tmpDir, { recursive: true })` in afterEach
    - For each test: write test YAML with writeYamlFile to temp path, then call the route handler functions imported from outcomes.ts. Since Hono routes need a full app context, test by creating a minimal Hono app in the test, mounting outcomesRoutes, and calling `app.request('/api/outcomes?...')` to test the response.
    - Mock the outcome-store module: use `import { mock } from 'bun:test'` to mock `../../server/services/outcome-store.ts` exports (queryOutcomes returns test data, readOutcomes returns test data). This avoids file I/O in route tests.
  </action>
  <verify>
    <automated>cd /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app && bun test tests/server/outcomes-api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - app/server/routes/outcomes.ts exists and contains `export const outcomesRoutes`
    - app/server/routes/outcomes.ts contains `outcomesRoutes.get('/api/outcomes'`
    - app/server/routes/outcomes.ts contains `outcomesRoutes.get('/api/outcomes/:id/status'`
    - app/server/routes/outcomes.ts contains `queryOutcomes` import
    - app/server/routes/outcomes.ts contains `readOutcomes` import
    - app/server/index.ts contains `import { outcomesRoutes } from './routes/outcomes.ts'`
    - app/server/index.ts contains `app.route('/', outcomesRoutes)`
    - app/tests/server/outcomes-api.test.ts exits 0 with all tests passing
    - GET /api/outcomes route returns JSON array
    - GET /api/outcomes/:id/status returns `{ status: ... }` or 404
  </acceptance_criteria>
  <done>Both outcomes API endpoints are functional with route tests passing. Server registers the new routes.</done>
</task>

<task type="auto">
  <name>Task 2: Outcomes page + bottom nav + app router + nav gap fix</name>
  <files>app/frontend/pages/outcomes.ts, app/frontend/components/bottom-nav.ts, app/frontend/app.ts, app/frontend/lib/api.ts</files>
  <read_first>
    - app/frontend/pages/runs.ts (BLUEPRINT — exact structural pattern for list-detail split panel, filter bar, usePoll usage)
    - app/frontend/components/bottom-nav.ts (current 4-tab implementation, Page type definition)
    - app/frontend/app.ts (current router, Page type definition, line 123 nav gap bug)
    - app/frontend/lib/api.ts (current API client, get/post/put pattern)
    - app/frontend/lib/use-poll.ts (usePoll hook signature)
    - app/frontend/lib/use-toast.ts (showToast signature)
    - app/frontend/index.html (CSS variables reference)
    - app/shared/types.ts (OutcomeRecord type)
    - .planning/phases/11-frontend-outcomes-ui-polish/11-UI-SPEC.md (component contracts)
  </read_first>
  <action>
    **1. Extend api.ts** (add after the getHealth method, before closing brace of `api` object):
    ```typescript
    // Outcomes
    getOutcomes(filter?: { target?: string; type?: string; status?: string }): Promise<OutcomeRecord[]> {
      const params = new URLSearchParams()
      if (filter?.target) params.set('target', filter.target)
      if (filter?.type) params.set('type', filter.type)
      if (filter?.status) params.set('status', filter.status)
      const qs = params.toString()
      return get<OutcomeRecord[]>(`/api/outcomes${qs ? `?${qs}` : ''}`)
    },

    getOutcomeStatus(id: string): Promise<{ status: OutcomeRecord['status'] }> {
      return get<{ status: OutcomeRecord['status'] }>(`/api/outcomes/${id}/status`)
    },
    ```
    Add `OutcomeRecord` to the import from `../../shared/types.ts`.

    **2. Create app/frontend/pages/outcomes.ts** (per D-01, D-02, D-03, D-04, D-14):
    - Import: `html` from htm/preact, `useState`, `useEffect`, `useCallback` from preact/hooks, `OutcomeRecord` type, `api`, `usePoll`, `showToast`
    - Export function `Outcomes()`
    - State: `outcomes: OutcomeRecord[]` (init []), `selectedId: string | null` (init null), `targetFilter: string` (''), `typeFilter: string` (''), `statusFilter: string` (''), `checking: boolean` (false)
    - `loadOutcomes = useCallback(() => { api.getOutcomes().then(setOutcomes).catch(console.error) }, [])`
    - `useEffect(() => { loadOutcomes() }, [])`
    - `usePoll(loadOutcomes, 60_000, true)` — always poll (outcomes change async, per D-14)
    - Filter logic: `const filtered = outcomes.filter(o => { if (targetFilter && o.target !== targetFilter) return false; if (typeFilter && o.type !== typeFilter) return false; if (statusFilter && o.status !== statusFilter) return false; return true })`
    - Unique targets: `[...new Set(outcomes.map(o => o.target))]`
    - Selected outcome: `const selectedOutcome = selectedId ? outcomes.find(o => o.id === selectedId) ?? null : null`
    - Status dot color mapping function: open=var(--accent), merged=var(--success), completed=var(--success), closed=var(--error), cancelled=var(--muted)
    - Type badge text: pr="PR", linear_issue="Issue"
    - Summary text: since OutcomeRecord has NO `summary` field, display `type + " \u00b7 " + target` (e.g. "PR \u00b7 e2e-pipeline") as the list item text

    **Detail panel view** (when selectedId && selectedOutcome):
    - Back link: `<a href="#/outcomes">Back to outcomes</a>` styled same as runs.ts back link
    - Fields card (same card pattern as target-detail.ts North star): background var(--panel), border 1px solid var(--border), border-radius 6px, padding 12px
    - Field rows: Target, Type (with type badge), Status (with colored status dot + text), Created (created_at), Signal ID (monospace 12px), Run ID (monospace 12px)
    - URL: `<a href={url} target="_blank" rel="noopener noreferrer" style="color:var(--accent);font-size:14px;">${url}</a>`
    - Below URL: label "View on GitHub" for type=pr, "View on Linear" for type=linear_issue (per UI-SPEC copywriting)
    - Check status button: padding 3px 8px, font-size 12px, border 1px solid var(--accent), color var(--accent), background transparent, label "Check status" (or "Checking..." when loading). onClick: set checking=true, call `api.getOutcomeStatus(selectedOutcome.id).then(res => { update local status in outcomes array; setChecking(false) }).catch(() => { showToast('Status check failed', 'error'); setChecking(false) })`

    **List view** (no selection):
    - Outer: `height:100%;display:flex;flex-direction:column;overflow:hidden`
    - Filter bar: `padding:12px 16px;border-bottom:1px solid var(--border);display:flex;gap:12px;flex-shrink:0` with 3 selects (per D-02):
      - Target: "All targets" default + unique targets
      - Type: "All types" | "pr" | "linear_issue"
      - Status: "All statuses" | "open" | "merged" | "closed" | "completed" | "cancelled"
    - List: `flex:1;overflow-y:auto`
    - Empty state (no outcomes): centered div "No outcomes recorded yet" heading + "Outcomes appear here after a run creates PRs or Linear issues." body (per UI-SPEC copywriting)
    - Empty state (filtered, no match): "No outcomes match the current filters."
    - Each list item: `padding:12px 16px;border-bottom:1px solid var(--border);cursor:pointer;display:flex;align-items:center;gap:8px` with hover background var(--panel)
    - Item content: type badge (11px, padding 2px 6px, border-radius 4px, bg var(--btn-secondary), color var(--muted)) + target name (14px) + summary text (12px muted, flex:1, truncated 60 chars with text-overflow:ellipsis) | status dot (8px circle with status color) + created date (12px muted, using timeAgo helper)

    **3. Update bottom-nav.ts** (per D-18, D-19):
    - Change `type Page = 'dashboard' | 'runs' | 'health' | 'config'` to `type Page = 'dashboard' | 'runs' | 'outcomes' | 'health' | 'config'`
    - Add Outcomes tab between Runs and Health in the nav: `<a href="#/outcomes" style="${tabStyle('outcomes')}">Outcomes</a>` — insert between the Runs and Health links

    **4. Update app.ts** (per D-17, D-18):
    - Change `type Page` (line 16) to `type Page = 'dashboard' | 'runs' | 'outcomes' | 'health' | 'config'`
    - Add to `getPage()` (between runs and health checks): `if (hash.startsWith('#/outcomes')) return 'outcomes'`
    - Add import: `import { Outcomes } from './pages/outcomes.ts'`
    - Add render branch (line 125, after runs): `${page === 'outcomes' && html\`<$\{Outcomes} />\`}`
    - **Nav gap fix** (D-17, UI-01): line 123, change `padding-bottom:48px` to `margin-bottom:48px` in the middle div style
  </action>
  <verify>
    <automated>cd /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app && bun test</automated>
  </verify>
  <acceptance_criteria>
    - app/frontend/pages/outcomes.ts exists and contains `export function Outcomes`
    - app/frontend/pages/outcomes.ts contains `usePoll(loadOutcomes, 60_000, true)`
    - app/frontend/pages/outcomes.ts contains `api.getOutcomes`
    - app/frontend/pages/outcomes.ts contains `api.getOutcomeStatus`
    - app/frontend/pages/outcomes.ts contains `No outcomes recorded yet`
    - app/frontend/pages/outcomes.ts contains `No outcomes match the current filters`
    - app/frontend/pages/outcomes.ts contains `Check status`
    - app/frontend/pages/outcomes.ts contains `target="_blank"`
    - app/frontend/components/bottom-nav.ts contains `'outcomes'` in Page type
    - app/frontend/components/bottom-nav.ts contains `href="#/outcomes"` and text `Outcomes`
    - app/frontend/components/bottom-nav.ts tab order is: Dashboard, Runs, Outcomes, Health, Config
    - app/frontend/app.ts contains `'outcomes'` in Page type
    - app/frontend/app.ts contains `#/outcomes` in getPage()
    - app/frontend/app.ts contains `import { Outcomes }` from outcomes.ts
    - app/frontend/app.ts contains `page === 'outcomes'` render branch
    - app/frontend/app.ts line with middle div uses `margin-bottom:48px` (NOT padding-bottom)
    - app/frontend/lib/api.ts contains `getOutcomes` method
    - app/frontend/lib/api.ts contains `getOutcomeStatus` method
    - app/frontend/lib/api.ts imports `OutcomeRecord` type
    - Full test suite passes (bun test)
  </acceptance_criteria>
  <done>Outcomes page is accessible via #/outcomes with filter bar (target/type/status), list items with type badge + status dot + date, detail panel with all fields + clickable URL + check status button. Bottom nav shows 5 tabs in correct order. Nav gap black line is gone.</done>
</task>

</tasks>

<verification>
1. `cd /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app && bun test` — full suite green
2. `cd /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app && bun test tests/server/outcomes-api.test.ts` — outcomes route tests green
3. `grep -n "outcomesRoutes" app/server/index.ts` — route registered
4. `grep -n "margin-bottom:48px" app/frontend/app.ts` — nav gap fixed
5. `grep -n "'outcomes'" app/frontend/components/bottom-nav.ts` — 5th tab present
</verification>

<success_criteria>
- Outcomes page renders at #/outcomes with list-detail split panel
- 3 filter dropdowns work (target, type, status)
- Detail panel shows all OutcomeRecord fields with clickable URL
- Check status button calls GET /api/outcomes/:id/status and updates display
- Bottom nav has 5 tabs: Dashboard, Runs, Outcomes, Health, Config
- No black line between content area and bottom nav
- All existing tests remain green
- New outcomes API route tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-frontend-outcomes-ui-polish/11-01-SUMMARY.md`
</output>
