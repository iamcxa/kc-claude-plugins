---
phase: 14-extended-feedback
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - skills/kc-nightwatch/SKILL.md
autonomous: true
requirements: [EXTFEED-01]

must_haves:
  truths:
    - "After nightwatch posts to Slack (Step 5.4), the improvement-log entry for that run includes a slack_url field with the message URL"
    - "On the next run, Phase 0 reads the previous run's slack_url from improvement-log and collects reactions via slack_read_thread MCP"
    - "Reactions are mapped to verdicts: thumbsup=accepted, thumbsdown=rejected, thinking_face=uncertain (D-02)"
    - "Each mapped reaction produces a FeedbackEntry with source 'slack_reaction' appended to feedback.yaml"
    - "When slack_url is absent (silent night, Slack delivery failure), Slack reaction collection is skipped silently without error"
    - "Slack MCP failures never block Phase 1+ execution"
  artifacts:
    - path: "skills/kc-nightwatch/SKILL.md"
      provides: "Step 0.4.5 (Slack reaction collection) + Step 5.2 slack_url field + Step 5.4 slack_url capture"
      contains: "slack_reaction"
  key_links:
    - from: "SKILL.md Step 5.4 (Slack posting)"
      to: "SKILL.md Step 5.2 (improvement-log)"
      via: "slack_url field written to improvement-log after successful Slack send"
      pattern: "slack_url"
    - from: "SKILL.md Step 0.4.5 (Slack reaction collection)"
      to: "improvement-log.md"
      via: "reads slack_url from previous run entry"
      pattern: "slack_url"
    - from: "SKILL.md Step 0.4.5"
      to: "nightwatch-feedback.yaml"
      via: "appendFeedback with source slack_reaction"
      pattern: "slack_reaction"
---

<objective>
Add Slack reaction collection to the nightwatch skill — store the Slack message URL after posting (Phase 5), then collect reactions from it on the next run (Phase 0).

Purpose: EXTFEED-01 requires that Slack reactions on nightwatch reports are parsed and appear as feedback entries. The type foundation (slack_reaction source, slack_feedback store key) is built in Plan 14-01. This plan adds the skill-layer collection logic: (1) capturing the Slack message URL at post time, and (2) reading reactions and writing FeedbackEntries on the next run.

Output: Updated SKILL.md with Step 0.4.5 (Slack reaction collection) and updated Steps 5.2/5.4 (slack_url capture and storage).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-extended-feedback/14-CONTEXT.md
@.planning/phases/14-extended-feedback/14-RESEARCH.md
@.planning/phases/14-extended-feedback/14-01-SUMMARY.md

@skills/kc-nightwatch/SKILL.md

<interfaces>
<!-- Key types and contracts. After Plan 01, these will be available. -->

From app/shared/types.ts (after Plan 01):
```typescript
export interface FeedbackEntry {
  signal_id: string
  target: string
  run_id: string
  verdict: 'accepted' | 'rejected' | 'uncertain'
  reason?: string
  source: 'user' | 'pr_status' | 'linear_status' | 'slack_reaction' | 'pr_review'
  submitted_at: string
}
```

Improvement-log format (from SKILL.md Step 5.2, lines 909-920):
```markdown
### {plugin_name}
- signal: {signal_id}
  action: quick-fix | proposal | linear-issue | e2e-flow | alert | auto-fix | skipped
  branch: {branch_name}
  pr: {PR URL or "local branch"}
  issue: {issue identifier}
  files_changed: [{list}]
  reason: "{if skipped, why}"
```

Feedback YAML path: `~/.claude/kc-plugins-config/nightwatch-feedback.yaml`
Improvement-log path: `~/.claude/kc-plugins-config/nightwatch-improvement-log.md`
Channels config: `~/.claude/kc-plugins-config/channels.yaml`

Slack MCP tools available in session:
- `slack_read_thread` — reads a thread/message and its reactions
- `slack_send_message` — sends a message (returns message URL/ts)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add slack_url capture to Step 5.2 and Step 5.4</name>
  <files>skills/kc-nightwatch/SKILL.md</files>
  <read_first>
    - skills/kc-nightwatch/SKILL.md (lines 878-1077 — Phase 5: Output, covering Steps 5.1-5.4)
  </read_first>
  <action>
    **Two modifications to SKILL.md Phase 5 (per D-11):**

    **Modification A — Step 5.4 (Slack Morning Report), after the delivery strategy section (around line 1077):**

    After the Slack message is successfully sent (via MCP or webhook), add instructions to capture the message URL. Insert the following BEFORE the "Silent night report" section:

    ```markdown
    **Capture Slack message URL for feedback collection:**

    After successful Slack delivery (MCP or webhook):
    1. If sent via `slack_send_message` MCP — the response includes the message URL or `ts` value. Construct the full URL: `https://{workspace}.slack.com/archives/{channel_id}/p{ts_without_dot}`
    2. If sent via webhook — webhook responses do not return message metadata. Set `slack_url: null` (reaction collection will be skipped next run).
    3. If delivery failed — set `slack_url: null`.

    Store the `slack_url` value for use in Step 5.2 (improvement-log update).
    ```

    **Modification B — Step 5.2 (Update Improvement Log), in the improvement-log entry format (around lines 909-920):**

    Add `slack_url` to the per-run entry format. Update the format block to include:

    ```markdown
    ## {YYYY-MM-DD}

    slack_url: {full Slack message URL or null}

    ### {plugin_name}
    - signal: {signal_id}
      action: quick-fix | proposal | linear-issue | e2e-flow | alert | auto-fix (Phase 1) | skipped
      branch: {branch_name}
      pr: {PR URL or "local branch"}
      issue: {issue identifier}
      files_changed: [{list}]
      reason: "{if skipped, why}"
    ```

    Add a note after the format block:
    ```markdown
    **slack_url field:** Written after Step 5.4 Slack delivery. Contains the full Slack message URL for reaction collection on the next run (see Step 0.4.5). Value is `null` when Slack delivery fails or uses webhook fallback (no URL returned). This field is at the run-date level (not per-target) because one Slack message covers all targets.
    ```

    **Important:** Place `slack_url` at the run-date level (under `## {YYYY-MM-DD}`), NOT per-target. There is one Slack morning report per run, not per target.
  </action>
  <verify>
    <automated>grep -c "slack_url" /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/skills/kc-nightwatch/SKILL.md</automated>
  </verify>
  <done>Step 5.4 instructs the skill to capture the Slack message URL after sending. Step 5.2 improvement-log format includes the slack_url field at the run-date level. Both null and URL cases are documented.</done>
</task>

<task type="auto">
  <name>Task 2: Add Step 0.4.5 — Slack Reaction Collection</name>
  <files>skills/kc-nightwatch/SKILL.md</files>
  <read_first>
    - skills/kc-nightwatch/SKILL.md (lines 298-349 — Step 0.4 Scan Feedback from Previous Runs, the insertion point)
    - skills/kc-nightwatch/SKILL.md (lines 878-920 — Phase 5 Step 5.2, to understand improvement-log format with new slack_url field from Task 1)
  </read_first>
  <action>
    **Add a new Step 0.4.5 after the existing Step 0.4 (line 349) and before Phase 0.5 (line 350).**

    Insert the following section into SKILL.md:

    ```markdown
    ### Step 0.4.5: Collect Slack Reaction Feedback

    **Purpose:** Read reactions on the previous run's Slack morning report and convert them to feedback entries. This implements EXTFEED-01 (D-09, D-12).

    **Prerequisites:** Step 0.1 must have loaded the improvement-log. Step 0.4 must have completed (PR/Linear feedback scanned first).

    **Step 1 — Find previous Slack message URL:**

    Read `~/.claude/kc-plugins-config/nightwatch-improvement-log.md` and find the most recent run entry that has a `slack_url` field with a non-null value.

    - If no `slack_url` found in any recent entry → skip silently. This is expected for silent nights or when Slack delivery failed. Do NOT log a warning.
    - If `slack_url: null` → skip silently (same reason).

    **Step 2 — Read reactions via Slack MCP:**

    Call `slack_read_thread` with the Slack message URL from Step 1.

    If the MCP tool is not available in this session → log `[WARN] Slack MCP unavailable — skipping reaction feedback collection` and continue to Phase 0.5.

    If the MCP call fails (timeout, auth error, channel not found) → log `[WARN] Slack reaction collection failed: {error} — skipping` and continue to Phase 0.5. Never block Phase 1+.

    **Step 3 — Map reactions to verdicts (per D-02):**

    For each reaction on the message:

    | Reaction | Emoji aliases | Verdict |
    |----------|---------------|---------|
    | thumbsup | `+1`, `thumbsup` | `accepted` |
    | thumbsdown | `-1`, `thumbsdown` | `rejected` |
    | thinking_face | `thinking_face`, `thinking` | `uncertain` |
    | Any other reaction | — | Skip (ignore) |

    **Dedup rule:** If the same user reacted with multiple mapped reactions, take the strongest signal: `rejected` > `uncertain` > `accepted`. Rationale: a thumbsdown overrides a thumbsup from the same person.

    **Step 4 — Write FeedbackEntries:**

    For each mapped reaction (after dedup), append a FeedbackEntry to `~/.claude/kc-plugins-config/nightwatch-feedback.yaml`:

    ```yaml
    - signal_id: "{from improvement-log — use the first signal_id in that run's entries, or 'run-{date}' if no signals}"
      target: "{target name from improvement-log entry, or 'nightwatch-report' for run-level reactions}"
      run_id: "{run date from improvement-log, e.g., 2026-03-24}"
      verdict: "{mapped verdict}"
      reason: "Slack reaction: {emoji_name} by {user}"
      source: "slack_reaction"
      submitted_at: "{current ISO 8601 timestamp}"
    ```

    Append under the `slack_feedback` key in the feedback YAML (this key is created by Plan 14-01's FeedbackStore routing).

    **Signal_id correlation:** The Slack morning report covers all targets in a single message. For per-signal correlation:
    - If the improvement-log entry for that run has exactly 1 signal → use that signal_id
    - If the improvement-log entry has multiple signals → use `run-{date}` as a synthetic signal_id (the reaction is on the whole report, not a specific signal)
    - The dashboard will display these as run-level feedback

    **Step 5 — Log results:**

    ```
    [PHASE 0] Slack reactions collected: {count} entries from {emoji_count} reactions ({accepted_count} accepted, {rejected_count} rejected, {uncertain_count} uncertain)
    ```

    If zero mapped reactions found → log `[PHASE 0] Slack reactions: none found on previous report` (info, not warning).
    ```

    **Placement:** This step goes between Step 0.4 (line 349, end of "Store feedback in improvement-log") and Phase 0.5 (line 350, "Indicator Baseline Measurement"). The step number 0.4.5 is chosen to sit logically after existing PR/Linear feedback scanning (0.4) and before baseline measurement (0.5).

    **Collector interface abstraction (per D-10):** The skill orchestrates MCP calls directly — the abstraction boundary is at the skill level. To swap the MCP backend for a Bot API implementation later, only this step's "Step 2 — Read reactions via Slack MCP" section needs updating. The reaction mapping (Step 3) and FeedbackEntry writing (Step 4) remain unchanged. Document this with a comment:

    ```markdown
    > **Abstraction note (D-10):** Steps 3-5 are backend-agnostic. To replace Slack MCP with Bot API (`fetch` + `SLACK_BOT_TOKEN`), only Step 2 needs changing — the rest of the pipeline stays the same.
    ```

    **Batching (discretion):** One MCP call per message URL. The improvement-log typically has one Slack message per run, so batching adds complexity with zero benefit.
  </action>
  <verify>
    <automated>grep -c "Step 0.4.5" /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/skills/kc-nightwatch/SKILL.md && grep -c "slack_reaction" /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/skills/kc-nightwatch/SKILL.md && grep -c "slack_read_thread" /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/skills/kc-nightwatch/SKILL.md</automated>
  </verify>
  <done>Step 0.4.5 exists in SKILL.md with: (1) improvement-log slack_url lookup with graceful skip on absence, (2) slack_read_thread MCP call with error handling, (3) reaction-to-verdict mapping per D-02, (4) FeedbackEntry creation with source 'slack_reaction', (5) D-10 abstraction note for future Bot API swap. Slack MCP failures never block Phase 1+.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -c "slack_url" skills/kc-nightwatch/SKILL.md` returns 5+ (Step 5.2 format, Step 5.4 capture, Step 0.4.5 lookup)
2. `grep -c "Step 0.4.5" skills/kc-nightwatch/SKILL.md` returns 2+ (heading + reference)
3. `grep -c "slack_reaction" skills/kc-nightwatch/SKILL.md` returns 3+ (source value, verdict mapping, FeedbackEntry)
4. `grep -c "slack_read_thread" skills/kc-nightwatch/SKILL.md` returns 2+ (Step 2 MCP call + context)
5. `grep -q "D-10" skills/kc-nightwatch/SKILL.md` — abstraction note present
6. `grep -q "D-02" skills/kc-nightwatch/SKILL.md` — reaction mapping references decision
7. `grep -q "thumbsup.*accepted" skills/kc-nightwatch/SKILL.md` — reaction mapping table exists
8. `grep -q "Skip.*silently" skills/kc-nightwatch/SKILL.md` — graceful skip when no slack_url
</verification>

<success_criteria>
- SKILL.md Step 5.4 captures the Slack message URL after successful delivery
- SKILL.md Step 5.2 improvement-log format includes slack_url at the run-date level
- SKILL.md Step 0.4.5 reads slack_url from previous run, calls slack_read_thread, maps reactions to verdicts per D-02, and writes FeedbackEntries with source 'slack_reaction'
- Graceful handling: absent slack_url skips silently, MCP failures log warning but never block Phase 1+
- D-10 abstraction boundary documented (only Step 2 needs changing for Bot API swap)
- The full feedback loop is closed: post Slack message -> store URL -> next run reads reactions -> writes feedback entries
</success_criteria>

<output>
After completion, create `.planning/phases/14-extended-feedback/14-03-SUMMARY.md`
</output>
