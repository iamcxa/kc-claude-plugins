---
phase: 16-health-page-enrichment
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - app/frontend/components/forge-result-card.ts
  - app/frontend/components/calibration-table.ts
  - app/frontend/components/sparkline.ts
  - app/frontend/pages/health.ts
  - app/tests/frontend/forge-result-card.test.ts
  - app/tests/frontend/calibration-table.test.ts
  - app/tests/frontend/sparkline.test.ts
autonomous: false
requirements: [VIZ-02, VIZ-03, FORGE-01]

must_haves:
  truths:
    - "Per-indicator sparklines show a tooltip on hover with the percentage value and run ID"
    - "A calibration table is visible with columns Indicator, Threshold, Reject %, Feedback showing all indicators sorted by reject rate descending"
    - "Rows with fewer than 10 feedback entries show threshold_null_reason instead of a threshold value"
    - "A forge results card shows pass/fail status, relative time, and expands to show branch and details"
    - "The health page sections appear in order: HealthSummaryBar, ForgeResultCard, Target Cards, CalibrationTable, Reject Rate Charts"
  artifacts:
    - path: "app/frontend/components/forge-result-card.ts"
      provides: "ForgeResultCard component with expand/collapse, status colors, null/stale states"
      contains: "ForgeResultCard"
    - path: "app/frontend/components/calibration-table.ts"
      provides: "CalibrationTable component with sorted rows and N-gate display"
      contains: "CalibrationTable"
    - path: "app/frontend/components/sparkline.ts"
      provides: "Enhanced Sparkline with tooltip hit areas and activeIdx state"
      contains: "runIds"
    - path: "app/frontend/pages/health.ts"
      provides: "Health page wiring all new components with data fetching"
      contains: "ForgeResultCard"
    - path: "app/tests/frontend/forge-result-card.test.ts"
      provides: "Pure helper tests for relativeTime, statusColor"
      contains: "relativeTime"
    - path: "app/tests/frontend/calibration-table.test.ts"
      provides: "Sort and threshold cell logic tests"
      contains: "threshold_null_reason"
    - path: "app/tests/frontend/sparkline.test.ts"
      provides: "Tooltip coordinate math and edge guard tests"
      contains: "pointSpacing"
  key_links:
    - from: "app/frontend/pages/health.ts"
      to: "app/frontend/components/forge-result-card.ts"
      via: "import and render ForgeResultCard with forgeData state"
      pattern: "ForgeResultCard.*forgeData"
    - from: "app/frontend/pages/health.ts"
      to: "app/frontend/components/calibration-table.ts"
      via: "import and render CalibrationTable with calibration state"
      pattern: "CalibrationTable.*calibration"
    - from: "app/frontend/pages/health.ts"
      to: "app/frontend/lib/api.ts"
      via: "api.getForgeResults() and api.getCalibration() fetch calls"
      pattern: "api\\.getForgeResults\\(\\)"
    - from: "app/frontend/components/sparkline.ts"
      to: "app/shared/types.ts"
      via: "runIds prop parallel to values for tooltip"
      pattern: "runIds.*string"
---

<objective>
Create ForgeResultCard, CalibrationTable, and sparkline tooltip components, then wire them into the health page.

Purpose: This delivers all three Phase 16 requirements (VIZ-02, VIZ-03, FORGE-01) as visible UI on the health page.
Output: Three new/modified components, three test files, updated health page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-health-page-enrichment/16-CONTEXT.md
@.planning/phases/16-health-page-enrichment/16-RESEARCH.md
@.planning/phases/16-health-page-enrichment/16-UI-SPEC.md
@.planning/phases/16-health-page-enrichment/16-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 16-01 that this plan consumes -->

From app/shared/types.ts:
```typescript
export interface HealthIndicatorData {
  current: number
  trend: 'improving' | 'stable' | 'degrading'
  history: number[]
  run_ids?: string[]  // parallel to history (added in Plan 16-01)
}

export interface CalibrationData {
  indicator: string
  total_feedback: number
  reject_count: number
  reject_rate: number
  current_threshold: number | null
  threshold_null_reason?: string
  history: number[]
}

export interface ForgeResultData {
  forge_result: {
    status: 'pass' | 'fail'
    branch: string | null
    details: string
  } | null
  run_date: string | null
  stale: boolean
}
```

From app/frontend/lib/api.ts (after Plan 16-01):
```typescript
getForgeResults(): Promise<ForgeResultData> {
  return get<ForgeResultData>('/api/forge/results')
},
getCalibration(): Promise<CalibrationData[]> {
  return get<CalibrationData[]>('/api/feedback/calibration')
},
```

From app/frontend/components/sparkline.ts (current — will be modified):
```typescript
interface Props {
  values: number[]
  width?: number
  height?: number
}
export function Sparkline({ values, width = 80, height = 20 }: Props) { ... }
```

From app/frontend/pages/health.ts (current structure):
- Imports: html, useState, useEffect, Target, TargetHealthData, api, Sparkline, LineChart, HealthSummaryBar
- State: targets, healthData, loading, error
- Section order: heading -> HealthSummaryBar -> loading/error/sparse -> target cards -> reject rate charts
- Data fetch: api.getTargets() -> api.getHealth() for each target

From app/frontend/components/toast.ts (pattern reference):
- position:fixed absolute positioning for floating UI
- Signal-backed state, useEffect lifecycle

From app/tests/frontend/action-card.test.ts (test pattern):
- Re-implements pure helper functions in test file
- Tests logic without DOM/Preact rendering
- Uses describe/it/expect from bun:test
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ForgeResultCard and CalibrationTable components with tests</name>
  <files>
    app/frontend/components/forge-result-card.ts,
    app/frontend/components/calibration-table.ts,
    app/tests/frontend/forge-result-card.test.ts,
    app/tests/frontend/calibration-table.test.ts
  </files>
  <read_first>
    - app/frontend/components/toast.ts (pattern reference for floating positioned UI, signal state, html template syntax)
    - app/frontend/components/sparkline.ts (pattern reference for Preact+HTM component structure)
    - app/tests/frontend/action-card.test.ts (pattern reference for testing pure helpers without DOM)
    - app/shared/types.ts (lines 176-194 — CalibrationData and ForgeResultData interfaces)
    - app/frontend/pages/health.ts (understand where these components will be inserted)
    - .planning/phases/16-health-page-enrichment/16-UI-SPEC.md (visual contract — colors, spacing, typography, copy)
    - .planning/phases/16-health-page-enrichment/16-CONTEXT.md (locked decisions D-03 through D-08)
  </read_first>
  <behavior>
    ForgeResultCard helpers:
    - relativeTime(null) returns 'never'
    - relativeTime(ISO string 30min ago) returns 'just now' (diffH < 1)
    - relativeTime(ISO string 5h ago) returns '5h ago'
    - relativeTime(ISO string 3d ago) returns '3d ago'
    - statusColor({stale:true,...}) returns 'var(--muted)'
    - statusColor({forge_result:null,...}) returns 'var(--muted)'
    - statusColor({forge_result:{status:'pass'},...,stale:false}) returns 'var(--success)'
    - statusColor({forge_result:{status:'fail'},...,stale:false}) returns 'var(--error)'

    CalibrationTable helpers:
    - sortByRejectRate([{reject_rate:0.2}, {reject_rate:0.8}, {reject_rate:0.5}]) returns sorted descending [0.8, 0.5, 0.2]
    - formatThreshold(null, 'Accumulating data (5/10)') returns 'Accumulating data (5/10)'
    - formatThreshold(0.45, undefined) returns '45%'
    - formatThreshold(0.123, undefined) returns '12%' (Math.round)
    - sortByRejectRate([]) returns []
  </behavior>
  <action>
    **ForgeResultCard** (`app/frontend/components/forge-result-card.ts`):

    Create a Preact+HTM component with the following exported pure helpers and component:

    1. `relativeTime(isoDate: string | null): string` — Returns 'never' for null, 'just now' for <1h, '${N}h ago' for <24h, '${N}d ago' otherwise. Uses `Math.floor(diffMs / 3600000)` for hours, `Math.floor(diffH / 24)` for days.

    2. `statusColor(data: ForgeResultData): string` — Returns 'var(--muted)' when `data.stale === true` OR `data.forge_result === null`. Returns 'var(--success)' when `data.forge_result.status === 'pass'`. Returns 'var(--error)' when `data.forge_result.status === 'fail'`. (Per D-04.)

    3. `statusIcon(data: ForgeResultData): string` — Returns unicode: '\u2713' for pass, '\u2717' for fail, '\u2014' for null/stale.

    4. `ForgeResultCard({ data }: { data: ForgeResultData | null })` component:
       - When `data === null` (fetch error): render "Unavailable" in var(--muted) (per RESEARCH Pitfall 5)
       - When `data.forge_result === null`: render "\u2014 No forge run recorded" in var(--muted), no expand behavior (per UI-SPEC null state)
       - Otherwise: collapsed state shows `[icon] [Pass|Fail] \u2014 [relativeTime]` with color from statusColor. Click toggles `expanded` useState. Expanded state shows border-top separator, "Branch: {branch}" in mono font, "Details:" with `<ul>` list of detail lines (split by newline).
       - Container: `background:var(--panel);border:1px solid var(--border);border-radius:6px;padding:12px;margin:0 16px 16px;cursor:pointer;`
       - `role="button"` and `tabIndex={0}` on the clickable div
       - `aria-expanded={expanded}` attribute
       - `onKeyDown` handler: Enter (key === 'Enter') and Space (key === ' ') toggle expand, Space calls `e.preventDefault()`
       - Expand/collapse: instant toggle (no CSS transition) — per UI-SPEC
       - Stale state: all text color set to var(--muted) when `data.stale === true`
       - Badge text: "Pass" or "Fail" using `font-size:14px;font-weight:600;color:${statusColor(data)}`
       - Relative time: `font-size:14px;color:var(--muted);margin-left:auto;`
       - Expanded section: `padding-top:8px;border-top:1px solid var(--border);margin-top:8px;`
       - Branch: `font-size:14px;color:var(--text);font-family:var(--font-mono);`
       - Details list: `font-size:14px;color:var(--muted);margin:4px 0;padding-left:16px;`

    **CalibrationTable** (`app/frontend/components/calibration-table.ts`):

    Create a Preact+HTM component with exported helpers:

    1. `sortByRejectRate(data: CalibrationData[]): CalibrationData[]` — Returns `[...data].sort((a, b) => b.reject_rate - a.reject_rate)`. Never mutates original. (Per D-06.)

    2. `formatThreshold(threshold: number | null, nullReason?: string): string` — When `threshold === null`, return `nullReason ?? 'Accumulating data'`. When not null, return `${Math.round(threshold * 100)}%`. (Per D-07 — uses backend threshold_null_reason, NOT hardcoded.)

    3. `CalibrationTable({ calibration, loading }: { calibration: CalibrationData[] | null; loading: boolean })` component:
       - Loading state (`loading === true`): single row spanning 4 cols: "Loading..." in var(--muted)
       - Empty state (`calibration` is `[]`): single row spanning 4 cols: "No feedback collected yet \u2014 run the pipeline to generate data." in var(--muted) (per UI-SPEC copy)
       - Normal state: section header "Calibration" (11px, semibold, var(--muted), margin-bottom:8px), then table
       - Container wrapper: `background:var(--panel);border:1px solid var(--border);border-radius:6px;padding:12px;margin:0 16px 16px;`
       - Table: `width:100%;border-collapse:collapse;font-size:14px;`
       - Header row: `font-size:11px;font-weight:600;color:var(--muted);text-align:left;padding:8px 12px;border-bottom:1px solid var(--border);`
       - Column headers: "Indicator", "Threshold", "Reject %", "Feedback" (per D-07)
       - Data rows: `font-size:14px;color:var(--text);padding:8px 12px;border-bottom:1px solid var(--border);`
       - Last row: no bottom border (use `:last-child` or conditional inline style)
       - Threshold cell: when null, render `html\`<span style="font-style:italic;color:var(--muted);">${formatThreshold(row.current_threshold, row.threshold_null_reason)}</span>\``. When not null, render `formatThreshold(row.current_threshold)`.
       - Reject % cell: `${Math.round(row.reject_rate * 100)}%`
       - Feedback cell: `${row.total_feedback}`
       - Rows sorted by `sortByRejectRate()` (descending reject rate per D-06)

    **Tests** — following action-card.test.ts pattern (re-implement pure helpers in test file, no DOM):

    `app/tests/frontend/forge-result-card.test.ts`:
    - Import `describe, it, expect` from `bun:test`
    - Re-implement `relativeTime` and `statusColor` functions
    - Test all relativeTime cases: null, <1h, hours, days
    - Test all statusColor cases: stale, null forge_result, pass, fail

    `app/tests/frontend/calibration-table.test.ts`:
    - Import CalibrationData type from shared/types.ts
    - Re-implement `sortByRejectRate` and `formatThreshold` functions
    - Test sort descending, empty array, single element
    - Test formatThreshold: null with reason, null without reason, value rounding
  </action>
  <verify>
    <automated>cd /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app && bun test tests/frontend/</automated>
  </verify>
  <acceptance_criteria>
    - app/frontend/components/forge-result-card.ts exports `ForgeResultCard`, `relativeTime`, `statusColor`
    - forge-result-card.ts contains `role="button"` and `tabIndex={0}` and `aria-expanded`
    - forge-result-card.ts contains `var(--success)` and `var(--error)` and `var(--muted)` color assignments
    - forge-result-card.ts contains `No forge run recorded` string for null state
    - forge-result-card.ts contains `Unavailable` string for fetch error state (data === null)
    - forge-result-card.ts contains `font-family:var(--font-mono)` for branch display
    - app/frontend/components/calibration-table.ts exports `CalibrationTable`, `sortByRejectRate`, `formatThreshold`
    - calibration-table.ts contains column headers "Indicator", "Threshold", "Reject %", "Feedback"
    - calibration-table.ts contains `font-style:italic;color:var(--muted)` for null threshold display
    - calibration-table.ts contains `No feedback collected yet` empty state string
    - calibration-table.ts contains `.sort((a, b) => b.reject_rate - a.reject_rate)` sort logic
    - app/tests/frontend/forge-result-card.test.ts contains tests for relativeTime and statusColor
    - app/tests/frontend/calibration-table.test.ts contains tests for sortByRejectRate and formatThreshold
    - `bun test tests/frontend/` exits 0 with all tests passing
  </acceptance_criteria>
  <done>ForgeResultCard and CalibrationTable components created with all states (normal, null, stale, loading, empty), helper function tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enhance Sparkline with tooltip hit areas and create sparkline tests</name>
  <files>
    app/frontend/components/sparkline.ts,
    app/tests/frontend/sparkline.test.ts
  </files>
  <read_first>
    - app/frontend/components/sparkline.ts (full file — current 34-line implementation to be enhanced)
    - app/frontend/components/toast.ts (absolute positioning pattern reference)
    - app/tests/frontend/action-card.test.ts (pure helper test pattern)
    - .planning/phases/16-health-page-enrichment/16-UI-SPEC.md (sparkline tooltip spec: hit rects, tooltip positioning, edge guards)
    - .planning/phases/16-health-page-enrichment/16-RESEARCH.md (Pattern 1 SVG hit areas, Pattern 4 tooltip positioning, Pitfall 1 mouseleave)
  </read_first>
  <behavior>
    Sparkline pure helpers:
    - tooltipLeft(0, 5, 80) returns { left:'0px', transform:'none' } (first point — left-anchored)
    - tooltipLeft(4, 5, 80) returns { right:'0px', left:'auto', transform:'none' } (last point — right-anchored)
    - tooltipLeft(2, 5, 80) returns { left:'40px', transform:'translateX(-50%)' } (middle point — centered)
    - computeHitRects([10,20,30], 80, 20) returns 3 rect objects with x, width, height=20
    - computeHitRects first rect starts at x=0
    - computeHitRects last rect ends at x=width
    - formatTooltipValue(0.75) returns '75%'
    - formatTooltipValue(0.123) returns '12%'
  </behavior>
  <action>
    Enhance `app/frontend/components/sparkline.ts`:

    1. Add `useState` import: `import { useState } from 'preact/hooks'`
    2. Extend Props interface (per D-11):
       ```typescript
       interface Props {
         values: number[]
         width?: number
         height?: number
         runIds?: string[]  // parallel to values — tooltip shows run ID when present
       }
       ```

    3. Add exported pure helper functions (testable without DOM):

       `tooltipStyle(idx: number, total: number, width: number)` — Returns an object with CSS position properties:
       - First point (idx === 0): `{ left: '0px', transform: 'none' }`
       - Last point (idx === total - 1): `{ right: '0px', left: 'auto', transform: 'none' }`
       - Middle points: `{ left: '${(idx / (total - 1)) * width}px', transform: 'translateX(-50%)' }`

       `formatTooltipValue(v: number): string` — Returns `${Math.round(v * 100)}%`

    4. Inside component function, add `const [activeIdx, setActiveIdx] = useState<number | null>(null)` after the existing early return for `values.length < 2`.

    5. Compute `pointSpacing`: `const pointSpacing = values.length > 1 ? width / (values.length - 1) : width`

    6. Wrap the existing `<svg>` in a container div: `<div style="position:relative;display:inline-block;">`. The tooltip div and the SVG are siblings inside this container.

    7. Add `pointer-events:none` to the `<polyline>` so rects capture all events.

    8. After the `<polyline>`, add invisible `<rect>` hit areas inside the SVG — one per data point:
       ```
       ${values.map((v, i) => {
         const x = (i / (values.length - 1)) * width
         const rectX = i === 0 ? 0 : (i === values.length - 1 ? x - pointSpacing / 2 : x - pointSpacing / 2)
         const rectW = (i === 0 || i === values.length - 1) ? pointSpacing / 2 : pointSpacing
         return html`
           <rect
             key=${i}
             x=${rectX} y=${0} width=${rectW} height=${height}
             fill="transparent"
             onMouseEnter=${() => setActiveIdx(i)}
             onMouseLeave=${() => setActiveIdx(null)}
           />
         `
       })}
       ```

    9. After the `</svg>`, add the tooltip div (sibling, not inside SVG — per RESEARCH Pitfall 1):
       ```
       ${activeIdx !== null && html`
         <div style="position:absolute;top:-36px;${tooltipStyleStr(activeIdx, values.length, width)};
                     background:var(--panel);border:1px solid var(--border);
                     border-radius:4px;padding:4px 8px;font-size:11px;color:var(--text);
                     white-space:nowrap;pointer-events:none;z-index:10;">
           <div>${formatTooltipValue(values[activeIdx])}</div>
           ${runIds?.[activeIdx] && html`<div style="color:var(--muted);">${runIds[activeIdx]}</div>`}
         </div>
       `}
       ```
       Where `tooltipStyleStr` converts `tooltipStyle()` result to inline CSS string.

    10. Close the container `</div>`.

    **Test file** (`app/tests/frontend/sparkline.test.ts`):
    - Re-implement `tooltipStyle` and `formatTooltipValue` in test
    - Test tooltipStyle edge cases (first, last, middle indices)
    - Test formatTooltipValue rounding
    - Test that pointSpacing calculation is correct for various array lengths
  </action>
  <verify>
    <automated>cd /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app && bun test tests/frontend/sparkline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - app/frontend/components/sparkline.ts interface Props contains `runIds?: string[]`
    - sparkline.ts exports `tooltipStyle` and `formatTooltipValue`
    - sparkline.ts contains `useState<number | null>(null)` for activeIdx
    - sparkline.ts contains `position:relative;display:inline-block` wrapper div
    - sparkline.ts contains `<rect` elements with `onMouseEnter` and `onMouseLeave` handlers
    - sparkline.ts contains `pointer-events:none` on the polyline
    - sparkline.ts tooltip div contains `pointer-events:none;z-index:10`
    - sparkline.ts contains `color:var(--muted)` for run ID line in tooltip
    - app/tests/frontend/sparkline.test.ts exists with tests for tooltipStyle and formatTooltipValue
    - `bun test tests/frontend/sparkline.test.ts` exits 0
  </acceptance_criteria>
  <done>Sparkline shows tooltip with value % and run ID on hover, tooltip positions correctly at edges and center, all sparkline tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Wire ForgeResultCard, CalibrationTable, and enhanced Sparkline into health.ts</name>
  <files>app/frontend/pages/health.ts</files>
  <read_first>
    - app/frontend/pages/health.ts (full file — current 160 lines, understand data flow and section order)
    - app/frontend/components/forge-result-card.ts (just created — verify export name and props)
    - app/frontend/components/calibration-table.ts (just created — verify export name and props)
    - app/frontend/components/sparkline.ts (just modified — verify new runIds prop)
    - app/frontend/lib/api.ts (verify getForgeResults() and getCalibration() methods exist)
    - app/shared/types.ts (verify ForgeResultData, CalibrationData, HealthIndicatorData types)
    - .planning/phases/16-health-page-enrichment/16-CONTEXT.md (D-01 page layout order, D-05 forge data source)
    - .planning/phases/16-health-page-enrichment/16-UI-SPEC.md (page layout contract — section order)
  </read_first>
  <action>
    Modify `app/frontend/pages/health.ts`:

    1. Add imports at top of file:
       ```typescript
       import type { ForgeResultData, CalibrationData } from '../../shared/types.ts'
       import { ForgeResultCard } from '../components/forge-result-card.ts'
       import { CalibrationTable } from '../components/calibration-table.ts'
       ```

    2. Add new state variables inside `Health()` function (after existing state declarations around line 34):
       ```typescript
       const [forgeData, setForgeData] = useState<ForgeResultData | null>(null)
       const [calibration, setCalibration] = useState<CalibrationData[] | null>(null)
       const [calibrationLoading, setCalibrationLoading] = useState(true)
       ```

    3. Add a second `useEffect` (after the existing one that fetches targets+health) for forge and calibration data:
       ```typescript
       useEffect(() => {
         api.getForgeResults().then(setForgeData).catch(() => setForgeData(null))
         api.getCalibration()
           .then(data => { setCalibration(data); setCalibrationLoading(false) })
           .catch(() => { setCalibration([]); setCalibrationLoading(false) })
       }, [])
       ```
       The forge fetch uses `.catch(() => setForgeData(null))` so ForgeResultCard renders "Unavailable" on error (per RESEARCH Pitfall 5). The calibration fetch uses `.catch(() => { setCalibration([]); setCalibrationLoading(false) })` to show empty state on error.

    4. Insert `<${ForgeResultCard} data=${forgeData} />` into the template AFTER the HealthSummaryBar and BEFORE the loading/error/sparse banners. Per D-01, the section order is:
       - Page heading ("Flywheel Health") — existing, unchanged
       - HealthSummaryBar — existing, unchanged
       - **ForgeResultCard** — NEW, insert here
       - Loading / error / sparse-data notices — existing, unchanged
       - Target Cards — existing (enhanced with runIds below)
       - **CalibrationTable** — NEW, insert before reject rate charts
       - Reject Rate Charts — existing, unchanged

    5. In the target cards section (around line 120), update the Sparkline usage to pass `runIds`:
       Change:
       ```
       <${Sparkline} values=${indicator.history} />
       ```
       To:
       ```
       <${Sparkline} values=${indicator.history} runIds=${indicator.run_ids} />
       ```
       This passes the parallel `run_ids` array from `HealthIndicatorData` (added in Plan 16-01) to the enhanced Sparkline component.

    6. Insert `<${CalibrationTable} calibration=${calibration} loading=${calibrationLoading} />` AFTER the target cards section and BEFORE the reject rate charts section. Place it inside the `${!loading && !error && ...}` guard.

    7. Verify the "runs analyzed" text on line 108 uses `font-size:11px` (not 12px) per UI-SPEC typography standardization. If it says `font-size:12px`, change to `font-size:11px`.
  </action>
  <verify>
    <automated>cd /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app && bun test</automated>
  </verify>
  <acceptance_criteria>
    - health.ts imports ForgeResultCard from '../components/forge-result-card.ts'
    - health.ts imports CalibrationTable from '../components/calibration-table.ts'
    - health.ts imports ForgeResultData and CalibrationData from '../../shared/types.ts'
    - health.ts contains `useState<ForgeResultData | null>(null)` for forgeData
    - health.ts contains `useState<CalibrationData[] | null>(null)` for calibration
    - health.ts contains `api.getForgeResults().then(setForgeData).catch`
    - health.ts contains `api.getCalibration().then`
    - health.ts contains `<${ForgeResultCard}` appearing AFTER HealthSummaryBar and BEFORE the loading div
    - health.ts contains `<${CalibrationTable}` appearing AFTER target card maps and BEFORE reject rate charts
    - health.ts Sparkline usage contains `runIds=${indicator.run_ids}`
    - Full test suite (`bun test`) exits 0
  </acceptance_criteria>
  <done>Health page renders ForgeResultCard, CalibrationTable, and tooltipped Sparklines in the correct D-01 section order, with data from Phase 15 endpoints</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification of health page enrichment</name>
  <files>app/frontend/pages/health.ts</files>
  <action>Human verifies the health page visually in the browser. No code changes — verification only.</action>
  <verify>User confirms all components render correctly per D-01 section order</verify>
  <done>User types approved</done>
  <what-built>
    Complete health page enrichment: ForgeResultCard with expand/collapse and pass/fail/stale/null states, CalibrationTable with sorted rows and N-gate threshold display, Sparkline tooltips showing value % and run ID on hover. All wired into the health page in D-01 section order.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd /Users/kent/Project/kc-claude-workspace/kc-claude-plugins/kc-nightwatch/app && bun run server/index.ts`
    2. Open http://localhost:3201 and navigate to the Health page
    3. Verify ForgeResultCard appears below HealthSummaryBar:
       - Shows pass/fail status or "No forge run recorded"
       - Click to expand — shows branch name + details
       - If stale (>36h), card text is gray (var(--muted))
    4. Verify sparkline tooltips:
       - Hover over data points in any target card sparkline
       - Tooltip shows value as percentage (e.g., "75%")
       - If run IDs are present, second line shows run ID
       - First/last data points: tooltip does not clip (anchored to edge)
    5. Verify CalibrationTable between target cards and reject rate charts:
       - Shows 4 columns: Indicator, Threshold, Reject %, Feedback
       - Rows sorted by reject rate descending
       - Indicators with <10 feedback show italic text (e.g., "(5/10)") instead of threshold
    6. Verify overall page layout matches D-01: HealthSummaryBar -> ForgeResultCard -> Target Cards -> CalibrationTable -> Reject Rate Charts
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. All frontend tests pass: `cd app && bun test tests/frontend/`
2. All server tests pass: `cd app && bun test tests/server/`
3. Full suite: `cd app && bun test`
4. Visual verification via checkpoint
</verification>

<success_criteria>
1. ForgeResultCard shows pass/fail/stale/null states with correct colors (var(--success)/var(--error)/var(--muted)) per D-04
2. ForgeResultCard expands on click to show branch and details per D-03
3. CalibrationTable shows all indicators sorted by reject_rate descending per D-06 with columns per D-07
4. CalibrationTable rows with fewer than 10 feedback show threshold_null_reason per D-07
5. Sparkline hover tooltip shows value % and run ID per D-09 with custom div (not native SVG title) per D-09
6. Page section order matches D-01: HealthSummaryBar > ForgeResultCard > Target Cards > CalibrationTable > Reject Rate Charts
7. All test suites pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-health-page-enrichment/16-02-SUMMARY.md`
</output>
