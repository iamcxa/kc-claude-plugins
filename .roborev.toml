# Repository-owned implementation-exit reviewer. The workflow passes these
# values explicitly and forces `--panel none`; this file installs no hook.
review_agent = "codex"
review_model = "gpt-5.6-terra"
review_reasoning = "thorough"
review_min_severity = "medium"
