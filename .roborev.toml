# Repository fallback for the fixed implementation-exit reviewer. Every request
# also passes these values and --panel none explicitly; no hook or panel is installed.
review_agent = "codex"
review_model = "gpt-5.6-terra"
review_reasoning = "medium"
review_min_severity = "high"
