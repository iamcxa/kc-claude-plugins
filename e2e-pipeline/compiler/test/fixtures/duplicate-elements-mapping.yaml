version: 2
app: test-app-dupes
base_url: "http://localhost:3000"
pages:
  page-a:
    url_pattern: "/a"
    elements:
      data_table:
        selector: '[role="table"]'
        description: "Table on page A"
      unique_a:
        selector: ".unique-a"
        description: "Only on A"
  page-b:
    url_pattern: "/b"
    elements:
      data_table:
        selector: '[role="grid"]'
        description: "Table on page B"
      unique_b:
        selector: ".unique-b"
        description: "Only on B"
