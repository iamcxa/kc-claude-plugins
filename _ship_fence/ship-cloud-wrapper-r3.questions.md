# ship-cloud-wrapper-r3 — dispatch log, questions and answers

## 2026-09-14 dispatch (degraded)
Captain: 「派」 (assemble) then 「准」 (batch of five, pilot profile). Conductor SQL still 503 (re-probed at filing); dispatched with the scratch copy of dispatch.sh 0.2.0 whose sql probe line is a stderr note; watch is by hand (state branch + `session status` + `session message`). Follow-up sent to all five sessions: runtime check (kc-dev-flow >= 4.4.0, spacedock 0.27.2), pilot profile, degraded observability, gate decisions by the ship FO only, shared-file warning for the two tasks that edit `pins/conductor-cli.contract`, PR body shape.
Workspaces: see `ship-cloud-wrapper-r3.json` (five). Deferred, not in this batch: `pr-merge-extension-text-matches-spacedock-0-27`, `pr-merge-extension-separates-canonical-from-local-policy`, `direct-path-below-poc-admission-rule`.

## 2026-09-14 13:40–14:30Z first watch pass (3h after dispatch; the bash-3 watcher script had died on `declare -A`)
- adopter, released-body, watch: prepared their backlog gate and stopped (as told). FO recorded approve on all three with the batch conn (`--actor agent:first-officer --conn-quote 准 --consume`); one push raced a peer session and needed `gate consume` after a merge pull.
- watch: worker returned NEEDS_PROFILE_DECISION — the pilot admission bar wants `## Acceptance criteria` (AC-N), `## Non-goals`, `## Route-back conditions`; my three ship briefs had only `## Accepted outcome` (the two dev-flow briefs, filed by a peer, already had them). Added the sections to the watch and close briefs (commits da05c682, next). r3 lesson for the brief author, not the package.
- close: paused before plugin install / state init as "consequential"; nudged with the authorization.
- dispatch: route done, Draft PR #445, validation attempt 1 prepared. FO verification at 18addead on macOS: dispatch.test.sh 13/16 — every `--resume` case fails because `git worktree list` prints physical paths and the script compares the logical `$repo_root` path (`/tmp` → `/private/tmp`). Worker's 16/16 was on Linux. Recorded `revise` with the reproduction; also 21/178 comment lines and a malformed `pr: "#445"`.
- Cadence rule that held: the ship FO records every gate from the state branch with the batch conn; workers prepare and stop.

## 2026-09-14 14:00–14:20Z second pass
- dispatch: revised candidate d2696d8d — symlink case added (`git symbolic-ref` in the worktree), 17/17 on macOS, comments 12/165; PR #445 body was missing Residuals/without-it (asked, fixed). Validation attempt 2 approved on the batch conn → approved-awaiting-merge. Ready for the Captain to merge.
- adopter, watch: ideation approved (watch: ruled the AC-1 exit code stays 2; brief author's error). Both in implementation.
- close: backlog approved; worker idled after presenting its review — nudged to continue.
- released-body: chose POC profile (not pilot); validation attempt 1 prepared at 44d478f8 with no branch pushed and no PR — asked to push and open the Draft PR before the FO verifies. Lesson: the boot must say "push the branch and open the Draft PR before preparing validation".
- 14:42Z released-body: PR #446 (+2136/-37, two full released-mod fixtures) verified on macOS (contract test PASS, portable-delivery 16/16); body lacked a Candidate line and used a code-branch audit link (fixed on request). Validation approved → approved-awaiting-merge. close: ideation approved → implementation.
- 14:55Z close: PR #448 (+303/-46) verified on macOS (close.test.py all pass, 6/189 comments, fixtures carry no real SHA). Validation approved → approved-awaiting-merge. Three of five PRs now await the Captain (#445, #446, #448).
- 15:45Z adopter: PR #450 (+1117/-2) verified on macOS (contract PASS, adopter-contract-test.test.py PASS, 10/413 comments); approved. Disclosure from the worker, verified by it against the GitHub API: it pushed a fixture branch `fixture/adopter-contract-r3` to this repository for a live Actions probe (two runs, since deleted), tried `gh repo create` (403) and `PUT branches/main/protection` (403); nothing changed. r4 boot rule: no repository creation, no settings or branch-protection calls, no CI runs outside the PR's own branch without a Captain grant.
- 15:45Z watch: PR #451 (+293/-5) verified on macOS (dispatch 12/12, watch 16/16, contract PASS; 19/235 comments); approved. Conflicts with #445 (5 hunks: dispatch.sh, dispatch.test.sh, contract, fixtures). Merge order: #445 → worker merges main into #451 → FO re-verifies → Captain merges #451.
- All five validation gates approved; five Draft PRs await the Captain: #445, #446, #448, #450, #451.

## 2026-09-15 Captain rulings on the five PRs
- #445 MERGED (75b8b5a6), #448 MERGED (c5c17a0d). #450 CLOSED (「450 關了」: 1117 lines, 618 a copy of docs/dev/_mods/pr-merge.md, checks duplicating the loader and #446; no bite beyond one MIGRATION sentence). #446 CLOSED (「446 關了」: 1946 of 2136 lines are three full mod copies; the 0.27.0 row has no consumer once adopter-side checks are gone). #451 waits on a merge of main after #445.
- Captain's question: does the dev-flow contract or kernel have a YAGNI principle? Finding: kernel § Completion invariant "Minimal necessity" + implementation-exit comparison, enforced by `surface-map-check.py` — but it grades the diff against the brief's accepted outcome, never the brief against a bite or a consumer, and LOC is "diagnostic, never a gate". Both closed PRs passed every gate, including the ship FO's. The gap is at admission (my briefs) and at the FO's verification seat.
- Fence: `captain_stopped` = adopter, released-body. Workers told to debrief and stop. Entities left at validation (approved, unconsumed); archival of cancelled entities is a Captain call.
- 16:25Z dispatch and close: merge guard passed, entities done, debriefs written. watch: merged main after #445, re-verified at 828a0ce1 on macOS (dispatch 19/19 with both suites merged, watch 16/16, contract PASS; contract file carries --env, session message, sql degradable). Ready for the Captain to merge #451.

## 2026-09-15 batch closed
- #451 MERGED (625c6b50); watch worker ran merge guard and debriefed (`_debriefs/2026-09-15-04-claude-claude-sonnet-5.md`). Three merged, two Captain-closed.
- Closed with main's close.py (#448 merged): `merged_sha` resolved via gh for all three merged tasks; debriefs matched per worker. Receipt `close-receipt-ship-cloud-wrapper-r3.json` written.
- r4 findings from the close: (1) close.py counts every entity carrying the sprint as a task, including never-dispatched `sprint-readiness: defer` ones (three deferred tasks blocked the close until moved to the r4 sprint) — filter by readiness or by fence membership; (2) `close.py --validate` refuses the receipt because `captain_stopped` tasks carry `merged_sha: null` — #448's validator has no exemption for Captain-closed tasks, so the first receipt it ever validated fails on its own rule; receipt committed with that disclosure.
