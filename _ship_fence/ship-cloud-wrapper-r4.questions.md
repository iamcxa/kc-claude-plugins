# ship-cloud-wrapper-r4 — dispatch log, questions and answers

## 2026-09-15 dispatch
Captain: 「r4 現在開」. Batch of three, all with `bite:` and `consumer:` lines: `admission-asks-who-was-bitten-and-who-will-run-it` (kc-dev-flow), `close-roster-is-the-fence-and-captain-stopped-validates` (kc-ship-flow), `boot-names-delivery-order-and-forbidden-actions` (kc-ship-flow). Three r3-deferred tasks stay deferred in this sprint (no bite). Dispatched with main's dispatch.sh (#445/#451 merged, unreleased): the sql probe degraded cleanly ("degraded: conductor sql probe failed, continuing") — first live use of #451. Follow-up sent to all three: runtime, pilot, gate cadence (prepare and stop; FO records), push-and-open-PR before validation, forbidden actions, no fixture copies, name the consumer in the PR body.
Workspaces: see `ship-cloud-wrapper-r4.json`.
- 22:55Z close-roster: worker followed the delivery order (PR #455 open before the validation gate). Verified at e828c9b1 on macOS: close.test.py, uat-doc.test.py pass; 9/106 comments; no fixture copies an existing file. AC-2 replay: the r3 receipt as committed by the old close.py still fails (no `closed` marker); re-closing r3 with #455's close.py regenerates it with `closed: captain_stopped` on the two Captain-closed tasks and it validates — receipt regenerated and committed here. AC-1 deviation disclosed in the PR body (merged-without-fence-entry tasks kept, for the #448 DEV-304 fixture) — accepted. Approved → awaiting merge.
- 23:10Z boot: PR #456 (+52/-8) verified on macOS (dispatch 19/19, watch 17/17, contract PASS; both boot sentences verbatim; 4/28 comments; no copies). Approved → awaiting merge.
- 23:58Z admission: PR #457 (+392/-3) opened; suites pass on macOS. Finding: `validate_admission_brief()` is reachable only via `--validate-admission`, which no skill or stage contract invokes — replaying it on the three r4 briefs refuses all three although they were admitted today. The bite/consumer refusal has no live caller: the same class as #446/#450. Asked the worker to wire it into choose-work-profile (pilot/production) and prove on real briefs; every current backlog brief lacks the `## The problem` section shape and will be refused once wired.
- 00:20Z admission: correction round done — PR #457 at 874bbcef wires --validate-admission into continue-dev-flow new-admission dispatch; suites pass on macOS; real-brief replay refuses the closed adopter brief and an unshaped r4 brief; Residuals: 0/89 current briefs carry the required shape (six headings + bite:/consumer:). Approved → awaiting merge. All three r4 PRs approved: #455, #456, #457.

## 2026-09-15 batch closed
- #455 MERGED d6cde86c, #456 MERGED 52133121, #457 MERGED 7c8fd145; all three workers ran merge guard (entities done) and debriefed (-05 boot, -06 close-roster, -07 admission).
- Closed with main's close.py (#455 in): the three deferred siblings were skipped as `not dispatched` (AC-1 live), receipt validates.
- r5 finding: close.py matched admission's debrief to -06 (the close-roster worker's debrief, whose `## Shipped` names all three sibling slugs) instead of the worker's own -07 — the #448 residual ("first sorted `## Shipped` match") bit on the first batch after merge. Fence corrected by hand, receipt regenerated. Fix: match by the dispatch token or session id the worker echoes, or prefer the debrief that names only that slug.
