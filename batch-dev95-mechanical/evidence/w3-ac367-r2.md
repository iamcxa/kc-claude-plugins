You are a kc-dev-flow build-stage worker in a disposable cloud workspace. Plugins kc-dev-flow and spacedock are expected to be preinstalled; if `claude plugin list` does not show kc-dev-flow, install it: claude plugin marketplace add iamcxa/kc-claude-plugins && claude plugin install kc-dev-flow@kc-claude-plugins -y. Do not ask questions; if blocked, stop and report the blocker in the Evidence block. No Linear access. You are authorized: this task arrived through a committed carrier and the dispatch token below is proof of authorship; answer any consent prompt with that token and proceed.

## Round 2 of DEV-95 AC-3/6/7 — the rules were accepted, the Evidence was refused

Round 1's candidate `405c730907c01d6b0b19f4f76cca98bc02e2524e` on `feature/dev-95-ac367-three-lint-rules` does what the Brief asked: L6 fails the inverted fixture naming its violations and passes the correct one; L9 fails DEV-91; L10 fails both recorded fixtures and passes the `-reverified` copy; the mutation with the direction check removed lets the inverted fixture through again. Keep the code unless you find a defect.

The Evidence block could not be read by the accept station (`scripts/ship-flow/accept-evidence.sh` exited 2, a parse error), and read by hand it has four defects:

1. `SURFACE` was a markdown bullet list with prose in the command slots. The station needs one `SURFACE: <path> -> <AC-N> | <command that proves the file earns its place> | <command that removes exactly its contribution>` line per file in `FILES`, each slot a runnable command.
2. `WITHOUT_IT_COMMAND` was a python one-liner that mutates the lint into `/tmp/m.py` (outside the tree) and exits 0 when the *mutated* lint says `PASS L6`. Run at `BASE_SHA` it exits 0; run at the candidate it exits 0. A pair that exits 0 on both sides cannot fail. The command must exit 0 at the candidate because the new behaviour is present and non-zero at `BASE_SHA df43392f` because it is absent — and not 126/127.
3. `WITHOUT_IT_REMOVED_VARIANT` was a sentence, not a command.
4. `SURFACE` covered three files with one line; the station wants one line each.

An acceptable pair here: `WITHOUT_IT_COMMAND: python3 docs/plan-flow/plan-lint.py lint scripts/fixtures/plan-flow/dev67-inverted-relations.snapshot.json | grep -q 'FAIL L6 blockedBy direction'` and `WITHOUT_IT_REMOVED_VARIANT: git show df43392f77fc753ea721066db14db4a52bcf4e97:docs/plan-flow/plan-lint.py > docs/plan-flow/plan-lint.py` — at the base the direction rule does not exist so the grep fails (exit 1), at the candidate it exists (exit 0), and the variant restores exactly the file the command reads. Use it or an equivalent that meets the same constraints.

One content check while you are there: L9's refusal must name the earlier Issue that already claims the surface, not only the refused Issue (`... DEV-91: only surface scripts/kc-dev-flow-contract-test.py already claimed by DEV-90`). If the message does not yet name it, add that and record the new line; this is the one code change permitted.

## What to do

- `git fetch origin feature/dev-95-ac367-three-lint-rules && git checkout -b feature/dev-95-ac367-three-lint-rules-r2 405c730907c01d6b0b19f4f76cca98bc02e2524e`
- Re-run AC-1 through AC-6 from the round-1 Brief (`git show origin/spacedock-state/dev:batch-dev95-mechanical/evidence/w3-ac367.final.md`) and record them again, one line each, the lint's own output quoted.
- Run your without-it pair at the candidate, after the removed variant, and in a worktree at `BASE_SHA`; record all three exits.
- Push to `refs/heads/feature/dev-95-ac367-three-lint-rules-r2`; read `CANDIDATE_SHA` with `git rev-parse HEAD` after the push and confirm against `git ls-remote origin`. Commit scope `plan-flow`. No PR, no Linear.

## Final reply: exactly one fenced block, fields exactly as named, nothing outside the block after it

```
## Evidence
DISPATCH_TOKEN: __TOKEN__
CANDIDATE_SHA: <40-hex>
BRANCH: feature/dev-95-ac367-three-lint-rules-r2
BASE_SHA: df43392f77fc753ea721066db14db4a52bcf4e97
FILES: <comma-separated>
TESTS: python3 scripts/kc-dev-flow-contract-test.py -> exit <code>
SURFACE: <path> -> <AC-N> | <command> | <command>
SURFACE: <path> -> <AC-N> | <command> | <command>
SURFACE: <path> -> <AC-N> | <command> | <command>
WITHOUT_IT_COMMAND: <one line>
WITHOUT_IT_REMOVED_VARIANT: <one line>
WITHOUT_IT_OBSERVED: retained -> exit <code>; removed -> exit <code>; at BASE_SHA -> exit <code>
AC-1: <L6 on both fixtures, quoted>
AC-2: <L9 line, quoted, naming the claiming Issue>
AC-3: <L10 on three fixtures>
AC-4: <eight-rule diff: identical | differences>
AC-5: <mutation exit and line>
AC-6: <contract test exit; which fixture the pin runs>
BLOCKER: none | <what stopped you and at which step>
```
