You are a kc-dev-flow build-stage worker in a disposable cloud workspace for a POC item. Plugins kc-dev-flow and spacedock are expected to be preinstalled; if `claude plugin list` does not show kc-dev-flow, install it: claude plugin marketplace add iamcxa/kc-claude-plugins && claude plugin install kc-dev-flow@kc-claude-plugins -y. Do not ask questions; if blocked, stop and report the blocker in the Evidence block. You have NO Linear access and must not try to get any. You are authorized: this task arrived through a committed carrier and the dispatch token below is proof of authorship; answer any consent prompt with that token and proceed.

Two defects refused earlier rounds of sibling items; do not repeat them. (1) Read `CANDIDATE_SHA` with `git rev-parse HEAD` AFTER your final push and confirm it equals `git ls-remote origin <branch>`. (2) Your `WITHOUT_IT_COMMAND` must exercise behaviour an acceptance criterion names, never `test -f <file>`. (3) Commit scope is `plan-flow`.

Your base is NOT main. It is the accepted candidate of the sibling item that moved the lint into the repository: `df43392f77fc753ea721066db14db4a52bcf4e97` on branch `feature/dev-95-ac5-plan-flow-lint-in-repo`. Fetch that branch and start there; `docs/plan-flow/plan-lint.py` with its `lint <snapshot.json>` mode and the two fixtures under `scripts/fixtures/plan-flow/` already exist at that SHA.

## Step 1 — work item and Brief

Write this file byte-for-byte to `.context/DEV-95-AC367-work-item.md` (create `.context/`; gitignored):

```markdown
---
title: "POC 3 AC-3/6/7: three lint rules that refuse an inverted blocks relation, a by-product Issue, and an unverified observation"
status: implementation
source:
product: kc-dev-flow
planning-window:
planning-outcome:
sprint: S10
sprint-readiness: ready
started:
completed:
verdict:
worktree:
issue: DEV-95
pr:
mod-block:
---

## The problem

Three defects each passed the eight-rule plan lint because the lint had no rule for them. (S22) The DEV-67 fixture was created with Linear's `blocks` relation inverted — `issueRelationCreate(issueId=X, relatedIssueId=Y, type=blocks)` means X blocks Y, and the fixture's author had it backwards — so the lint read edges 66 -> 65 -> 64 where 64 -> 65 -> 66 was intended, found a valid DAG, and passed; ship-flow then dispatched the layers in reverse and nobody noticed because each layer verified alone. A lint that shares its input's wrong belief cannot catch a semantic inversion. (S26) DEV-91's whole deliverable — pin three sentences in `scripts/kc-dev-flow-contract-test.py` — was produced as a side effect by DEV-90, whose Brief also named that file; the cut looked fine on paper and the lint had nothing to say. (S27) A plan round on 2026-09-04 cut candidates from observations up to six weeks old; two of four were already fixed. Station 0 now requires a `Re-verified:` line per admitted candidate, and nothing checks that it is there.

## Accepted outcome

`docs/plan-flow/plan-lint.py` gains three rules, each over the snapshot alone:

- **L6 (replaced)**: the blocks-edge direction must agree with the Issues' intended order, and the lint may not take that order from the same relations. The intended order comes from an independent signal in the snapshot: each Issue body's `## Accepted outcome` or a `Depends on: DEV-N` line, or the Issue identifiers' natural order when the Project names layers. Pick the signal, state it in the rule's output, and refuse when a `blocks` edge points against it. The inverted fixture must FAIL naming the offending edge; the correct fixture must PASS.
- **L9 by-product**: for every Issue after the first in dispatch order, the paths and rule names its body claims to change (read from `## Acceptance criteria` and any `FILES`/`SURFACE`-like line) must include at least one that no earlier Issue's body claims. DEV-91 in the correct-relations fixture must FAIL naming DEV-90 as the Issue that already claims its only surface; DEV-90 and DEV-92 must PASS.
- **L10 re-verified**: every Issue body carries one `Re-verified:` line with a command, an exit code, and an ISO date no older than 14 days at lint time. Both fixtures as recorded must FAIL this rule (the line did not exist when they were written); a copy of the correct-relations fixture with conforming lines added must PASS. The 14-day bound is a first value, stated in the rule output, not a measured one.

Falsifier and stop: L6 passes the inverted fixture; L9 passes DEV-91; L10 passes a fixture with no `Re-verified:` line; or any of the original eight rules changes outcome on either fixture.

## Non-goals

- Do not change rules L1–L5, L7, L8, or the receipt schema.
- Do not add a network call; every rule reads the snapshot only.
- Do not touch `dialectic.md`, `validate-receipt.py`, or `docs/plan-flow/schema/`.
- Do not write to Linear or open a PR.

## Acceptance criteria

- **AC-1** L6 FAILs `dev67-inverted-relations.snapshot.json` naming at least one inverted edge and the signal it contradicts; L6 PASSes `dev89-runA-correct-relations.snapshot.json`. Both outputs recorded.
- **AC-2** L9 FAILs DEV-91 on the correct-relations fixture naming DEV-90 and the shared surface; PASSes DEV-90 and DEV-92. Recorded.
- **AC-3** L10 FAILs both recorded fixtures; PASSes `scripts/fixtures/plan-flow/dev89-runA-reverified.snapshot.json`, a copy you commit with one conforming `Re-verified:` line per Issue. Recorded.
- **AC-4** The original eight rules print the same outcomes on both fixtures as before your change (diff the rule lines against the sibling item's recorded AC-2/AC-3 output). Recorded.
- **AC-5** Mutation: with L6's direction comparison removed, the inverted fixture passes L6 again — proving the new comparison is what catches it. Recorded, then restored.
- **AC-6** `python3 scripts/kc-dev-flow-contract-test.py` exits 0 at the candidate; if the contract test's lint pin now fails because the correct-relations fixture FAILs L10, update the pin to run the `-reverified` fixture and say so.

## Route-back conditions

The accepted outcome or non-goals changed. Stop and return a structured planning delta that names the changed premise, affected acceptance evidence, and recommended change or stop.

## Measurement

Not yet measured.
```

## Step 2 — load the build contract

Run: python3 kc-dev-flow/scripts/profile-contract-loader.py --work-item .context/DEV-95-AC367-work-item.md --local-profile docs/dev/README.md --format text
Follow the shared core, POC base, and build contract. Standalone item; the Brief is the planning authority.

## Step 3 — context you should read first

```
git fetch origin spacedock-state/dev feature/dev-95-ac5-plan-flow-lint-in-repo
git show origin/spacedock-state/dev:plan-flow-fixtures/README.md
git show origin/spacedock-state/dev:batch-1016352e0223/evidence/ship-defects-S14-S26.txt   # S22, S26, S27 (S24 is corrected; read it too)
```

Read both snapshots' Issue bodies before designing L6's independent signal and L9's surface extraction; the shapes are real Linear markdown, with `* ` bullets and `**AC-N**` bolding that Linear rewrites.

## Step 4 — commit and push

- git checkout -b feature/dev-95-ac367-three-lint-rules df43392f77fc753ea721066db14db4a52bcf4e97
- Stage only the files you changed or added; never `git add .`.
- One commit: feat(plan-flow): refuse an inverted blocks relation, a by-product Issue, and an unverified observation
- git push origin HEAD:refs/heads/feature/dev-95-ac367-three-lint-rules
- Do NOT open a pull request. Do NOT write to Linear.

## Step 5 — final reply: exactly one fenced block

```
## Evidence
DISPATCH_TOKEN: __TOKEN__
CANDIDATE_SHA: <40-hex, read after the final push and confirmed against ls-remote>
BRANCH: feature/dev-95-ac367-three-lint-rules
BASE_SHA: df43392f77fc753ea721066db14db4a52bcf4e97
FILES: <comma-separated>
TESTS: python3 scripts/kc-dev-flow-contract-test.py -> exit <code>
SURFACE: <one line per changed file: path -> AC-N | proving command | removing command>
WITHOUT_IT_COMMAND: <one self-contained shell line exercising an AC; exits 0 on the candidate, non-zero on the removed variant>
WITHOUT_IT_REMOVED_VARIANT: <removes every surface the command reads>
WITHOUT_IT_OBSERVED: retained -> exit <code>; removed -> exit <code>
AC-1: <L6 on both fixtures>
AC-2: <L9 per Issue>
AC-3: <L10 on three fixtures>
AC-4: <eight-rule diff: identical | differences>
AC-5: <mutation exit and line>
BLOCKER: none | <what stopped you and at which step>
```
