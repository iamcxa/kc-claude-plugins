# Corrected-candidate validation

Verdict: **REJECTED for implementation repair and Captain-owned count clarification**, not a Pilot pass. Candidate `9bb526170156a44cff90e2a2fab9eeab081e0eb1`, base `3b37000a16ca2eadad0cb5dfd8e43a5f1d06f0f8`, branch `feature/kc-pr-review-capability-protocol-pilot`. No product edit, status/pin change, gate action or blind paid experiment was performed. The green prior-repair checks below do not erase the new concrete defects.

## Fresh Claude result and complete provenance

Claude returned **NEEDS_FIXES**, with F1 High, F2–F8 Medium and F9–F14 Low. Its full unchanged answer is `claude-result.json`, SHA-256 `a2de335d6175033a219dc1b374bcdca8e3c07c56a39d81562653b5ed6f807a63`; all original caveats and internal usage are retained. Fresh stdin prompt embeds the exact cumulative diff and accepted contract, without author pass claims; `inputs.json` binds prompt SHA-256 `e266849e209301db08a3e2636777af5a1f84850840add84b492c9b048e81e3b8` and diff `c9b2fe0b003b73e1c231628090fdf99b03ca7919225a8cdd311c6fc3424b58d4`. Temporary scripts/prompt/diff remain at `/tmp/capability-revalidation.RXsHVd`.

One request, launched 2026-09-06 03:27:45 UTC, CLI PID 7006 / host exec session 6234; provider session `b96ca5d8-ce91-4571-a605-69ce87107132`. CLI exit 0, subtype success, is_error false, end_turn/completed, no API error, one turn, spawned subagents 0, web fetch/search 0, no permission denials. Duration 920572 ms; API duration 922695 ms. Requested `claude-opus-5 --effort xhigh`, safe mode, disabled slash commands, strict shipped empty MCP, tools empty, JSON and no persistence/resume; authentication `AUTH_OK method=claude.ai`. Main actual model is Opus 5. Effective effort has no separate result field: xhigh is explicit command evidence, not invented provider readback.

Total USD **6.11592275**, including Opus 5 **3.44122975**, Haiku **0.099723**, and Fable advisor **2.57497**. Reported aggregate usage: input 4, cache creation 197204, cache read 138367, output 64825 including thinking 58119. Full modelUsage and advisor iterations are preserved rather than incorrectly re-summed as disjoint counters. No host-issued fallback, second review, paid capability run or blind measurement occurred.

## Actionable batch and each fresh Claude disposition

Evidence files `new-findings-probes.json` and `runner-findings-probes.json` contain actual minimal reproductions. All run on synthetic local fixtures; product bytes are unchanged. The implementation owner receives this batch only through FO; no repair is authorized by the report.

| Claude item | Disposition, concrete falsifier and owner |
|---|---|
| F1 High: post-identity terminal | **Genuine**, AC-2/AC-10. Adapter `main` lines 1640–1665 and 1733–1756: actual finalization with `[null]` exits 2 but drops run_id/config_hash/review_key and emits a four-field REQUEST_INVALID echo despite a minted ReviewIdentity. Existing malformed-input test passes the wrong semantic expectation. Preserve the minted identity and correct phase/reason, not merely schema validity. No approval bypass observed. The repeated StopIteration subclaim remains covered by replay relationship validation, not a separate defect. |
| F2 Medium: provider parse boundary | **Genuine**, AC-5/AC-10; adapter `dispatch` lines 936–968. Actual `structured_output:null` raises AttributeError with zero provider records/invocation audit; NaN envelope raises canonicalization ValueError with zero invocation audit. Required capability must end failed/incomplete with raw-attempt accounting, not escape the per-attempt classifier. Shares terminal handler repair with F1, but needs its own input-boundary test. |
| F3 Medium: monotonic integer bound | **Genuine**, AC-2; adapter `audit`/`dispatch` and AuditEvent's Positive bound. Same audit accepts 100/101 ns, rejects 9007199254740992/9007199254740993. Absolute monotonic readings can exceed the schema's 2^53−1 limit; that threshold is about 104.25 days in nanoseconds, not a guaranteed OS clock epoch. Repair the schema/producer units consistently; do not alter timers merely to improve measured speed. |
| F4 Medium: lost treatment terminal | **Genuine**, AC-8 record fidelity/AC-10. Core `pilot_execute` 1639–1645 calls `pilot_telemetry` 911–914, which demands a collated seal/policy even on a real terminal path. Actual moved-head collate returns INVALIDATED/identity_change; telemetry refuses, and the real `pilot_attempt` wrapper replaces it with generic attempt_failed. Preserve the actual terminal and available/unknown cost independently of success-only telemetry; never skip charged attempts. |
| F5 Medium: outer retry | **Disproved as a required repair** by accepted spec lines 678–691: one backup is allowed only for both-arm frozen-input unavailability; treatment-only invalid/incomplete or excluded control is promotion failure, not a replaceable sample. Existing fixed-slot re-entry refusal is real, but adding latest-success selection would violate this contract. Admission/adjudication failure stops the experiment; any new recovery/reselection route requires Captain approval. Duplicate-invocation diagnostic is grouped with F10/F11, not new retry state. |
| F6 Medium: writable owner receipt | **Genuine**, AC-8 integrity; core `pilot_execute` 1499–1513 grants Write and add-dir to the directory containing the prewritten manifest and runner receipt; `pilot_attempt` 1260–1300 uses exclusive creation in both success and failure. Synthetic schema-valid preplant makes finalization raise FileExistsError, yet real `pilot_join` accepts all ten records and the planted control wallclock_ms=1. Runner-owned measurements must not be accepted from child-writeable authority files. A mere new subdirectory is not automatically sufficient with arbitrary Bash; require a falsifier proving child-written owner metadata cannot be consumed. No paid child or remote access used. |
| F7 Medium: wrong adapter authority | **Genuine**, AC-8; core telemetry 833–839 loads its sibling adapter/catalog, not the pinned treatment arm. A valid unchanged arm preparation/provider trace gives complete cost with matching runner; changing only an ephemeral runner catalog code_change threshold produces Invalid: plan or shape authority drift. Bind evaluation to the recorded arm's behavior or explicitly fail a runner/arm precondition before spend; do not fabricate an arm defect after the call. The receipt guard's schema-only import is not independently shown wrong for unchanged V1. |
| F8 Medium: blind recommendation | **Genuine unchecked field**, AC-8; `pilot_normalize` 2040 passes recommendation verbatim. Same valid normalized fixture accepts `APPROVE` and `APPROVE (profiled treatment)` while neutralizing summary. Real legacy vocabulary divergence remains unmeasured; source legacy event enums are already APPROVE/COMMENT/REQUEST_CHANGES. Fix the allowed-field normalization, not the scoring rule or findings. |
| F9 Low: configuration disposition | **Genuine documented-trigger mismatch**, grouped with F1 (AC-1/AC-10). Spec journey lines 143–145 promises INVALIDATED/configuration_change, but adapter has no such producer; config/plan mismatch raises Invalid into the invalid-intake handler. Correct the existing transition or route a precise contract revision through Captain; merely relabeling it forward-only is not reviewer authority. |
| F10 Low: missing unit input | **Genuine diagnostic closure gap**, core `cmd_pilot_run` 1114–1130 / adjudication 1303. Missing unit_input reaches pathlib.Path(None): actual branch probe raises TypeError before a named refusal; admission planner-arm-dir is likewise unguarded. Require the existing unit-kind fields before constructing paths; no new CLI required. No model launch occurs in the probe. |
| F11 Low: repeated phase | **Genuine diagnostic-only gap**, core `cmd_pilot_compare` 2061–2062: first valid blind phase succeeds, second raises FileExistsError. Existing seal/compare exclusive writes have the same source pattern. Idempotent repeated success is not required; a clear nonzero named refusal that preserves existing sealed files suffices. Group with F10, not F5's unauthorized new attempts. |
| F12 Low: ambiguous inline anchor | **Genuine**, AC-7; adapter `collate` 1171–1178 takes first quote occurrence. Synthetic committed file containing `value = 2` twice and a contribution concerning the second assignment yields inline line 1. Disambiguate using existing accepted provenance or refuse ambiguous inline placement; do not invent a location. |
| F13 Low: capability child environment | **Conditional hardening, not established production-path defect**. Actual `main` command is tool-less; direct dispatch accepts an internal adapter command used by tests. No path giving an ordinary capability child Bash/remote-write tools was established. Do not add an OS-wide sandbox or duplicate harness policy solely from this internal-call hypothetical. |
| F14 Low: CI timing wording | **Conditional authority/evidence issue, not proven absence of measurement**. Spec distinguishes measured added wall time before trigger widening from a hosted dollar-cost claim. Author has local protocol/real-journey timing, and this validation has fresh local timings; hosted CI cost remains unmeasured. Cumulative workflow adds five path selectors (three during repair) and a focused two-command job. Evidence does not establish that the relevant wall-time measurement preceded each trigger edit. Do not amend the Captain-owned precondition or equate missing hosted cost with missing local timing; FO must resolve this sequence/wording obligation before unconditional acceptance. |

Other explicitly unverified Claude questions are not additional defects: severity order is already CRITICAL/HIGH/MEDIUM/LOW/NIT; runtime identity/config/observation integration has real-seam evidence; the optional two-diagram validator validates its requested review pair, not arbitrary docs (no contrary invocation shown). A real MEDIUM-only runtime journey was not rerun; existing runtime approval authority is unchanged, so it remains unverified rather than an asserted failure. Provider support for the schema lookahead has no actual paid capability proof; preserve this as an admission compatibility risk, not a confirmed universal failure or permission for a new paid probe.

Repairs deduplicate to adapter terminal/provider/audit closure (F1/F2/F3/F9), terminal telemetry and pinned-arm evaluation (F4/F7), runner-owned measurement integrity (F6), blind/inline projection (F8/F12), and CLI duplicate/missing-input diagnostics (F10/F11). Count and CI-precondition questions are separate Captain/FO authority obligations. No new outer retry, provider, workflow, ledger, service, dependency or product write is approved.

## Independent repair verification

The eight protocol tests below were separately invoked against unchanged final candidate bytes, not presented as one full-suite run. Commands, outputs, exact durations and final clean-tree readback are in `local-checks.json`.

| Prior group | Fresh observed proof and falsifier |
|---|---|
| 1 sampled routing / control | Exact skill snippet with both flags off returns legacy without calling the model stub; newline Token rejected. The selected Pilot runner fixture refuses control-produced protocol artifacts while retaining incurred cost. Restoring forced opt-in or removing artifact refusal breaks these checks. |
| 2 closed malformed input / unsupported evidence | Actual CLI overlong repository, missing prepared and null fallback return validated schema_failure terminals, no traceback. Committed Latin-1 and >1 MiB selected material become explicit unsupported/missing evidence, no capability requests and incomplete terminal. Unassigned answers/tampered selected material refuse; Pilot normalization rejects malformed findings. |
| 3 complete attempted cost | Failed/all-failed and failed-then-success dispatch retain unknown attempt counts, retry counts and known subtotals, never complete cost. Selected Pilot fixture actually runs stub arm/admission/tool-less adjudication, retains stalled/failed receipts, and refuses subsequent launch once reservations or observed overspend exceed the same total. Unknown usage remains charged by reservation, not skipped. |
| 4 inherited credentials | Selected Pilot child receives no synthetic Git credential helper or SSH socket, keeps synthetic model auth, and cannot push to a private local bare repository; target remains ref-free. This proves the specified Git credential/transport boundary, not arbitrary Bash egress isolation. |
| 5 inline shape / blind prose | Actual cross-capability HIGH/MEDIUM same-defect journey produces one path/line/RIGHT/quote-bound inline, two runtime candidates and one confirmed blocker; final event REQUEST_CHANGES and no approval. Fresh run 122.751 seconds. Equivalent blind samples get identical neutral summary without discarding findings. |
| 6 promised proofs / retained records | Actual post-runtime rehydrate mutation emits validated projection_mismatch, fresh 104.572 seconds. Selected runtime receipt case passes 9/0 and refuses failed lanes carrying candidates without partial append. Existing document table test is executed against an altered temporary retained document (1 failure), then original text (0 failures). |

No additional full green suites were repeated. Selected Pilot fixture: 1/0, 4.532 seconds process time, with expected refusal diagnostics; selected runtime receipt fixture: 9/0, 19.942 seconds process time. All capability/model calls in these checks are local synthetic stubs, never paid provider requests. Their durations are local test mechanics, not review-speed samples or hosted incremental CI cost.

## Historical Claude disposition retained

The original full result and 14 dispositions remain unchanged in `../validation-55b7cd28/`. Original M1-M6 confirmed parts and M8 confirmed document/proof portions now have the six-group evidence above; L1/L2/L5 closure portions are covered by the corrected targeted tests. M3 posting StopIteration subclaim remains unverified, not a demonstrated defect. M7 remains a performance hypothesis reserved for budget-gated measurement, not an optimization assignment. M8's external fixture filename was a historical estimate; six inline fixtures and existing blocker/pending-post tests were already present. L3 was disproved by existing pipefail. L4 is conditional exposure, not a leak from a tool-less adjudicator receiving embedded bytes. L6 had no demonstrated runtime identity mismatch; no new accessor is demanded. Fresh Claude findings will be separately dispositioned, never silently replaced by these historical conclusions.

## Context, retention and necessity

Independent section-level check maps the retained spec's object/question/status tables to schema/catalog, its selection/evidence/collation rules to the adapter, its approval and event rules to the existing runtime, and confirmation/posting rules to unchanged owners. PRODUCT's default-off selected-evidence claim and ARCHITECTURE's external adapter/runtime authority split match the fresh sampled-command and real receipt/inline journeys. This is fresh `project_context` validation evidence for the approved update at the exact candidate; no root-context authority is replaced.

All eight relocated historical blocks are exact substrings of their rejected-candidate originals (`document-checks.json`), independently checked against both original Git objects and the section-by-section correction diff. No missing historical block was found. Current authority/limits/proof requirements remain in retained documentation. The actual newly added diagram is `kc-pr-flow/docs/review-runtime.md`, not the spec and not unchanged ARCHITECTURE context. Its source exactly matches the saved rendered input, SHA-256 `1ad36f5f9a236541c19cd2610eeac5a17f5628306a737fa17e2818073c6db05e`; existing SVG/PNG hashes match author evidence, screenshot inspected. The eight-node/seven-edge intake→plan→evidence→calls→receipt→decision→confirmation→posting order agrees with observed owners. No new render or diagram was created.

Runtime blob `699535682a914a54faa22a29c42a12ad4c6fdb63` is unchanged from prior validation. Posting `70bbe96f5417369a1decf00a9bd9eea372180edc` and daemon `15542da657ccaa19a7518142f8f96dcdcd47a072` remain equal to base. Therefore prior unchanged-owner regression and actual projector-removal without-it evidence are retained as historical exact-owner evidence, not relabeled as newly executed. Current fresh blocker and mismatch journeys exercise that retained projector. Schema/catalog, adapter, runtime projection, existing ablation owner, focused tests/CI and scoped context remain mapped to approved lifecycle/proof obligations; no new service, dependency, posting owner or expansion surface was found.

## Scope and still-unfulfilled outcome

Cumulative standard numstat is 6423 additions + 8 deletions = 6431 lines across 20 files; 169 total lines remain under 6600, not promised repair capacity. The focused 1800 stop is **Captain-owned count ambiguity, not unconditionally passed**:

| Added/deleted focused surface | Exact count and current locator |
|---|---|
| schema | 375 additions, schema file lines 1–375 |
| catalog | 96 additions, catalog file lines 1–96 |
| protocol test and inline fixtures | 1024 additions, protocol test lines 1–1024 |
| corpus fixture | 7 additions, corpus lines 1–7 |
| runtime integration/refusal test | 47 additions, runtime test lines 116–162 |
| ablation Pilot tests and enabling edits | 387 additions + 3 deletions = 390; added Pilot function lines 892–1267 is 376, selector 44 is 1+1, historical fixture isolation 322–329 is 8+1, all-case dispatch 1269 is 1+1, Pilot selector 1274 is 1 |

The narrow four-path count is 1502; including all focused runtime/ablation changes is **1939**, conservatively **139 over 1800**. Even counting only the 376-line new Pilot function (excluding its 14 lines of enablement/historical-fixture churn) gives **1925**, 125 over. No files or lines have been dropped to manufacture a pass.

Original accepted spec at 41186709 says "schema, catalog, protocol fixtures, and their focused tests" without an explicit shell-test exclusion. Its 1700 estimate nevertheless equals the four dedicated estimates 540+180+720+260, while separately listing runtime integration and ablation tests. The historical implementation report at task line 189 defined focused as schema+catalog+protocol test+fixture paths; subsequent Captain approvals changed only total lines and preserved 1800. Thus narrow historical arithmetic exists, but it is not an explicit Captain exclusion of shell-hosted protocol/Pilot tests. This report does not settle that scope ambiguity or raise the stop. FO must present the exact denominator/limit decision with the confirmed repair scope; no repair continuation or unconditional within-limit claim follows from 1502.

AC-1/AC-2/AC-5/AC-7/AC-10 remain incomplete because of the concrete defects above; AC-3/AC-4/AC-6 have bounded exercised implementation evidence but not an independent whole-outcome pass while those boundaries fail. AC-8 has both fixable harness defects and separately NOT RUN paid evidence; AC-9 is NOT RUN. There is no admitted real corpus, five-pair quality comparison, independently frozen paid adjudication, accepted quality floor or measured 33.3% speed improvement. Therefore Pilot outcome remains unproven. Hosted incremental CI cost per PR remains unmeasured. Prior RoboRev UNKNOWN(stale), zero requests remains unchanged.

All budget proof is synthetic: the existing runner now reserves and accounts for admission, arms, retries/backups and adjudication in one experiment directory; subsequent refusal cannot undo a provider overspend, and per-call max-budget-usd is a request rather than a guaranteed hard-dollar ceiling. The historical USD115 / 23-unit planning envelope is not current-model cost assurance or Captain spend approval. Fresh review cost likewise is not blind calibration. Captain still owns exact paid budget/corpus authorization and any changed scope, promotion or publication.

The one-round canonical `briefing.json` + `briefing.review.jsonl` conversion and neutral recorder publication are DEFERRED, NOT DONE under Captain approval state commit `49cded7562b7175c9d951df1ad6ccf5fcb3da94a`, reread and FO-confirmed before fresh review. No defect/test is waived and no substitute gate room or schema is invented.
