# Implementation correction evidence

Candidate: `9bb526170156a44cff90e2a2fab9eeab081e0eb1`; branch `feature/kc-pr-review-capability-protocol-pilot`; delivery base `3b37000a16ca2eadad0cb5dfd8e43a5f1d06f0f8`. This is author implementation evidence, not independent acceptance. Rejected candidate `55b7cd28`, its validation report, and historical stage pin are preserved.

## Authority and counted scope

Captain's total-limit approval was committed and reread at state revision `d60869b0b5908d2756a9e6013f55211788a9a2b6` before product edits. The first product edit changed the active retained limit to 6,600. Final standard `git diff --numstat` from delivery base reports 6,423 additions + 8 deletions = **6,431 / 20 files**; 169 lines remain. Focused schema 375 + catalog 96 + protocol tests 1,024 = **1,495**, or **1,502** including the seven-line corpus, below 1,800. Final correction-commit churn (1,268 additions / 265 deletions) is not the cumulative metric.

All repairs reuse the existing 20 product paths. Requiredness/catalog and closed schema remain authoritative; the adapter selects evidence and collates; runtime append/replay derives the typed decision; human confirmation and unchanged posting remain authoritative. Cost, failed attempts, admission and adjudication reuse the existing ablation runner/manifest/receipt owner and existing `run` command, with no independent ledger, CLI, dependency, workflow or service. Existing CI owners cover their actual inputs; the runtime job now watches the core, sampled skill and retained table it exercises, while the evaluation job alone runs full ablation. Hosted incremental cost per PR remains unmeasured.

## Six confirmed repair groups

1. **Sampled routing/control isolation.** The exact skill command inherits sampled flags; off stays legacy with no stub capability call. `test_sampled_skill_command_stays_off_and_tokens_are_closed` first failed at the route assertion and passed after repair (1/0, 0.273 s). Actual control runner output containing `protocol/prepared.json` was accepted before the guard and refused after it; its attempted-run receipt retains known cost. No fixture-only substitute for the exact snippet.
2. **Malformed input and unsupported material.** Actual CLI runs for a 257-character repository, missing `prepared`, and fallback `[null]` first produced tracebacks, then validated terminal JSON (1/0, 1.524 s). Committed Latin-1 and over-1-MiB selected material now produce explicit missing-evidence classification rather than caller-schema failure (1/0, 4.029 s). Newline tokens/repositories are rejected; out-of-plan results return `REQUEST_INVALID/schema_failure` (previous `required_gap` falsifier, 1/0, 1.199 s); malformed blind findings receive named refusal. Required-gap semantics were not broadened.
3. **Failed receipts and total cost.** Actual synthetic failed/all-failed and failed-then-success capability dispatch preserves every raw attempt, known subtotal and incomplete total (1/0, 3.097 s), never skips unknown usage or promotes it. The existing shared reservation sums each prior unit as at least its reservation, or its greater known incurred cost; missing/unknown usage retains the full reservation. Synthetic admissions, arms, retries, backup selection and independent adjudication share that budget and closed manifest/receipt branches. Prior 0.7 cost plus next 0.4 reservation under a 0.8 total first incorrectly launched and then refused. Missing/late/mismatched admissions, missing adjudication, unknown manifest keys, malformed terminal and incomplete effective samples are refused. Admission uses the independently pinned treatment planner and ordinary control triage before corpus freeze; comparison verifies their identities and chronology. A final stub exposed missing adjudicator `--json-schema`; the corrected tool-less request carries a closed result schema and exact input digest (Pilot RED, then 1/0 in 3.77 s).
4. **Synthetic credential isolation.** Shared child invocation clears inherited Git/GitHub/SSH variables, disables global/system Git config and transport protocols, uses an empty GitHub config, and retains unrelated model authentication. The synthetic host proved inherited credential helper and SSH socket unavailable, model-auth sentinel retained, and an actual push to a private local bare target refused with no refs created. This is the requested credential/transport boundary, not an OS-wide arbitrary-egress sandbox claim.
5. **Inline compatibility and arm-hidden samples.** A real quote-bound cross-capability HIGH/MEDIUM same-defect finding initially had zero inline comments; after repair the actual finish/rehydrate journey produced one merged, exact path/line/quote inline finding, two runtime candidates and forced REQUEST_CHANGES (1/0, 112.807 s). Normalization retains comparison findings but generates an arm-neutral summary; equivalent control/treatment findings no longer carry treatment protocol prose. Posting was not changed.
6. **Promised authority proof and retained documents.** `contract_tables()` derives question and status tables from the catalog/schema; the test reads the actual retained table and rejects drift (missing generator RED, then 1/0, 0.001 s). Actual rehydrate output mutated after the existing runtime invocation yields a validated `projection_mismatch` terminal (1/0, 83.264 s), not merely an enum fixture. Runtime `--case profiled-receipt` now rejects candidate-bearing failed lanes with no partial append (9/0). Already-covered confirmed-blocker and pending-post cases remain unchanged. Historical records and actual diagram proof are below.

## Final verification and exact owner reuse

- `python kc-pr-flow/scripts/review-capability.test.py PlannerTests.<each selected method>` ran **21 tests / OK**, 190.850 s (process real 190.99 s). Selection was every method except `test_cross_capability_high_finding_merges_and_forces_request_changes` and `test_actual_rehydrate_projection_mismatch_is_terminal`, whose already-green actual long journeys above were not rerun. Together this covers all 23 current methods, **not a single 23/0 invocation**. Adapter and protocol-test bytes below match all three runs. The final adjudication-only core change is outside the core functions used by these protocol tests.
- `bash kc-pr-flow/scripts/review-ablation.test.sh` completed **83 passed / 0 failed**, real 48.79 s, before the final adjudication schema assertion/fix. Then `bash kc-pr-flow/scripts/review-ablation.test.sh --case pilot` failed as intended and passed **1/0**, real 3.77 s, on final bytes. Legacy 82-case owner bytes were unchanged by that last fix. Do not describe the final candidate as one freshly executed 83/0 run.
- `bash kc-pr-flow/scripts/review-runtime.test.sh --case profiled-receipt`: **9/0** on final test/runtime bytes. Prior core runtime 372/0 and shadow 213/0 are reused only because their runtime owner is unchanged from rejected candidate; original split 17-case evidence remains historical and is not relabeled.
- Final Ruff checks passed for adapter/test; core passed with pre-existing UP031 excluded. Adapter/test formatting check, Bash syntax for changed test scripts, and `git diff --check` passed. No dependency was installed. Local Python 3.13.2 used the declared activated existing venv; this is not a hosted Ubuntu result.
- Local observed mechanical overhead in the selected run: projection 73.957477459 / 75.484491125 s, rehydrate 5.887656166 / 5.932452667 s, prepare 0.912665708 s. These are runtime mechanics, **not review speed or treatment improvement**. The sum of separately exercised protocol cases is about 387 s locally; hosted runtime/job timing is unmeasured and the separate existing job retains its eight-minute timeout.

Final Git blob bindings:

| Owner | Blob |
|---|---|
| review-capability.py | `8d976cea8457f666c6b1c104a177dc23442a23d9` |
| review-capability.test.py | `62abacf02d356cf4d887ef27c6d608eab349bd04` |
| review-ablation-core.py | `790657c13a0fdd287590015bd631d152fb680b0b` |
| review-runtime.sh | `699535682a914a54faa22a29c42a12ad4c6fdb63` |
| review-runtime.test.sh | `0b80186986f7835dda2a686f316b6c7f13e9aab4` |
| review-post.sh (equals delivery base) | `70bbe96f5417369a1decf00a9bd9eea372180edc` |
| pr-review-daemon.sh (equals delivery base) | `15542da657ccaa19a7518142f8f96dcdcd47a072` |

## Retained context and actual render

The 104 lines of eight historical blocks were preserved verbatim in [implementation-record-relocation.md](implementation-record-relocation.md), committed/synced and reread at `5baa89fd9b03aa3770ae84fee5dc3c579a9d4f19`, before deleting their retained-doc copies. The blocks were work-state/pending context, reverse-audit record, old file estimates, allowance/test/timing records; accepted outcome, active stop limits and authority boundaries remain in product documentation. Actual net record-only reduction was 101 lines. This is realized relocation credit, not the earlier assumed 106-line candidate.

PRODUCT and ARCHITECTURE continue to describe external default-off Lite planning/selected evidence and existing runtime-derived confirmation/posting. The runtime usage commands now explain admission/adjudication through the existing runner and qualify cost enforcement. The retained table is exercised from those actual product bytes.

The added diagram is **kc-pr-flow/docs/review-runtime.md**, its `flowchart LR` block, not the unchanged adjacent ARCHITECTURE diagram and not a diagram in the retained spec. Our initial two-file search and proposed source-disproof were wrong and are explicitly withdrawn. The original validation render obligation was fulfilled, not dismissed.

Rendered with existing `agent-browser`, own session `capability-doc-render`, at Mermaid Live Editor **11.17.2**. Initial editor paste auto-indented/duplicated input and errored; it was corrected using the editor's existing encoded state with the exact source block. The final source rendered successfully: eight expected node labels and seven directed edges, no syntax error, and visual inspection found connected nodes without overlapping labels. Source-to-code order was checked: intake/plan/evidence/capability calls in the adapter, receipt projection and typed decision in the runtime, then existing human confirmation and posting owner. No new diagram, dependency, Save/Share publication, or product probing was introduced; the own browser session was closed.

Artifacts: [exact input](runtime-usage-render.mmd), [rendered SVG](runtime-usage-render.svg), [screenshot](runtime-usage-render.png). SHA-256 respectively:

- source `1ad36f5f9a236541c19cd2610eeac5a17f5628306a737fa17e2818073c6db05e`
- SVG `a86b73f91466aea7af972af90792a8636df5c9e11e8fb9159551cc59ce7758ca`
- PNG `e955aea34570f387d93d137867460ab78e2e278ee1a9bd0aafa603d9e891acdc`

## Unchanged limitations and next authority

The guard is **prelaunch reservation plus refusal of subsequent unaffordable work**, and per-call existing `--max-budget-usd`; it is not a guaranteed provider-enforced hard dollar ceiling. A provider can overspend a single request; subsequent known overspend is charged and blocks further launches, but cannot undo already incurred cost. All enforcement evidence here uses stubs. No real admission, blind arm, adjudication or paid calibration was run. The historical six-review ledger (latest 2026-03-15, maximum USD 4.9612) remains old composite daemon telemetry, not current-model cost assurance. AC-8/AC-9 quality and speed results remain budget-gated and unclaimed.

The prior non-gating RoboRev observation remains UNKNOWN(stale), zero provider requests, without retry or new observation. All original Claude confirmed/disproved/conditional/unverified dispositions remain in the unchanged rejection evidence; performance is still a hypothesis, not a new repair goal. The missing canonical neutral review-round producer is owned by the First Officer, not fabricated here. No branch push, PR mutation, status/pin change, independent acceptance or self-dispatched review occurred. Return this exact committed candidate to the retained validator through the First Officer.
