# Local correction: observed failing-to-passing evidence

This is author execution evidence, not a review or an acceptance decision. Initial focused run outputs below were observed in the worker tool transcript; they were not redirected to files, so this document records excerpts/results rather than claiming reconstructed raw logs. Final full-suite output and the five later removal mutations are preserved verbatim alongside this file.

Product commands ran under `/Users/kent/conductor/workspaces/kc-claude-plugins/kc-pr-review-capability-pilot`, after activating `/Users/kent/conductor/workspaces/recce-cloud-infra-v1/brussels/.venv/bin/activate` and checking `which python`. Python command prefix was `python -B kc-pr-flow/scripts/review-capability.test.py`; each name below is a `PlannerTests` method. Shell command was `bash kc-pr-flow/scripts/review-ablation.test.sh --case pilot`. No provider/model was called: fixtures inject local subprocesses and transport stubs.

| Finding | Actual RED | Scoped correction and observed GREEN |
| --- | --- | --- |
| F1 — minted identity | `test_malformed_cli_artifacts_always_return_valid_terminals`: 1 test, 1 failure, 1.216 s; post-mint fallback `[null]` terminal omitted config_hash/review_key/run_id. | Capture the minted identity before dispatch/finalize; retain REQUEST_INVALID/schema_failure and full ReviewIdentity. Regression checks equality, not only schema. Accepted locator: retained spec lines 316–322. |
| F2 — provider shapes | `test_provider_shapes_and_large_monotonic_readings_keep_attempts`: structured_output:null raised AttributeError (`None.get`); NaN envelope raised canonical ValueError at persistence. | Reject invalid structured shapes/nonfinite serialization into terminal_failure while storing exact provider stdout and every invoked attempt. Both malformed subcases assert one raw artifact and audit entry per request. |
| F3 — monotonic bounds | Same test: actual 2**53 timestamp raised schema-union Invalid. | MonotonicNs is a positive integer without floating-safe-integer maximum; actual audit accepts 100, 2**53 and 2**63 and rejects bool, zero and negative. No timer origin/unit substitution. |
| F9 — configuration | `test_configuration_mismatch_is_invalidated_at_collation`: actual plan-hash drift raised Invalid rather than terminal. | Existing request binding boundary raises internal ConfigurationChanged, collate returns exact INVALIDATED/configuration_change and original identity. F1/F2/F3/F9 final focused run: 3 tests OK, 7.568 s; preceding F2/F3/F9 run: 2 tests, 4 errors, 4.207 s. |
| F7 — pinned arm | `test_failed_and_retried_provider_usage_is_never_skipped`: both recover=false/true errored with unexpected arm_dir; 1 test, 2 errors, 3.468 s. The retained validator separately preserves the original real runner/arm catalog drift failure. | Telemetry loads the passed pinned arm adapter. Regression makes sibling runner location nonexistent, then compares metrics with the actual arm; failed/retried usage remains accounted. GREEN 1 test, 3.408 s. This RED is the missing explicit arm seam, not a fabricated replay of the validator's catalog case. |
| F4 — failed terminal/cost | `test_terminal_attempt_keeps_identity_and_partial_cost_without_a_seal`: 1 error, 1.505 s; actual identity_change terminal became `side-car does not seal a collated run` and generic failure. | Real dispatch/collate/telemetry/execute/attempt path preserves terminal without success seal, outer known cost 0.1 and unknown inner usage, cost_status=incomplete and cost_usd=null. Only provider transport and arm guard are stubbed; no paid call. |
| F12 — duplicate quote | `test_ambiguous_inline_retains_finding_without_guessing_a_line`: 1 failure, 1.195 s; two equal source quotes produced guessed line 1. | Keep finding/severity/body and omit ambiguous inline placement. F4+F12 GREEN: 2 tests OK, 3.767 s. |
| F8 — blind label | Pilot shell case accepted `APPROVE (profiled treatment)`, causing `AssertionError: invalid fixture was accepted`. | Existing normalizer enforces APPROVE/COMMENT/REQUEST_CHANGES; findings/scoring remain intact. Existing pilot case subsequently GREEN 1/0, then final full shell suite. |
| F10 — missing input | Five later mutation runs include removal of admission unit_input, admission planner_arm_dir and adjudication unit_input checks; each actual command produced TypeError and exit 1 rather than named diagnostic. | Existing runner checks before Path construction; assertions require expected stderr and nonzero exit. See mutation raw logs and full shell GREEN. |
| F11 — repeat phases | Initial repeated blind invocation raised FileExistsError; later selective removal mutations for seal/compare each reproduced FileExistsError and exit 1. | Existing write/blind owners produce named nonzero refusal; regression compares existing sealed/blind bytes. No replacement sample, retry or idempotent-success semantics. See mutation raw logs and full shell GREEN. |

Two fixture-development failures are explicitly excluded from product RED counts: the first malformed-input setup attempted exclusive `store` twice (FileExistsError), then used write_bytes for intentional fixture replacement; the first diagnostic helper expected text in SystemExit(1), then correctly captured stderr. Neither is product-fix evidence.

`mutations.py` copies the unchanged final product plugin into owned temporary directories and removes one guard at a time. `mutations.json` and the five `*-red.log` files record commands and actual failures. These are diagnostic removal instruments, not relocated regression tests; every permanent assertion remains counted in the product's focused denominator.

## Equivalent in-file reduction

The protocol test file decreased from 1,024 to 966 lines while retaining old assertions and adding the assigned regressions. Shared same-file module loading, CLI, posting-log, manual-fallback and contribution fixtures remove duplicated construction. The successful budget/raw-audit dispatch checks now run within the actual successful rehydrate/posting journey; manual fallback binding/refusal checks now run within the actual incomplete-fallback rehydrate journey. The two merges retain all assertions, including removal of the temporary `unexpected` field before exercising the original `unknown` case. No tests moved to another denominator or external dependency.

Including schema growth, all ablation test changes and new regressions, focused count decreased only 36 net: 1,939 to 1,903. This is 103 over the unchanged 1,800 cap. It is an attempted but insufficient reduction, not a fit claim.
