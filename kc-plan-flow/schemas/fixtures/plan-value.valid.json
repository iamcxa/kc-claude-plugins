{
  "schema": "kc-plan-value/v1",
  "project": {
    "name": "Share artifacts publicly from the terminal",
    "user_value": "A terminal user hands one link to a reviewer and reads every comment back where they started.",
    "hypothesis": "If we let a reader publish what they are looking at and read the replies in place then they will ask for review before the work hardens",
    "wedge": "The publish path is the one broken link; everything downstream of it already works.",
    "outcome": "A person who installed from the tap publishes a file and hands out a link that opens for anyone.",
    "exit": [
      "A tap install publishes to production and returns a working link",
      "A reviewer comments in a browser and the owner reads it back in the terminal"
    ],
    "boundaries": [
      {
        "rule": "The reader owns one local review session and holds no relay knowledge; every relay operation lives in the separate client.",
        "agreed_with": "the host repository's maintainer",
        "agreed_at": "subspace-v0#43"
      },
      {
        "rule": "Invocation runs both ways and neither binary is the parent, so each locates the other and each refuses a counterpart too old for the contract it needs.",
        "agreed_with": "the host repository's maintainer",
        "agreed_at": "subspace-v0#43"
      }
    ]
  },
  "milestones": [
    {
      "name": "A - Publishing works from the terminal",
      "target": "2026-09-13",
      "description": "A terminal user publishes a file and gets a link that works for anyone. Ships on a tap install, not from a checkout."
    }
  ],
  "issues": [
    {
      "title": "A terminal user publishes a file and gets a link",
      "acceptance": "A user runs the client against a local file and gets back a link that works for anyone holding it",
      "milestone": "A - Publishing works from the terminal",
      "kind": "value",
      "independent_because": "nothing it needs is missing on the trunk; the archaeologist classified its surface WORKING"
    },
    {
      "title": "Credential minting does not widen permissions outside the publish path",
      "acceptance": "Publishing a file whose path holds no state-root ancestor leaves unrelated parent directories untouched",
      "milestone": "A - Publishing works from the terminal",
      "kind": "defect",
      "protects": "A terminal user publishes a file and gets a link"
    }
  ],
  "questions_for_the_archaeologist": [
    "What does the trunk carry today for publishing a local file to a remote destination?"
  ],
  "needs_a_ruling": [
    "Whether the architectural parent whose children span three milestones stays open as an issue"
  ],
  "dependencies": [
    {
      "blocked": "Credential minting does not widen permissions outside the publish path",
      "blocked_by": "A terminal user publishes a file and gets a link",
      "because": "the second issue's acceptance run needs the artifact the first produces, established by archaeology rather than by the order these were written"
    }
  ],
  "captain_ruling": {
    "ruled_on": "2026-09-08",
    "on": "the two value points, the milestone and its single dependency",
    "changed": [
      "the second value was split out of the first, which had been one ticket spanning both"
    ]
  }
}
